# 飞书读取覆盖

更新时间：2026-08-08  
目标窗口：2026-07-09 至 2026-08-08

逐日明细：[近 30 天逐日聊天、会议与纪要明细](./近30天逐日聊天与会议明细.md)

## 本次成功读取

- 通过全局消息搜索读到 2026-07-09 至 2026-08-07 的工作日报主线。周末和个别日期没有独立日报，不能据此判断当天没有工作消息。
- 成功读取“座舱大模型助手产品群”近 3 天消息，共 47 条，并展开了可访问的高德 Agent 跟踪文档。
- 成功读取 Aime 个人助理“今天”的消息，结果为 0 条。
- 成功检索以下主题的近 30 天消息：动态示例、触发器、高德 Agent、记忆触发器、Planner 编排。部分结果因正文或关联文档过长被截断。
- 成功读取“触发器定时功能技术方案评审”的智能纪要和部分文字记录。
- 成功读取近 30 天主日历的元信息：返回 90 个日程，但结果被截断，因此不能声称已经逐场读完。

## 结果完整度

| 来源 | 状态 | 是否可视为完整 |
|---|---|---|
| 工作日报 7/9 至 8/7 | 成功，按日期检索 | 主线较完整，仍是二次摘要 |
| 座舱大模型助手产品群近 3 天 | 成功，47 条 | 仅近 3 天完整，不代表 30 天 |
| Aime 今天 | 成功，0 条 | 仅今天 |
| 动态示例搜索 | 返回 20 条，has_more=true | 不完整 |
| 触发器搜索 | partial/truncated，has_more=true | 不完整 |
| 高德 Agent 搜索 | 7 条，has_more=false | 该关键词结果完整 |
| 记忆触发器搜索 | 15 条，正文截断 | 消息命中数完整，正文不完整 |
| Planner 编排搜索 | 10 条，正文截断 | 消息命中数完整，正文不完整 |
| 近 30 天日历 | 90 个日程，truncated=true | 不完整；日历也不能单独证明实际参会 |

## 当前权限或工具缺口

- 群聊总枚举多次出现 `fetch failed`，因此无法证明已经覆盖全部群。
- Aime 近 7 天、近 30 天整段读取曾超时；本次改用全局搜索补日报。
- 普通单聊不一定能通过会话枚举完整读取，需要已知 chat_id 或关键词命中。
- 部分消息图片、视频和附件读取失败，原因是开放平台要求机器人位于原消息会话。
- “上汽&字节 Bug 日清会”“指标评审 with 字节”缺会议纪要权限。
- 两场地图搜索策略讨论已在 2026-08-08 复测成功，现可读取智能纪要和逐字稿；此前“妙记正文无权限”的判断已撤回。
- “上汽&字节 Bug 日清会”“指标评审 with 字节”复测仍明确返回 `Feishu API 121005: no permission`。
- 部分其他会议返回 `Feishu API 99991400: request trigger frequency limit`，这是接口限流，不属于权限问题。
- `ai car 数据构建及评测` 群未稳定定位。不能说它没有消息，只能说本次没有可靠覆盖。
- 近 30 天日历返回 90 个日程后被截断，且不少日程没有 `current_user_participation`，只能证明有安排，不能证明实际参加。

## 因此，本次总结的证据等级

- **高可信**：座舱产品群近 3 天原文、高德 Agent 跟踪文档、触发器定时评审纪要。
- **中可信**：工作日报中连续出现、且被群聊或文档交叉印证的线程。
- **待复核**：只在某一天日报出现、存在口径反转、旧 DDL 已经过期、或会议正文无权限的结论。

## 下次每日增量怎么读

1. 先读今天全部可访问的新消息与会议。
2. 对照 `当前问题线程.md`，只更新“新增证据、结论变化、Owner变化、DDL变化、风险解除或新增”。
3. 对近 30 天中仍未关闭的线程继续回溯，不把旧问题因当天没提到就自动删除。
4. 每次输出都附覆盖与权限缺口，不用“已读取全部消息”这种超出证据的话。
