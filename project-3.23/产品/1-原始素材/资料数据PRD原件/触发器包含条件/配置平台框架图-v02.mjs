import fs from 'node:fs';
const out = new URL('./配置平台框架图-v02.svg', import.meta.url);
const C={ink:'#173458',blue:'#185DB7',pale:'#EDF4FF',line:'#8AA3C1',gray:'#F4F6F8',muted:'#52657B',green:'#EBF5F0',olive:'#586D54',warm:'#FFF6E8'};
let s=[];
const esc=x=>String(x).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
function rect(x,y,w,h,fill=C.gray,stroke='none',r=12){s.push(`<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="${r}" fill="${fill}" stroke="${stroke}" stroke-width="2"/>`)}
function text(x,y,lines,size=22,weight=400,color=C.ink,anchor='start',lh=32){s.push(`<text x="${x}" y="${y}" font-size="${size}" font-weight="${weight}" fill="${color}" text-anchor="${anchor}">${lines.map((l,i)=>`<tspan x="${x}" dy="${i?lh:0}">${esc(l)}</tspan>`).join('')}</text>`)}
function line(points,arrow=true,dash=false){s.push(`<polyline points="${points}" fill="none" stroke="${C.line}" stroke-width="2.5" ${arrow?'marker-end="url(#arrow)"':''} ${dash?'stroke-dasharray="7 5"':''}/>`)}
function title(y,n,t,sub){text(48,y,[`${n}  ${t}`],29,700);if(sub)text(48,y+36,[sub],21,400,C.muted);}
s.push('<svg xmlns="http://www.w3.org/2000/svg" width="1700" height="1950" viewBox="0 0 1700 1950"><defs><marker id="arrow" markerWidth="12" markerHeight="12" refX="9" refY="4" orient="auto" markerUnits="strokeWidth"><path d="M0 0 L10 4 L0 8 z"/></marker></defs>');
rect(0,0,1700,1950,'#FFFFFF','none',0);
text(48,64,['系统预设任务 · 配置平台业务框架'],40,700);
text(48,110,['把已接入能力组合成完整任务：条件满足后做什么，用户回应后怎么办，何时结束。'],25,400,C.muted);
title(178,'一','能力库：平台让配置人员选什么','能力团队接入；平台按渠道、车型、版本和端云位置筛选可用能力。');
const cap=[['状态与识别','任务开关、档位、车速、车辆开关','天气／路面／儿童／充电设备识别'],['交互与用户反馈','即时交互卡、按钮、语音播报','确认、取消、关闭、超时'],['动作与执行结果','车辆动作、主动推荐、识别调用','实际车辆状态、失败、无法确认']];
cap.forEach((a,i)=>{let x=48+i*543;rect(x,238,518,130,C.gray);text(x+24,278,[a[0]],26,700);text(x+24,315,a.slice(1),22,400,C.muted,undefined,31)});

title(431,'二','配置主线：配置人员依次完成五步','平台根据所选处理方式展示配置项，不需要交互的任务不必填写卡片和播报。');
const steps=[
 ['1  选任务与范围',['关联哪项预设任务','用于哪些车型、版本','由端侧还是云端运行'],['决定：谁能使用这项任务']],
 ['2  配触发规则',['何种变化启动检查','当前还须满足哪些条件','全部满足／任一满足'],['决定：什么时候才触发']],
 ['3  选处理方式',['直接执行 ｜ 仅告知','先询问 ｜ 发起主动推荐','选择对应动作或承接业务'],['决定：命中后走哪条路']],
 ['4  配交互与后续',['卡片、按钮、播报内容','确认后动作与执行顺序','取消、超时和结果分支'],['决定：每种回应怎么办']],
 ['5  设运行策略',['持续时间、次数、间隔','等待时长、失效条件','退出条件与退出动作'],['决定：何时停、何时再来']]
];
steps.forEach((a,i)=>{const x=48+i*326;rect(x,496,300,262,'#FFFFFF',C.blue);rect(x,496,300,56,C.pale,'none',12);text(x+20,533,[a[0]],25,700);text(x+20,594,a[1],22,400,C.ink,undefined,36);text(x+20,730,a[2],20,500,C.blue);if(i<4)line(`${x+300},524 ${x+326},524`)});

title(828,'三','配置结果长什么样：用“雨天切换湿滑模式”走一遍','本例选择“先询问”。平台必须能表达以下顺序和分支，运行时由对应业务执行。');
const nodes=[
 [48,900,206,160,'收到检查事件',['天气变为下雨','或路面变为湿滑']],
 [280,900,300,160,'判断当前条件',['任务开启；车速＞30 km/h','下雨或路面湿滑','当前不是湿滑模式']],
 [606,900,320,160,'展示询问卡与播报',['“要切换湿滑模式吗？”','按钮：确认／取消','本地语音匹配同一按钮']],
 [952,900,300,160,'确认后再次检查',['询问未失效、未处理','任务与最新车况仍允许']],
 [1278,900,374,160,'执行并确认实际结果',['切换湿滑模式','读取实际驾驶模式','达到目标才反馈成功']]
];
nodes.forEach(([x,y,w,h,h1,ls],i)=>{rect(x,y,w,h,i===2?C.warm:C.pale);text(x+18,y+38,[h1],24,700);text(x+18,y+78,ls,21,400,C.ink,undefined,29);if(i<4)line(`${x+w},975 ${nodes[i+1][0]},975`)});
text(934,959,['确认'],18,600,C.blue,'middle');
rect(280,1125,972,62,C.gray);text(766,1166,['结束本次，不执行车辆动作；长期任务保持开启时继续等待'],23,500,C.ink,'middle');
line('430,1060 430,1125');text(442,1100,['不满足'],19,400,C.muted);
line('766,1060 766,1125');text(778,1100,['取消／关闭／超时'],19,400,C.muted);
line('1102,1060 1102,1125');text(1114,1100,['复核不通过'],19,400,C.muted);
rect(1278,1125,374,62,C.green);text(1465,1165,['更新成功／失败／无法确认'],22,500,C.ink,'middle');line('1465,1060 1465,1125');

title(1252,'四','产品策略：配置值由场景定，处理规则由公共框架支持');
const pol=[
 ['触发前','任务关闭、数据过期 → 不触发','持续判断 + 次数／间隔限制重复'],
 ['询问中','真正展示后再等答复、播报','语音禁用保留点击；失效则撤销'],
 ['执行时','同一次确认只能执行一次','先检查最新车况，再调用车控'],
 ['结束后','失败或未知不默认重试车控','退出不等于自动反向操作车辆']
];
pol.forEach((a,i)=>{const x=48+i*408;rect(x,1283,380,134,C.gray);text(x+18,1321,[a[0]],25,700);text(x+18,1360,a.slice(1),20,400,C.ink,undefined,31)});
text(48,1451,['需在场景规则中确认：频控计数口径、拒绝后再次提醒、优先级与卡片展示的配合、等待和退出参数。'],21,400,C.muted);

title(1523,'五','校验与生效：平台校验配置，运行模块执行任务');
rect(48,1553,345,135,C.pale);text(69,1592,['平台校验'],25,700);text(69,1630,['能力是否适配、必填项是否齐全','先后顺序与结果分支是否完整'],21,400,C.ink,undefined,30);
rect(467,1553,315,135,C.pale);text(488,1592,['通过：发布到指定范围'],25,700);text(488,1630,['云端：扩展现有配置链路','端侧：当前仍按需求开发发版'],20,400,C.ink,undefined,30);
line('393,1602 467,1602');
rect(856,1553,796,135,C.green);text(877,1592,['运行职责不变'],25,700);text(877,1630,['任务中心／接入业务提供真实启停状态；触发器判断条件','交互模块展示并返回选择；执行业务调用车控、确认结果'],22,400,C.ink,undefined,32);line('782,1602 856,1602');
rect(48,1722,734,62,C.warm);text(69,1763,['不通过：定位缺失配置或能力 → 补齐后重新校验'],24,500);line('220,1688 220,1722');
text(48,1836,['端云统一配置、下发与同步属于后续建设；运行位置不因断网自动切换。'],24,500,C.blue);
text(48,1894,['复用目标：能力和组合方式支持后，修改条件、文案和目标动作即可复用，不再每个场景单独开发。'],23,500,C.ink);
s.push('</svg>');fs.writeFileSync(out,s.join('\n'));
console.log(out.pathname);
