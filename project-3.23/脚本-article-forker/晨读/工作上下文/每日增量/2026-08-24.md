# 2026-08-24 工作上下文增量

## 本次读取范围

- 飞书只读能力核验成功。当前授权允许读取可访问群聊、消息、会议、妙记、文档与日历；普通私聊无法保证完整枚举，部分图片和附件因机器人不在原会话而不可读。
- 可枚举会话 231 个。对“上汽”近 30 天搜索返回 100 条命中，正文被接口截断；已直接读取触发器、动态 Advisor、需求冲刺、端状态等相关群的近 30 天窗口，部分大群同样返回 partial/truncated。
- 同时读取到 2026-08-24 的全源滚动任务审计消息。该审计称其已尝试 231 个可见会话，其中 230 成功、1 个 Restricted；这是二次审计材料，不替代本次逐条原文读取。

## 今天新增或需要重新确认的事项

1. 8 月 25 日版本节点临近。今天的个人优先级仍是：本人 P0 与 8·25 必解票逐票补 owner、原因、状态与复验/转交回执；动态示例三张票与自动大灯边界示例补齐示例 ID、查重、召回、Fornax、目标环境及实车证据；主动服务四项配置需验证旧任务状态、回滚与实车结果。
2. `PROTO-RSKX-20260821`：SLS、saic-dna、master 三份协议原 DDL 为 8 月 20 日。当前只确认 SLS address 字段已补，尚无整体研发验收。
3. `TRIGGER-ONESHOT-20260819`：首次满足、失败补发、成功停止、恢复重置、上下电与端云职责仍需写成状态机，并补验收用例。
4. `TRIGGER-SEQUENCE-20260820`：下车道别的短期 Backup 是“开门”触发；跨上报顺序、状态暂存、时间窗能力列为长期问题。8 月 25 日前缺文档与实车验收证据。
5. 触发器群的原始消息还显示：Dev/Test 环境已有改动，其他环境尚未部署；多个 advice 同时下发会出现后续车控未创建；Advisor 是否输出需要结合车辆 Context。
6. 端状态群的原始消息显示：出现过“端状态上报成功但未入库”；数值范围校验目前未完整实现，若做范围定义还需评估端侧改动和存量上报兼容。

## 与今日 FDE 文章的真实连接

文章讲的“数据脏、旧系统不通、Demo 到上线有鸿沟”，直接命中三条仍在推进的线程：

- 三份协议与端状态入库，对应企业数据和旧系统接入。
- 下车道别的时序能力与 one-shot 状态机，对应从单次 Demo 走到可解释、可回滚的生产规则。
- 动态示例和主动服务实车证据，对应 Agent 上线后的评测、观测和反馈回流。

## 证据边界

- Meego/Jira 票据正文、实时状态、经办人与关闭证据没有可读直连，不能把二次审计里的“待完成”写成实时票据事实。
- Bug 日清会的部分会议纪要无权限，今天的未来会议只有日历邀请，不能视为已参会或已有会议结论。
- 搜索与群读存在 partial/truncated，不能声称本次已逐条读完全部 30 天工作消息。
