<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>端云触发器 · 人话终版 v4</title>
<style>
  * { box-sizing: border-box; }
  body { font-family: -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif; line-height: 1.85; color: #1f2937; max-width: 1060px; margin: 0 auto; padding: 32px 24px 80px; background: #fafaf9; }
  h1 { font-size: 1.7rem; border-bottom: 3px solid #dc2626; padding-bottom: 12px; color: #b91c1c; }
  h2 { font-size: 1.32rem; margin-top: 48px; padding: 10px 14px; background: #1f2937; color: #fff; border-radius: 8px; }
  h3 { font-size: 1.08rem; margin-top: 28px; color: #b91c1c; border-left: 4px solid #dc2626; padding-left: 10px; }
  h4 { font-size: 1rem; margin-top: 20px; color: #92400e; }
  p { margin: 10px 0; }
  ul, ol { margin: 8px 0; padding-left: 26px; }
  li { margin: 5px 0; }
  code { background: #fef3c7; padding: 1px 6px; border-radius: 4px; font-size: 0.92em; color: #92400e; }
  .cloud { color: #2563eb; font-weight: 700; }
  .edge { color: #ea580c; font-weight: 700; }
  .pm { color: #dc2626; font-weight: 700; }
  table { width: 100%; border-collapse: collapse; margin: 14px 0; font-size: 0.93rem; background: #fff; }
  th { background: #1f2937; color: #fff; padding: 9px 10px; text-align: left; }
  td { border: 1px solid #e5e7eb; padding: 9px 10px; vertical-align: top; }
  tr:nth-child(even) { background: #f9fafb; }
  .box { padding: 14px 18px; border-radius: 8px; margin: 14px 0; }
  .red { background: #fef2f2; border-left: 4px solid #dc2626; }
  .blue { background: #eff6ff; border-left: 4px solid #2563eb; }
  .orange { background: #fff7ed; border-left: 4px solid #ea580c; }
  .green { background: #f0fdf4; border-left: 4px solid #16a34a; }
  .gray { background: #f3f4f6; border-left: 4px solid #6b7280; }
  .up { background: #f0fdf4; border-left: 4px solid #16a34a; padding: 10px 16px; border-radius: 6px; margin: 10px 0; font-size: 0.95rem; }
  .up::before { content: "⬆ 上游（谁给我数据）："; font-weight: 700; color: #16a34a; }
  .down { background: #fefce8; border-left: 4px solid #ca8a04; padding: 10px 16px; border-radius: 6px; margin: 10px 0; font-size: 0.95rem; }
  .down::before { content: "⬇ 下游（我给谁数据）："; font-weight: 700; color: #ca8a04; }
  .flow { display: flex; flex-wrap: wrap; align-items: center; gap: 6px; margin: 12px 0; font-size: 0.9rem; }
  .n { padding: 8px 12px; border-radius: 8px; border: 1.5px solid; background: #fff; }
  .n.c { border-color: #2563eb; } .n.e { border-color: #ea580c; } .n.p { border-color: #7c3aed; }
  .n.g { border-color: #16a34a; } .n.k { border-color: #6b7280; }
  .ar { color: #6b7280; font-weight: 700; }
  .toc { background: #fff; border: 1px solid #e5e7eb; border-radius: 8px; padding: 16px 22px; margin: 20px 0; }
  .toc a { color: #2563eb; text-decoration: none; } .toc a:hover { text-decoration: underline; }
  .en { color: #6b7280; font-size: 0.88em; }
  .lead { font-size: 1.05rem; background: #fff; border: 1px solid #fecaca; padding: 16px 20px; border-radius: 10px; }
  .quote { border-left: 3px solid #bbb; padding: 8px 16px; margin: 12px 0; color: #555; font-size: 0.94em; background: #fafafa; border-radius: 0 6px 6px 0; }
  .quote b { color: #333; }
  .atom { background: #fff; border: 1px solid #e5e7eb; border-radius: 10px; padding: 18px 22px; margin: 18px 0; }
  .atom-title { font-weight: 700; font-size: 1.05rem; margin-bottom: 10px; padding-bottom: 6px; border-bottom: 2px solid #e5e7eb; }
  .case { background: #faf5ff; border: 1px dashed #a78bfa; border-radius: 8px; padding: 14px 18px; margin: 12px 0; }
  .case::before { content: "实际 case："; font-weight: 700; color: #7c3aed; }
  .debate { background: #fff; border: 1px solid #e5e7eb; border-radius: 10px; padding: 16px 20px; margin: 16px 0; }
  .debate-title { font-weight: 700; font-size: 1rem; color: #7c3aed; margin-bottom: 8px; }
  .timeline { border-left: 3px solid #a78bfa; padding-left: 16px; margin: 10px 0; }
  .timeline p { margin: 4px 0; font-size: 0.93rem; }
</style>
</head>
<body>

<h1>端云触发器（Trigger System，条件满足就自动干活的系统）· 人话终版 v4</h1>
<p class="en">2026-06-23 · 所有英文带中文解释 · 每个知识点带上游+下游+实际case · 6.22评审全部37个讨论点</p>

<div class="lead">
<b>这份文档是干啥的：</b>讲清楚"豆包上车"（Doubao Driver Agent，字节跳动做的车载 AI 助手）里那套<b>触发器系统（trigger system，条件满足就自动执行的机制）</b>——用户说一句话、或者车自己感知到什么，云端（cloud，远程服务器）和车端（edge，车上本地）怎么分工、一步步把它办了。<br/><br/>
<b>怎么读：</b>每个模块都带<span style="background:#f0fdf4;padding:2px 6px;border-radius:3px;font-weight:700;color:#16a34a">⬆ 上游</span>（谁给我数据）和<span style="background:#fefce8;padding:2px 6px;border-radius:3px;font-weight:700;color:#ca8a04">⬇ 下游</span>（我给谁数据）色块。每个概念都有<span style="background:#faf5ff;padding:2px 6px;border-radius:3px;font-weight:700;color:#7c3aed">实际 case</span>用真实数据演示。所有英文术语都带（中文解释）。
</div>

<div class="toc">
<b>目录</b>
<ol>
  <li><a href="#s1">这套系统是干嘛的 + 云端车端怎么分</a></li>
  <li><a href="#s2">一句话进来怎么走（主链路 + 四路分流详解）</a></li>
  <li><a href="#s3">信号库④（Signal Library，信号仓库）：6字段逐个展开</a></li>
  <li><a href="#s4">event（事件）vs condition（条件/状态）：6.22 最核心的设计决策</a></li>
  <li><a href="#s5">㉔ 端侧触发器（Edge Trigger Engine）：9 原子逐个展开 + 7字段详解</a></li>
  <li><a href="#s6">⑮ 云端触发器（Cloud Trigger Engine）：14 原子逐个展开</a></li>
  <li><a href="#s7">VLM（Vision Language Model，端侧视觉语言模型）链路：18 步完整 walkthrough（走查）</a></li>
  <li><a href="#s8">VQA（Visual Question Answering，云端视觉问答）链路：5 步 walkthrough + 和 VLM 的 7 项区别</a></li>
  <li><a href="#s9">Advisor（顾问）/ Director（决策者）/ Context（记事本）/ GoalList（目标看板）：四角色关系</a></li>
  <li><a href="#s10">通用视觉事件通道（Generic Visual Event Channel，兜底长尾）</a></li>
  <li><a href="#s11">任务中心（Task Center）对接：佳杰需要触发器提供什么</a></li>
  <li><a href="#s12">AlwaysOn（常驻观测）调度归属</a></li>
  <li><a href="#s13">6.22 会议全部讨论点还原（37 个议题）</a></li>
  <li><a href="#s14">行动项（Action Items）+ 待定项（Open Items）</a></li>
</ol>
</div>


<!-- ============ §1 ============ -->
<h2 id="s1">一、这套系统是干嘛的 + 云端（cloud）车端（edge）怎么分</h2>

<p><b>触发器（trigger，满足条件就去干活的机器）</b>解决的是——<b>用户没开口、但车该主动做事</b>的那些场景（scenario，具体的使用情境）。</p>

<div class="case">
用户14:00说"十分钟后关座椅加热"→ 这不是让车现在就关，是让车<b>等10分钟</b>（condition，条件）→ 14:10到了（条件满足）→ 自动关（action，执行动作）。<br/>
用户说"看到我妈上车提醒我"→ 条件是"FaceID（人脸识别）识别到妈妈"→ 满足的那一刻 → 自动TTS（Text-to-Speech，文字转语音）播报"妈妈上车了"。<br/>
用户说"副驾睡着了把音乐调小"→ 条件是VLM（Vision Language Model，端侧视觉语言模型）检测到"副驾闭眼低头超过30秒"→ 满足的那一刻 → 自动把音量从60调到20。
</div>

<div class="box red">
<b>一句话分工：</b><br/>
<span class="cloud">云端（cloud，远程服务器）</span> = <b>"登记（register） + 拍板（decide）"</b>——规则（rule，触发条件+执行动作的组合）怎么定、冲突（conflict，两个规则打架）谁优先（priority，优先级）、复杂判断（需要大模型推理的）。<br/>
<span class="edge">车端（edge，车上本地处理器）</span> = <b>"感知（sense） + 执行（execute）"</b>——等不了云端的事（断网、太慢、太频繁），本地办掉。<br/>
<b class="pm">铁律：同一条规则，绝不能云端和车端两边都判（duplicate judging，重复判定）</b>——否则结论打架。比如云端判"温度没超标"、车端判"超标了"，到底听谁的？所以只能一个地方判。
</div>

<table>
<tr><th>环节</th><th><span class="cloud">云端</span>干（登记+拍板）</th><th><span class="edge">车端</span>干（感知+执行）</th><th>实际case</th></tr>
<tr><td><b>注册（register）</b><br/>定规则</td><td>定规则、校验（validate，检查合法性）信号能力（这车有没有这个传感器）、打包成指令</td><td>只接收已校验的规则、本地存下（persist，持久化保存）</td><td>用户说"到公司提醒我拿外卖"→ 云端⑮定规则{condition: 距离公司<500m, action: TTS"快到公司了，别忘了拿外卖"} → 下发给车端㉔存下</td></tr>
<tr><td><b>判定（evaluate）</b><br/>条件成不成立</td><td>复杂条件（需要语义理解的，如"副驾是不是在睡觉"）、跨任务冲突仲裁（arbitration，多个规则打架时判谁赢）</td><td>简单条件（纯数值比较，如温度>25）、倒计时（countdown，定时器）、弱网本地条件</td><td>"副驾在睡觉"需要理解VLM输出的文字→云端判。"温度>45度"只需比个数→端侧判。"10分钟后"只需倒计时→端侧判</td></tr>
<tr><td><b>执行（execute）</b><br/>干活</td><td>需要大模型推理（inference，模型计算）的复杂动作</td><td>简单车控（关窗调温）、TTS（Text-to-Speech，文字转语音）说话、推卡片（push card，在车机屏幕弹一张信息卡）</td><td>"帮我规划去最近加油站的路线"需要调POI（Point of Interest，兴趣点）搜索API→云端。"关座椅加热"只需发CAN指令→端侧直接关</td></tr>
<tr><td><b>结果回传</b><br/>让上面知道</td><td>从 Context（上下文记事本，全局统一事实源）统一读</td><td>写进信号库④ → 上报（report） Context，<span class="pm">不直接捅给 Advisor（顾问模块）</span></td><td>端侧关了座椅加热→写④{key:"seat_heat_fl", value:"off", timestamp:"14:10:01"}→同步⑫云端信号库→上报㉑Context→Director下次推理时能看到"座椅加热已关"</td></tr>
</table>


<!-- ============ §2 ============ -->
<h2 id="s2">二、一句话进来怎么走（主链路 + 四路分流详解）</h2>

<p>用户说一句话，⑨意图分流模块（Intent Router，意图路由器——判断用户想干嘛）做<b>四路分流（4-way routing）</b>——判断这句话属于哪一类，走哪条路：</p>

<table>
<tr><th>分流结果</th><th>走哪条路</th><th>举例</th><th>为什么分开</th><th>实际case</th></tr>
<tr><td><b>①车控指令</b><br/>（Vehicle Control）</td><td>端侧⑤离线fc（function call，函数调用）+白名单（whitelist，允许清单）→ ⑥命中（match，匹配上了）? → ㉘直接执行</td><td>"打开车窗"、"空调调到24度"</td><td>这种不需要大模型（LLM），白名单直接映射（mapping，一一对应）执行，最快——延迟（latency）<200ms</td><td>用户："开车窗"→ASR转文字→⑨判断是车控→⑤查白名单命中"open_window"→⑥确认→㉘发CAN指令给车身控制器→车窗打开。全程不过云端，200ms内完成</td></tr>
<tr><td><b>②复杂需求</b><br/>（Complex Request）</td><td>L5 → ⑲Director（决策者）汇入 → ⑳Director推理（inference，模型计算）</td><td>"帮我照看后排宝宝"</td><td>需要大模型拆解（decompose，把一件复杂的事拆成多个步骤）、创建监工（Advisor实例）、多步骤</td><td>用户："帮我照看后排宝宝，毯子掉了提醒我"→⑨判断是复杂需求→送Director→Director拆解成：1.建动态Advisor（临时监工）2.注册VLM观测任务3.设阈值穿越检测→最终18步链路（见§7）</td></tr>
<tr><td><b>③条件任务</b><br/>（Conditional Task）</td><td>⑩意图解析（Intent Parse，理解用户说的条件和动作分别是什么）→ ⑬配场景（Configure Scenario，在配置平台生成规则）→ ⑮注册（Register）</td><td>"10分钟后关座椅加热"、"到公司提醒我拿外卖"</td><td>要等条件满足再执行，需要注册（register，登记）到触发器</td><td>用户"到公司提醒我拿外卖"→⑩解析出 condition（条件）=GPS距离公司<500m，action（动作）=TTS播报"别忘了外卖"→⑬生成规则DSL（Domain Specific Language，领域专用语言，写规则的格式）→⑮校验+下发→㉔本地监听GPS信号→满足→播报</td></tr>
<tr><td><b>④拒识成功</b><br/>（Rejection）</td><td>写入㉑Context事件日志（event log，记录发生了什么事）</td><td>车外噪音、无意义语音</td><td>不是对车说话，过滤掉（filter out，筛掉不要的）</td><td>高速上风噪触发了ASR，转出"嗖嗖嗖"→⑨判断不是有效指令→拒识→记日志→不做任何事</td></tr>
</table>

<h3>条件任务的完整链路（以"十分钟后关座椅加热"为例）</h3>
<div class="flow">
<span class="n e">用户说话</span><span class="ar">→</span>
<span class="n e">②ASR（Automatic Speech Recognition，自动语音识别）转文字</span><span class="ar">→</span>
<span class="n e">⑨意图分流：条件任务</span><span class="ar">→</span>
<span class="n c">⑩解析：condition=10分钟后，action=关座椅加热</span><span class="ar">→</span>
<span class="n c">⑬配场景：生成规则DSL</span><span class="ar">→</span>
<span class="n c">⑮注册校验+端云分类（classify，分到端侧还是云端）：定时器→发端侧</span><span class="ar">→</span>
<span class="n e">㉔接收+本地倒计时</span><span class="ar">→</span>
<span class="n e">到点→㉘车控执行</span><span class="ar">→</span>
<span class="n c">结果写Context</span>
</div>

<div class="up">用户语音 → ASR（自动语音识别）文字 → ⑨意图判断结果（分流类型+原始query，用户原话）</div>
<div class="down">⑩解析出的 condition（条件）+action（动作）结构化数据 → ⑬配置平台（Configuration Platform，火山后台的规则配置界面）生成规则DSL → ⑮注册 → 最终到达执行端</div>

<div class="case">
<b>14:00:00</b> 用户说"十分钟后关座椅加热"<br/>
<b>14:00:01</b> ASR转文字："十分钟后关座椅加热"<br/>
<b>14:00:02</b> ⑨分流→条件任务。⑩解析→ <code>{condition: {type:"timer", delay:600}, action: {tool:"vehicle_basic_control", params:{seat_heat_fl:"off"}}}</code><br/>
<b>14:00:03</b> ⑬生成规则DSL（规则语言）→ ⑮校验（seat_heat_fl这个信号存在吗？→查④信号库→存在✓）→ 入库 → 15-9分类：定时器→发端侧<br/>
<b>14:00:04</b> L14下发给㉔ → ㉔存本地（persist持久化）→ 启动定时器（目标时间=14:10:00）<br/>
<b>14:10:00</b> 定时器到点 → 24-9白名单检查（座椅加热∈白名单✓）→ ㉘发CAN（Controller Area Network，车辆内部通信总线）指令关座椅加热<br/>
<b>14:10:01</b> 24-8回写④{key:"seat_heat_fl", value:"off"} → L9同步⑫ → 上报㉑Context
</div>

<div class="box orange">
<b>【为什么定时器必须放车上——断网case实际推演】</b><br/>
14:00 用户说"十分钟后关座椅加热"→ 规则下发到车端<br/>
14:03 车进隧道，<b>断网</b>（network disconnected）<br/>
<b>如果定时器在云端：</b>14:10到了→云端想发指令→发不下去（断网）→用户在隧道里继续被烫→14:18出隧道恢复网络→云端补发→已经烫了8分钟<br/>
<b>如果定时器在车端（现在的方案）：</b>14:10到了→㉔本地直接执行→关掉→用户不被烫。出隧道后补传结果到Context<br/>
<b>这就是车端触发器㉔存在的全部理由——四个字：断网能跑。</b>
</div>


<!-- ============ §3 ============ -->
<h2 id="s3">三、信号库④（Signal Library，信号仓库）：6字段逐个展开 + 上下游</h2>

<p>车上有各种来源的信息（车门开没开、车速多少、温度、摄像头看到啥），全汇总到一个地方叫<b>信号库④（Signal Library，信号仓库）</b>。它存的是<b>快照（snapshot，某一时刻所有状态的完整记录）</b>——就像给车的所有状态拍一张照片，每500ms（毫秒，千分之一秒）更新一次。</p>

<div class="up">6类信号源（signal source，产生信号的来源）往④里写数据：<br/>
① <b>CDC车身总线</b>（Controller Domain Controller，车身域控制器，管所有硬件传感器）：档位/车速/温度/安全带/车门/车窗/雨刷/灯光/电量...<br/>
② <b>VLM视觉模型输出</b>（Vision Language Model，端侧视觉语言模型）：如"副驾在低头看手机"、"后排座椅上有蓝色毛毯"<br/>
③ <b>App埋点</b>（app event tracking，记录用户在车机App里点了什么）：用户切歌/调音量/打开导航...<br/>
④ <b>电子围栏</b>（geofence，虚拟地理边界）：进出某区域（如进入公司500米范围）<br/>
⑤ <b>音频</b>（audio signal）：环境音量/切歌事件/语音唤醒...<br/>
⑥ <b>时间</b>（timer signal）：定时器到点/日期变化/节日...</div>
<div class="down">④的数据有三条出路：<br/>
① → <b>⑫云端信号库</b>（Cloud Signal Library，云端信号仓库）：全集同步（full sync，所有数据复制一份）到云端做镜像（mirror，完全一样的副本）<br/>
② → <b>㉑Context</b>（上下文记事本，全局统一事实源）：按策略（policy，规则）上报关键状态，作为Advisor（顾问）/Director（决策者）的推理依据<br/>
③ → <b>㉔端侧触发器引擎</b>（Edge Trigger Engine）：本地读/订阅（subscribe，注册监听某个信号的变化），做端侧判定（evaluate，判断条件是否满足）</div>

<h3>每条信号的6个字段——以"副驾安全带"为例</h3>

<div class="case">
当前这辆车副驾的安全带状态，在④里存着的完整一条记录长这样：<br/>
<code>{id: "SIG-C-017", key: "belt_fr", value: "buckled", source: "CDC", timestamp: "15:30:45.123", ttl: 10}</code><br/>
下面逐个字段解释。
</div>

<table>
<tr><th>分类</th><th>字段</th><th>这个字段是什么（详解）</th><th>实际例子</th><th>谁写入（producer，生产者）</th><th>谁读取（consumer，消费者）</th><th>为什么需要这个字段（不要会怎样）</th></tr>
<tr><td rowspan="2"><b>①标识</b><br/>这条信号是谁</td>
<td><b>id</b><br/>（identifier，标识符，身份证号）</td>
<td>全局唯一（globally unique，全世界不重复）身份证号。前缀带分类：<b>C</b>=车控CDC、<b>V</b>=视觉VLM、<b>T</b>=定时器Timer、<b>G</b>=围栏Geofence、<b>A</b>=音频Audio、<b>E</b>=埋点Event</td>
<td><code>SIG-C-017</code>（第17号CDC车控信号）<br/><code>SIG-V-003</code>（第3号VLM视觉信号）</td>
<td>信号源在第一次注册（register，登记）时系统自动分配，之后不变</td>
<td>触发器引擎（⑮和㉔）用id匹配（match，对上）规则里的信号字段——规则写的是"当SIG-C-017变化时"</td>
<td><b>跨车型复用（cross-vehicle reuse）：</b>D6X和赛力斯底层CAN协议不同（一个0x301表示安全带、另一个0x412表示安全带），但同一条信号id（SIG-C-017）一样。规则只认id，不管底层协议怎么变，规则就能通用</td></tr>
<tr>
<td><b>key</b><br/>（键名，机器读的字段名）</td>
<td>命名规则：部件_位置_属性。<b>这是触发器写规则时用的字段名</b>——规则条件写 <code>if belt_fr == "unbuckled"</code>，用的就是key</td>
<td><code>belt_fr</code><br/>belt（安全带）+ f（front前排）+ r（right右侧=副驾）<br/><br/>其他例子：<code>temp_cabin</code>（舱内温度）、<code>speed_vehicle</code>（车速）、<code>gear_current</code>（当前档位）</td>
<td>信号源按schema（数据格式定义，规定每个字段叫什么、什么类型）写入</td>
<td>触发器写规则时用key做条件匹配——<code>if belt_fr == "unbuckled" AND speed_vehicle > 10</code>（副驾没系安全带且车速大于10）</td>
<td><b>解耦（decoupling，让两边不互相依赖）：</b>规则只认key名字，不关心底层CAN总线协议。换车型时改的是key到CAN信号的映射（adapter适配层），规则本身不动</td></tr>

<tr><td rowspan="2"><b>②内容</b><br/>说了啥、谁说的</td>
<td><b>value</b><br/>（值，信号当前的内容）</td>
<td>信号当前值。3种数据类型：<br/>
① <b>枚举（enum，有限选项）</b>：buckled已系/unbuckled未系、P档/R档/D档<br/>
② <b>数字（number）</b>：车速45、温度32.5、电量78<br/>
③ <b>自由文本（free text）</b>：VLM输出的自然语言描述"后排座椅上有蓝色毛毯"</td>
<td><code>buckled</code>（已系）<br/><code>45</code>（车速45km/h）<br/><code>"后排座椅上有蓝色毛毯，覆盖儿童腿部"</code></td>
<td>信号源（CDC/VLM/App）实时更新（real-time update，数据变了就立刻改）</td>
<td>㉔/⑮做规则判定时读value做比较；㉑Context注入Director推理时读，Director看到"副驾未系安全带"可以决定提醒</td>
<td><span class="pm"><b>坑（pitfall，容易犯的错）：</b>距离/速度值必须带单位字符串——写"200m"不能写裸数字200。因为端侧解析代码要剥单位（parse unit），裸数字200不知道是米还是公里还是英尺。</span>仁杰6/20确认：上汽固定用<b>米（m）/秒（s）/时分（hh:mm）</b>格式。所以value写<code>"200m"</code>、<code>"45km/h"</code>、<code>"14:10"</code></td></tr>
<tr>
<td><b>source</b><br/>（来源，谁产的这条信号）</td>
<td>标记这条信号是从哪个系统来的。固定值枚举：<code>CDC</code> / <code>VLM</code> / <code>APP</code> / <code>GEO</code>（围栏） / <code>AUDIO</code> / <code>TIMER</code></td>
<td><code>CDC</code>（来自车身总线，硬件传感器直接测的）<br/><code>VLM</code>（来自视觉模型推理，有误检可能）</td>
<td>信号源自标（self-label，自己标自己是谁）</td>
<td>三个用途：<br/>
① <b>排查找人</b>：这条信号不对，找谁？source=CDC找硬件团队，source=VLM找丁彬<br/>
② <b>可信度评估</b>（reliability，可靠性）：CDC是硬件实测（hardware measured），准确率>99%；VLM是模型推理，准确率~85%，可能误检（false positive，误报）<br/>
③ <b>断网影响评估</b>：source=CDC的断网不受影响（本地传感器）；source=VLM的端侧也不受影响（本地模型）；但source=GEO的围栏如果依赖云端地图API就断了</td>
<td>没有source字段的话，信号不对了不知道找谁，不知道该信几分</td></tr>

<tr><td rowspan="2"><b>③时效</b><br/>什么时候的、还准吗</td>
<td><b>timestamp</b><br/>（时间戳，信号产生的时刻）</td>
<td>信号<b>产生（generated）</b>的时刻——传感器测到值的那一刻，<b>不是</b>写入信号库的时刻（可能有网络延迟delay）。格式：<code>HH:MM:SS.mmm</code>（时:分:秒.毫秒）</td>
<td><code>15:30:45.123</code>（下午3点30分45秒123毫秒这个瞬间传感器测到的值）</td>
<td>信号源在信号产生时打戳（stamp，盖时间章）。CDC传感器自带硬件时钟精度±1ms；VLM用模型推理开始时间</td>
<td>三个消费场景：<br/>
① ⑮做<b>阈值穿越检测</b>（threshold crossing detection，见§6的15-10）时对比"上次vs这次"——用timestamp判断先后顺序<br/>
② <b>多信号对齐</b>（signal alignment）：规则要求"车速<10 AND 档位==R"<b>同时成立</b>——靠timestamp判断两个信号是不是在同一时间窗口内<br/>
③ 判"<b>持续N秒</b>"（duration check）靠它——"副驾闭眼超过30秒"需要earliest_timestamp到latest_timestamp差值≥30s</td>
<td><b>如果用写入信号库的时刻会怎样？</b>传感器15:30:45测到的值，经过网络传输15:30:46才写入④。如果timestamp标成15:30:46，那"持续5秒"的判断就偏了1秒——积累多了就不准。特别是VLM信号，推理本身就要1-3秒，偏差更大</td></tr>
<tr>
<td><b>ttl</b><br/>（Time-To-Live，存活时间，单位秒）</td>
<td>过了这个时间，这条信号算<b>过期（expired/stale，陈旧的、不新鲜的）</b>。过期后触发器判定时<b>跳过这条信号</b>，不拿它做决策。怎么判断过期？<code>当前时间 - timestamp > ttl</code>→ 过期</td>
<td><code>10</code>（秒）—— 这条安全带信号10秒后过期<br/><br/>不同信号的ttl差很远：<br/><code>speed_vehicle（车速）: ttl=1</code>（变化极快，1秒前的车速可能已经不准了）<br/><code>door_fl（左前门）: ttl=5</code><br/><code>belt_fr（副驾安全带）: ttl=10</code><br/><code>temp_cabin（舱内温度）: ttl=30</code>（温度变化慢，30秒内变化不大）<br/><code>VLM输出: ttl=6</code>（每3秒一帧，6秒保证至少有一帧有效的）</td>
<td>产品定义（PM我来定每个信号的ttl）+信号源按schema写入</td>
<td>㉔/⑮判定前先检查ttl——<code>if (now - signal.timestamp > signal.ttl) { skip; }</code>（如果过期了就跳过）。过期的信号不参与判定（evaluation），防止用过期数据误判（false trigger，误触发）</td>
<td><b>不设ttl会怎样？实际case：</b>14:30:00车速=130km/h写入④（高速行驶）。14:30:10车已停下（实际车速=0）。但因为断网，④里最后一条speed还是130。如果不看ttl，触发器读到speed=130判断"高速行驶中"→ 执行了"高速提醒降速"→ 但车其实停着。ttl=1秒，14:30:01就过期了，不会用来判定</td></tr>
</table>


<!-- ============ §4 ============ -->
<h2 id="s4">四、event（事件，刚刚发生的变化）vs condition（条件/状态，当前持续存在的情况）：6.22 最核心的设计决策</h2>

<p>这是 6.22 整场会<b>最重要的架构纠正</b>，佳杰当场抓出来的。不理解这个区别，后面所有链路都会画错。</p>

<h3>先说问题：6.22 之前我画错了什么</h3>
<p>我原来画的图：端侧VLM结果 → 写信号库④ → 上报㉑Context → 动态Advisor（临时监工）⑰从Context消费（consume，读取并使用）。<b>佳杰在25:48当场纠正：这条路是错的。</b></p>

<div class="quote"><b>佳杰 26:35原话：</b>"我需要一个'用户睡醒了'的事件。我不会去轮询（polling，反复查询）你的Context。我注册了这个事件之后，就等待你的云端触发器给我发这个事件。"</div>

<h3>为什么轮询（polling）Context拿事件（event）是错的——用佳杰的例子一步步推</h3>
<p><b>佳杰的case（29:34原话）："用户倒完车之后，如果车里有小朋友，就把儿童锁打开"</b></p>
<p>这句话拆成三部分：</p>
<table>
<tr><th>部分</th><th>是什么类型</th><th>存哪/怎么传</th><th>为什么是这个类型（详细推理）</th><th>实际数据长什么样</th></tr>
<tr><td><b>"用户倒完车"</b></td><td><b>event（事件）</b>——刚刚发生了一个变化：档位从R（Reverse，倒档）变到P（Park，停车档）</td><td>⑮检测到阈值穿越（threshold crossing，从一个值变到另一个值的那一刻）后组装（assemble，打包），<b>push（主动推送）给Advisor</b></td><td>你不可能让Advisor 24小时轮询（polling，每隔几秒查一次）Context问"倒车了没？倒车了没？倒车了没？"——<b>浪费：</b>99.9%时间都在做无用查询。<b>不实时：</b>如果每5秒轮询一次，档位在两次轮询之间从R变P又变D，这个"倒完车"事件就漏了</td><td>⑮检测到：<code>{field: "gear_current", from: "R", to: "P", timestamp: "14:32:09"}</code> → 组装成event：<code>{name: "倒车结束", detail: "档位从R变P", time: "14:32:09"}</code> → push给⑰</td></tr>
<tr><td><b>"车里有小朋友"</b></td><td><b>condition（条件/状态）</b>——当前持续存在的、不是"刚刚发生"的</td><td>存在㉑Context（全局记事本）里，Advisor需要时去读</td><td>"车里有小朋友"这个状态可能持续了一个小时——小孩上车后就一直在。它不是"刚刚发生"的，是"一直存在"的。Advisor收到"倒车结束"事件后，去Context查一下"车里有没有小孩"→ 有 → 继续</td><td>㉑Context里存着：<code>passengers: [{seat: "rear_left", type: "child", age: 3}]</code></td></tr>
<tr><td><b>"把儿童锁打开"</b></td><td><b>action（执行动作）</b></td><td>Advisor给Director发advice（建议）→ Director拍板（approve，批准）执行</td><td>event满足（✓刚倒完车）+ condition也满足（✓车里有小孩）→ 两个都对上了 → 触发（fire）action</td><td>⑰发advice：<code>{suggestion: "检测到倒车结束且后排有3岁儿童，建议开启儿童锁", priority: "P1"}</code> → Director批准 → 发车控指令 <code>child_lock: "on"</code></td></tr>
</table>

<div class="box red">
<b>正确做法（6.22定稿的铁律）：</b><br/>
<b>condition/状态</b>（"车里有小孩"、"车内温度32度"、"毯子在座椅上"、"电量78%"、"副驾在打电话"）→ 存㉑Context，推理时当背景条件用<br/>
<b>event/事件</b>（"用户刚醒了"、"车刚驶出地库"、"毯子从座椅掉到地上了"、"档位从R变P"、"副驾刚挂了电话"）→ ⑮检测阈值穿越（threshold crossing）后组装，<b>主动push给Advisor</b><br/>
<span class="pm">两条路不混。Advisor不轮询Context拿event。触发器不存condition到Context。凡画成"Context→Advisor当事件用"的都是错的。</span>
</div>

<div class="up">⑫云端信号库（Cloud Signal Library）持续同步的端侧信号快照（④的镜像mirror）</div>
<div class="down"><b>condition路径：</b>④ → ⑫ → 按策略上报㉑Context → Advisor/Director推理时读取<br/><b>event路径：</b>④ → ⑫ → ⑮监听（listen，持续关注）变化 → 阈值穿越（threshold crossing，从A值变到B值的瞬间） → 抖动过滤（debounce，防重复触发） → 组装语义化event → push（主动推送）给⑯⑰Advisor</div>

<h3>边界情况（edge case，特殊情况）：同一个信号既产生event又产生condition</h3>
<div class="case">
<b>"毯子的位置"信号：</b><br/>
VLM每3秒输出一次"后排座椅上有蓝色毛毯"——这是<b>condition（状态）</b>，写④ → 报㉑Context。任何模块想知道"毯子现在在哪"，去Context读就行。<br/><br/>
某次VLM输出变成"后排<b>地板上</b>有蓝色毛毯"——⑮检测到穿越（"座椅上→地板上"），组装<b>event（事件）</b>"覆盖物位置变化"push给Advisor。<br/><br/>
<b>两条路各走各的，不冲突：</b><br/>
- Context一直存着最新状态："毯子在地板上"（condition路径）<br/>
- Advisor同时收到一次性通知："毯子刚从座椅掉到地板了"（event路径）<br/>
- condition回答"现在什么样"，event回答"刚刚发生了什么变化"
</div>


<!-- ============ §5 ============ -->
<h2 id="s5">五、㉔ 端侧触发器（Edge Trigger Engine，车上本地的触发引擎）：9 原子（atom，最小不可分的步骤）逐个展开</h2>

<p>㉔是装在CDC（Central Domain Controller，赛力斯中央域控制器，车上最强的计算芯片）上的本地触发引擎。<b>它存在的理由就一条：断网能跑（offline capable）。</b></p>

<div class="box blue">
<b>架构约束（architectural constraint，设计上的硬限制）：</b>端侧只能当<b>"干活的手"</b>，不能当<b>"拍板的脑"</b>。判断的活儿全归云端⑮。㉔拿到云端打包好的规则，照着match（匹配比对）、execute（执行）就完了，自己不琢磨为什么。<br/><br/>
<b>为什么这么设计？三个好处：</b><br/>
① 端侧代码量小（~5000行 vs 云端~30000行），bug少<br/>
② 跨车型好移植（port，移到另一个平台）——换赛力斯到上汽，端侧逻辑几乎不改<br/>
③ 和云端结论不打架——只有一个脑在做决策
</div>

<div class="box red">
<b>6.22 scope（范围）收窄——仁杰的三问判定标准（13:14）：</b><br/>
<b>"这东西会不会变？""用户需不需要创建它？""需不需要AI？"</b>→ 全是"不"→ 回赛力斯CDC底软自己做。任一个"是"→ 走触发器。<br/><br/>
<b>还回去的6个场景：</b>起步安全（系安全带才能走）/ 上下车安全提醒 / 睡眠检测 / 迎宾（人靠近车就开灯）/ 充电大师 / 哨兵模式（停车监控）<br/>
<b>留下的：</b>用户临时设的条件任务、可运营的动态场景、需要VLM的视觉观测任务——这些"会变"、"需要AI"。
</div>

<div class="up">㉔的全部input（输入）：<br/>① L14从⑮云端触发器下发的<b>场景规则包</b>（scene rule package，包含condition条件、action动作、priority优先级等的完整规则定义）<br/>② ④端侧信号库的<b>实时信号快照</b>（real-time signal snapshot，每500ms毫秒读一次的当前所有状态）</div>
<div class="down">㉔的全部output（输出，5条路）：<br/>① <b>运行指令7字段</b>（见下方详解表）→ ㉕感知器（Perception Scheduler，感知调度器——调VLM看东西的那个模块）<br/>② <b>执行结果</b> → ④信号库回写（write back，把结果存回去） → ⑫同步云端<br/>③ <b>车控指令</b>（vehicle control command）→ ㉘车控执行（CAN总线发指令给硬件）<br/>④ <b>话术文本</b>（speech text，要播报的文字）→ ㉖TTS（Text-to-Speech，文字转语音）播报<br/>⑤ <b>卡片数据</b>（card data）→ ㉙HMI（Human-Machine Interface，人机交互界面，就是车机屏幕）显示</div>

<h3>9 原子逐个展开</h3>

<!-- 24-1 -->
<div class="atom">
<div class="atom-title">24-1 规则接收（Rule Reception，从云端收规则）</div>
<p><b>【是什么】</b>把云端⑮打包好的规则（rule，条件+动作的组合）接进车端。一条规则包含：scene_id（场景编号）、condition（什么条件，如"温度>45"）、action（干什么，如"开空调"）、priority（优先级P0-P4）、valid_phases（生效阶段：driving行驶中/parked停车中/all全时段）、expire_at（过期时间）。</p>
<p><b>【为什么需要】</b>规则在云端定好了（通过配置平台⑬或Director对话产生），不下发到车端，车端就没法本地执行。断网时车端两手空空——手里没规则，有信号也白搭。</p>
<p><b>【怎么干——三种下发（delivery）方式】</b></p>
<ul>
<li><b>全量下发（full sync，全部发一遍）</b>：新车激活/大版本更新时，把所有active（生效的）规则打包发过来。通常50-200条规则，约100KB数据</li>
<li><b>增量下发（incremental sync，只发变化的）</b>：只发新增（new）/修改（updated）/删除（deleted）的规则。日常运营改一条规则不需要重发全部</li>
<li><b>灰度下发（grayscale delivery，先发一小部分车试试）</b>：新规则先给10%的车（gray_ratio=0.1），跑3天没出问题再全量推。防新规则有bug影响所有车——比如规则写错了"温度>25就开空调"，夏天所有车同时开空调，后台服务器被打爆</li>
</ul>
<p><b>【支持回滚（rollback，撤回操作）】</b>云端发现有问题的规则 → 发一条revoke指令（撤销指令）→ ㉔收到后从本地删除该规则、停止执行。</p>
<div class="case">
<b>实际case——运营配了一条新场景：</b><br/>
产品在火山后台⑬配了场景"充电桩靠近自动提醒"：<br/>
<code>{scene_id: "SCN-GEO-012", condition: {signal: "charger_distance", op: "<", value: "50m"}, action: {type: "tts", content: "附近有充电桩，要去充电吗？"}, priority: "P3", valid_phases: ["driving"], expire_at: null}</code><br/>
→ ⑮校验通过 → 先灰度下发10%的车 → 3天后无问题 → 全量下发所有车 → ㉔收到存本地
</div>
<div class="up">L14从⑮云端触发器下发的规则包。格式：JSON（JavaScript Object Notation，一种通用数据格式），包含 scene_id（场景编号） / conditions[]（条件数组，可以有多个条件用AND连接） / actions[]（动作数组，可以同时执行多个动作） / priority（优先级） / valid_phases（生效阶段） / expire_at（过期时间） / gray_ratio（灰度比例）</div>
<div class="down">规则存入本地持久化存储（24-2 Local Persistence），供后续扫描匹配（24-3 Scan & Match）使用</div>
</div>

<!-- 24-2 -->
<div class="atom">
<div class="atom-title">24-2 本地持久化（Local Persistence，断电不丢的存储）</div>
<p><b>【是什么】</b>把规则存到车机硬盘（flash闪存/eMMC嵌入式存储，都是断电不丢数据的存储介质），重启不丢。</p>
<p><b>【为什么需要】</b>车每次熄火上电都重启（restart），内存（RAM，Random Access Memory，断电就清空的快速存储）清空。不持久化（persist，永久保存）的话，用户昨天设的"每天早8点提醒我吃药"第二天就断了。</p>
<p><b>【怎么干】</b>用轻量级KV存储（Key-Value Storage，键值对存储——像一个字典，key=规则名，value=规则内容），如LevelDB或SQLite。重启后从本地恢复（recover），同时向云端请求一次全量同步做校验。<b>云端下发的和本地缓存（cache，缓存）冲突时，以云端为准覆盖本地</b>——因为用户可能在手机App上改了规则，云端是最新的。</p>
<div class="case">
<b>实际case——车辆重启后的恢复过程：</b><br/>
<b>Day1 22:00</b> 用户设了3条规则（"8点提醒吃药"、"到公司提醒拿外卖"、"温度>30开空调"）→ ㉔存到本地SQLite<br/>
<b>Day1 23:00</b> 熄火（ignition off）→ 内存清空，但SQLite在硬盘上不受影响<br/>
<b>Day2 07:30</b> 上电（ignition on）→ ㉔启动 → 从SQLite恢复3条规则 → 同时向⑮请求全量同步 → ⑮返回3条规则（和本地一致） → 校验通过✓<br/>
<b>特殊情况：</b>如果用户Day1 23:30在手机App上删了"到公司提醒拿外卖"→ 云端⑮少了这条 → Day2同步时发现本地多了一条 → 以云端为准 → 删掉本地这条
</div>
<div class="up">24-1接收的规则包</div>
<div class="down">本地存储，供24-3扫描读取。重启后自动恢复，供所有后续原子使用</div>
</div>

<!-- 24-3 -->
<div class="atom">
<div class="atom-title">24-3 扫描+简单匹配（Scan & Simple Match，周期性检查条件是否满足）</div>
<p><b>【是什么】</b>每500毫秒（ms，千分之一秒——即每半秒）读一次④信号库快照（snapshot，当前所有信号的完整状态），逐条和本地规则做匹配（match，对比条件是否成立）。</p>
<p><b>【为什么需要】</b>规则下发下来了、信号也一直在变，得有人周期性（periodically，每隔一段固定时间）去比对"现在的情况对不对得上规则"。不扫的话规则白下发，永远不触发。就像闹钟设了但没人看时间，永远不会响。</p>
<p><b>【怎么干——只做最简单的数值比较】</b></p>
<ul>
<li>支持的运算符（operator，比较符号）：<code>=</code>等于 / <code>></code>大于 / <code><</code>小于 / <code>>=</code>大于等于 / <code>range(a,b)</code>在a到b之间 / <code>changed_to</code>值刚变成</li>
<li><b>不做语义判断（semantic reasoning，理解含义的判断）</b>——"副驾是不是在睡觉"这种，VLM输出的是文字"副驾闭眼低头15秒"，判断"这算不算在睡觉"需要语义理解 → 交给云端⑮-5模型推理（model inference）</li>
<li>运行阶段（phase，如driving/parked）也是云端在规则包里写死的（<code>valid_phases: ["driving"]</code>），㉔只查"我现在是不是行驶中"（读④的gear_current信号，不等于P就是行驶中）</li>
</ul>
<div class="case">
<b>实际case——一次扫描周期的完整过程（500ms内完成）：</b><br/>
<b>14:30:00.000</b> 开始扫描。本地有3条active规则：<br/>
规则A："temp_cabin > 30 → 开空调" valid_phases=["all"]<br/>
规则B："charger_distance < 50m → TTS提醒" valid_phases=["driving"]<br/>
规则C："timer到14:40:00 → 关座椅加热" valid_phases=["all"]<br/><br/>
读④信号库快照：temp_cabin=28.5, charger_distance="1200m", gear_current="D", timer目标=14:40:00, 当前时间=14:30:00<br/><br/>
<b>匹配过程：</b><br/>
规则A：temp_cabin=28.5 > 30? → <b>否，不触发</b><br/>
规则B：先查phase——gear_current="D"≠"P"→ 是行驶中✓。charger_distance="1200m"→剥单位→1200 < 50? → <b>否，不触发</b><br/>
规则C：14:30:00 >= 14:40:00? → <b>否，未到点</b><br/><br/>
本次扫描结果：0条命中。耗时约2ms。下次扫描14:30:00.500。
</div>
<div class="up">④端侧信号库的实时快照（每500ms读一次）+ 24-2存储的规则列表（rules list）</div>
<div class="down">匹配结果（哪些规则条件达成，matched rules）→ 24-4定时器 / 24-5防抖冷却</div>
</div>

<!-- 24-4 -->
<div class="atom">
<div class="atom-title">24-4 本地定时器（Local Timer，车上的闹钟）</div>
<p><b>【是什么】</b>㉔的<b>头号价值——断网也能倒计时的闹钟</b>。支持4种时间模式：</p>
<table>
<tr><th>模式</th><th>说明</th><th>实际case</th></tr>
<tr><td><b>倒计时（countdown）</b></td><td>从现在开始倒数N秒/分钟</td><td>"10分钟后关座椅加热"→ delay=600秒</td></tr>
<tr><td><b>定时（scheduled）</b></td><td>指定一个时间点</td><td>"下午3点提醒我接孩子"→ target=15:00:00</td></tr>
<tr><td><b>重复（recurring）</b></td><td>每天/每周重复</td><td>"每天早8点提醒我吃药"→ cron="0 8 * * *"（cron表达式，定时任务格式）</td></tr>
<tr><td><b>节日/日期（calendar）</b></td><td>特定日期</td><td>"6月22号评审那天提前1小时提醒我"</td></tr>
</table>
<p><b>【配置限制】</b>最小间隔1分钟（防1秒1次的骚扰）、最多同时30个倒计时（防内存溢出overflow）、倒计时可以显示在车机屏（HMI界面显示剩余时间）。到点后直接触发action（走24-9执行），不需要再绕云端确认。</p>
<div class="case">
<b>边界case——车重启后定时器怎么办？</b><br/>
<b>14:00</b> 用户设"30分钟后关空调"→ ㉔存下 <code>{target_time: "14:30:00"}</code>（存的是<b>目标时间点</b>，不是"还剩1800秒"——很关键）<br/>
<b>14:15</b> 车意外重启（软件crash崩溃了）→ 内存清空<br/>
<b>14:16</b> ㉔恢复 → 从SQLite读到 <code>{target_time: "14:30:00"}</code> → 用"14:30:00 - 14:16:00 = 还剩14分钟"重算剩余时间 → 继续倒计时<br/><br/>
<b>如果存的是"还剩1800秒"会怎样？</b>14:15重启，从SQLite读到"还剩1800秒"→ 从现在开始倒1800秒 → 14:45才触发 → 晚了15分钟。所以<b>必须存目标时间点，不能存剩余时间</b>。<br/><br/>
<b>如果重启时已过目标时间呢？</b><br/>
<b>14:00</b> 设"10分钟后关空调" → target=14:10:00<br/>
<b>14:08</b> 车crash重启<br/>
<b>14:12</b> ㉔恢复 → 读到target=14:10:00 → 14:12 > 14:10 → <b>已过期 → 立即触发</b>（虽然晚了2分钟，但总比不触发好）
</div>
<div class="up">24-1接收的规则中带timer类型（type: "timer"）的条件定义（含目标时间点target_time）</div>
<div class="down">定时器到点 → 24-9动作执行（TTS播报/车控/推卡片）。定时器状态变化 → 24-8回写④信号库</div>
</div>

<!-- 24-5 -->
<div class="atom">
<div class="atom-title">24-5 防抖冷却（Debounce + Cooldown，防止重复触发的两道保险）</div>
<p><b>【是什么】</b>两个互补机制：</p>
<p><b>防抖（debounce，信号抖动过滤）</b>= 信号在阈值（threshold，临界值）附近跳来跳去时不反复触发。要求"连续N秒（confirm window，确认窗口）都满足"才算真命中。</p>
<p><b>冷却（cooldown，触发后的冷静期）</b>= 触发后N秒内不再触发。防"持续满足 → 持续骚扰"。</p>

<div class="case">
<b>实际case①——防抖（debounce）：温度在阈值附近飘</b><br/>
规则："temp_cabin > 30 → 开空调"，防抖窗口=5秒（连续5秒都>30才算真的）<br/><br/>
<div class="timeline">
<p>14:30:00 temp=29.8 → 不满足</p>
<p>14:30:00.5 temp=30.1 → 满足！开始计时（0/5秒）</p>
<p>14:30:01 temp=30.2 → 满足（1/5秒）</p>
<p>14:30:01.5 temp=29.9 → <b>不满足！计时重置！</b>——这就是"抖动"（jitter），温度在30附近飘</p>
<p>14:30:02 temp=30.3 → 满足！重新计时（0/5秒）</p>
<p>14:30:02.5~14:30:07 → 连续5秒都>30 → <b>确认！触发开空调</b></p>
</div>
<b>如果没有防抖：</b>30.1→触发开、29.9→触发关、30.2→触发开——空调一分钟开关十几次，继电器（relay，控制开关的电子元件）都给搞坏了。
</div>

<div class="case">
<b>实际case②——冷却（cooldown）：防持续骚扰</b><br/>
规则："副驾未系安全带且车速>10 → TTS提醒系安全带"，冷却=120秒（触发一次后2分钟内不再触发），每日上限（daily cap）=5次<br/><br/>
<div class="timeline">
<p>14:30 条件满足 → 第1次提醒："请系好安全带哦~"</p>
<p>14:30:01~14:32:00 条件一直满足，但在冷却期 → <b>不触发</b></p>
<p>14:32:01 冷却结束，条件还满足 → 第2次提醒："安全带还没系呢~"</p>
<p>...（每2分钟提醒一次）</p>
<p>14:40 第5次提醒 → 达到每日上限 → <b>今天不再提醒</b></p>
</div>
<b>如果没有冷却：</b>每500ms扫一次，条件一直满足，每半秒提醒一次→用户被烦死→直接关掉整个助手。
</div>

<p><b>【6.22后的重要变更】</b><b>阈值穿越检测（threshold crossing detection，从false变true那一刻才算事件）</b>这个逻辑<b>上收到了云端⑮-10</b>（见§6），端侧㉔只做防抖+冷却，不做穿越判定。原因：端云各判各的标准会打架，放云端一个地方统一判。</p>
<div class="up">24-3扫描匹配结果中"条件刚满足"的规则列表</div>
<div class="down">过了防抖+冷却的规则 → 24-6（VLM视觉类）或24-9（非VLM类，直接执行）</div>
</div>

<!-- 24-6 -->
<div class="atom">
<div class="atom-title">24-6 VLM调度+资源仲裁（VLM Scheduling + Resource Arbitration，调视觉模型看东西 + 摄像头抢占排队）</div>
<p><b>【是什么】</b>把云端下发的视觉观测任务翻译成㉕感知器（Perception Scheduler，感知调度器）能理解的<b>运行指令——7字段（7-field task descriptor，7个字段的任务描述）</b>，发给㉕去调VLM（Vision Language Model，端侧视觉语言模型）执行。同时管摄像头资源冲突（resource conflict，多个任务抢同一个摄像头）。</p>
<p><b>【为什么需要】</b>摄像头在车端、VLM模型在车端AIBOX（AI计算盒子，一个独立的计算模块）上，调度指令必须本地下发。而且同一个摄像头同一时间只能给一个任务用（硬件限制），多个任务要排队（queue，按顺序等待）。</p>

<h4>7字段详解——VLM运行指令的完整结构（Task Descriptor）</h4>
<table>
<tr><th>#</th><th>字段</th><th>这个字段是什么（详细解释）</th><th>实际例子</th><th>谁设的（producer）</th><th>谁读的（consumer）</th><th>为什么需要（不要会怎样）</th></tr>
<tr><td>1</td><td><b>task_id</b><br/>（任务编号，全局唯一标识）</td><td>这次观测任务的唯一身份证。格式：VT-日期-序号。㉕用它标记"这个结果属于哪个任务"。㉔用它跟踪"这个任务完了没"。</td><td><code>VT-20260622-001</code><br/>（2026年6月22日第001号视觉任务）</td><td>⑮云端触发器在注册（register）时自动生成</td><td>㉕感知器：输出结果时带上task_id，否则不知道这个结果该给谁。㉔：跟踪任务生命周期（created创建→running运行中→completed完成→expired过期）</td><td><b>case：</b>如果没有task_id，㉕同时跑"看毯子"和"看宝宝表情"两个任务，输出"后排有蓝色毛毯"——这个结果是"看毯子"任务的还是"看宝宝"任务的？分不清→上游拿到错误的数据</td></tr>
<tr><td>2</td><td><b>task_goal</b><br/>（任务目标/观测SP，System Prompt系统提示词——告诉模型"你要观测什么"）</td><td>给VLM模型看的System Prompt（系统提示词，给AI模型的指令），告诉模型"你这次要观测什么、怎么描述"。<br/><br/><b>怎么来的：</b>用户说"帮我看毯子"→ ⑰动态Advisor（临时监工）改写成结构化的观测SP → ⑮的15-5编译 → 下发</td><td><code>"持续观测后排区域，上报覆盖物（毯子/被子/毛巾等）的位置（座椅上/地板上/人身上）。只描述客观状态，不做判断。"</code></td><td>⑰动态Advisor拆解用户query（原话）后改写的注意力SP（attention system prompt）→ ⑮编译成结构化格式 → 下发到㉔</td><td>㉕拿到后<b>增量追加（incremental append，加到后面而不是覆盖原来的）</b>到VLM的prompt列表里。<span class="pm">不是覆盖！原有AlwaysOn（常驻观测，一直在跑的基础观测任务）的SP不受影响</span></td><td><b>case：</b>没有task_goal，VLM只做基础AlwaysOn观测（看舱内人数、表情、动作这些固定项）。用户说"帮我看毯子"，VLM压根不知道要看毯子，输出里永远不会提到毯子→任务永远完不成</td></tr>
<tr><td>3</td><td><b>priority</b><br/>（优先级，P0最高P4最低）</td><td>P0-P4，数字越小越紧急。决定摄像头资源怎么分配——高优先级任务可以<b>抢占（preempt）</b>低优先级任务的摄像头</td><td><code>P2</code>（儿童照看）<br/><br/>完整优先级表：<br/>P0=VQA（云端视觉问答）/下车安全检测<br/>P1=紧急安全（如检测到碰撞风险）<br/>P2=用户主动发起的监控（"看毯子""看宝宝"）<br/>P3=AlwaysOn常驻观测<br/>P4=低优后台（如统计舱内人数变化）</td><td>⑮根据场景类型（scenario type）设定</td><td>㉕做摄像头资源仲裁时读——<b>同一个mutex_group（互斥组）里，高优抢占低优</b></td><td><b>case：</b>P2的"看毯子"任务到了，但舱内摄像头正在跑P3的AlwaysOn → P2>P3 → AlwaysOn暂停 → "看毯子"用摄像头 → 完事后AlwaysOn恢复。如果没有priority，两个任务抢摄像头→crash或数据乱</td></tr>
<tr><td>4</td><td><b>timeout</b><br/>（超时时间，单位秒——超过这个时间强制结束）</td><td>单次VLM推理（inference，模型计算一次）超过这个时间算超时（timeout）。超时后强制终止、释放摄像头。</td><td><code>30</code>秒（简单观测）<br/><code>60</code>秒（复杂多目标观测）</td><td>⑮根据任务复杂度设定</td><td>㉕计时（start a timer），超时后强制终止本次推理、释放摄像头资源、标记状态为timeout</td><td><b>case：</b>VLM模型出bug卡死了（hang，无响应），一直在推理不出结果。如果没有timeout→摄像头被这个死任务永远占着→其他任务排队排到天荒地老→VQA也用不了→用户问"窗外什么天气"也没法看</td></tr>
<tr><td>5</td><td><b>mutex_group</b><br/>（互斥组，Mutual Exclusion Group——同组内同一时间只能跑一个任务）</td><td>标记这个任务用哪组摄像头。车上有多组摄像头，同组内互斥（mutual exclusion，同一时间只允许一个），不同组互不影响</td><td><code>VLM_CABIN</code>（舱内摄像头互斥组）<br/><code>VLM_FRONT</code>（前摄/车外摄像头互斥组）<br/><br/>赛力斯D6X有：1个舱内红外摄像头 + 1个前摄（前方道路摄像头）</td><td>⑮根据任务需要的摄像头类型设定</td><td>㉕做仲裁：同一个mutex_group里同时只跑一个任务，按priority排队。不同mutex_group可以同时跑——"看毯子"用舱内摄像头 和 "看路况"用前摄 同时进行，不冲突</td><td><b>case：</b>"看毯子"（舱内摄像头）和"看路况"（前摄）不在同一组→可以同时跑。"看毯子"和"看宝宝表情"都是舱内摄像头→同组→排队。不标mutex_group的话㉕不知道哪些任务抢同一个资源→可能把两个舱内任务同时派给一个摄像头→数据冲突</td></tr>
<tr><td>6</td><td><b>trigger_source</b><br/>（触发来源，这个任务是谁发起的）</td><td>标记谁创建了这个任务。㉔在回写结果时带上来源，方便⑮把结果路由（route，送到正确的地方）给正确的订阅者（subscriber，订阅了这个事件的人）</td><td><code>dynamic_advisor_17</code>（⑰动态Advisor发起的）<br/><code>config_platform_SCN-VIS-005</code>（⑬配置平台配的静态场景）<br/><code>director_goal_G003</code>（Director通过GoalList发起的）</td><td>⑮在下发时根据注册来源标记</td><td>⑮收到㉔回传的VLM结果后，查trigger_source知道推给谁：来源是dynamic_advisor_17 → push给⑰。来源是config_platform → 走条件触发逻辑（15-4判定）</td><td><b>case：</b>㉕返回"后排地板上有蓝色毛毯"→㉔回写④→⑮收到→查trigger_source=dynamic_advisor_17→知道这是⑰的任务→组装event push给⑰。如果没有trigger_source→⑮不知道这结果该给谁→要么广播所有人（浪费）要么弄丢（任务失败）</td></tr>
<tr><td>7</td><td><b>expire_at</b><br/>（过期时间，Expiration Time——到了这个时间任务自动销毁）</td><td>这个任务到什么时候自动失效。到期后㉔清理任务（cleanup）、㉕停止观测、释放摄像头资源</td><td><code>2026-06-22T18:00:00</code>（今天下午6点过期）<br/><code>null</code>（无过期时间，直到用户主动取消——慎用！）</td><td>⑮根据任务生命周期设定。如用户说"看1小时"→ expire_at=当前时间+1h。如果没指定时长→默认4小时（防忘记取消）</td><td>㉔周期性检查：<code>if (now > task.expire_at) → 停止该任务、释放资源</code>。㉕收到停止指令后从VLM的prompt列表里删掉对应SP</td><td><b>case：</b>用户14:00说"帮我看毯子"→ expire_at=18:00。14:30用户说"不用看了"→ 云端发cancel（取消）指令 → ㉔提前清理。<b>如果用户忘了取消且没有expire_at</b>→ 任务永远跑→摄像头永远被占→第二天还在看毯子→浪费算力+占资源</td></tr>
</table>

<div class="up">⑮云端触发器通过L14下发的场景定义（含动态SP注意力提示词）+ ④信号库的实时信号（辅助判定是否需要触发VLM任务）</div>
<div class="down">7字段运行指令 → ㉕感知器（Perception Scheduler，感知调度器）。㉕拿到指令后调度端侧摄像头+VLM模型执行观测</div>

<p><b>【仲裁只管摄像头资源，不管业务冲突】</b>两个任务同时要用舱内摄像头 → ㉔按priority（优先级）决定谁先用。<b>但</b>"降温"和"节能"这种业务逻辑冲突（一个要开空调耗电，一个要省电关空调）→ 归云端⑮-8做业务仲裁（business arbitration），端侧不管。端侧只管"硬件资源谁先用"，不管"做这件事对不对"。</p>
</div>

<!-- 24-7 -->
<div class="atom">
<div class="atom-title">24-7 VLM五态（5 States）+ 结果去重（Deduplication，去掉重复的）</div>
<p><b>【是什么】</b>VLM模型返回结果后，㉔把结果归一（normalize，标准化）为5种标准状态（standard states）：</p>
<table>
<tr><th>状态（state）</th><th>含义</th><th>㉔怎么处理</th><th>实际case</th></tr>
<tr><td><b>success</b>（成功）</td><td>得到了有效（valid）观测结果</td><td>写入④信号库 → L9同步⑫云端信号库</td><td>VLM输出"后排座椅上有蓝色毛毯，覆盖儿童腿部"→ success → 写④ <code>{key: "rear_cover_position", value: "座椅上", source: "VLM"}</code></td></tr>
<tr><td><b>empty</b>（空结果）</td><td>观测了但没发现目标——不是出错，是真的没有</td><td>也写入④（value="未检测到目标"）→ 同步⑫。<b>empty是有效信息</b>——"没看到毯子"本身就是一个状态</td><td>"看毯子"任务，但后排根本没放毯子 → VLM输出"后排座椅未检测到覆盖物"→ empty → 写④ <code>{value: "未检测到目标"}</code></td></tr>
<tr><td><b>failed</b>（出错）</td><td>摄像头异常（camera error）/ 模型崩溃（model crash）/ 图像质量太差（如太暗）</td><td>上报错误日志（error log），<b>不自动重试</b>（no auto-retry）——防止同一个bug反复重试浪费资源。下个扫描周期会自然触发新一轮</td><td>舱内太暗（夜间没开灯），VLM看不清→failed。或者AIBOX内存不足（out of memory）模型crash→failed</td></tr>
<tr><td><b>timeout</b>（超时）</td><td>规定时间内没出结果——模型可能卡住了</td><td>释放摄像头资源，下个扫描周期可重新触发</td><td>timeout=30秒，VLM推理了35秒还没出结果→强制终止→释放摄像头给其他任务用</td></tr>
<tr><td><b>interrupted</b>（被打断）</td><td>被更高优先级任务抢占（preempt）了摄像头</td><td>当前任务暂停（pause），高优完成后自动恢复（resume，继续）</td><td>P3的AlwaysOn正在跑，P0的VQA来了要抓帧→AlwaysOn interrupted→VQA抓完帧（约100ms）→AlwaysOn恢复</td></tr>
</table>

<div class="box red">
<b>安全红线（safety boundary）：timeout绝对不能写成"无异常"（no_issue）。</b><br/>
<b>实际case：</b>"看宝宝哭没哭"任务，VLM推理超时了。timeout≠"宝宝没哭"——可能宝宝真的在哭但模型卡了出不了结果。如果timeout标成"无异常"→ Advisor以为宝宝没事→不提醒用户→安全事故。<br/>
<b>正确做法：</b>timeout标成<code>unknown</code>（未知）→ Advisor知道这次结果作废、不拿来做决策→可以选择立刻触发VQA做二次确认。
</div>

<p><b>结果去重（deduplication，去掉重复的）：</b>VLM每3秒出一次结果。如果连续10次都是"毯子在座椅上"，不需要10次都上报——只有<b>值变了（value changed）</b>或<b>首次上报（first report）</b>时才写④。重复的跳过，省带宽（bandwidth，数据传输能力）和存储空间。</p>
<div class="case">
<b>实际case——去重过程：</b><br/>
14:32:00 VLM输出"毯子在座椅上"→ 首次 → 写④ ✓<br/>
14:32:03 VLM输出"毯子在座椅上"→ 和上次一样 → 跳过 ✗<br/>
14:32:06 VLM输出"毯子在座椅上"→ 和上次一样 → 跳过 ✗<br/>
14:32:09 VLM输出"毯子在<b>地板上</b>"→ <b>值变了！</b>→ 写④ ✓ → ⑮检测到穿越（crossing）→ 组装event
</div>
<div class="up">㉕感知器（Perception Scheduler）返回的VLM推理结果（result text + status code状态码）</div>
<div class="down">归一后的标准状态+结果 → 24-8回写信号库④</div>
</div>

<!-- 24-8 -->
<div class="atom">
<div class="atom-title">24-8 结果回写（Result Write-back，把结果存回信号库）</div>
<p><b>【是什么】</b>把VLM观测结果、定时器触发结果、或车控执行结果写回④信号库，再通过L9同步（sync，同步）到⑫云端信号库，最终按策略（policy）上报到㉑Context（全局记事本）。</p>
<p><b>【为什么需要】</b>端侧干完活儿，上面的模块（云端Advisor顾问/Director决策者）得知道结果。不回写的话，端侧关了座椅加热，云端还以为加热开着呢，下次用户问"座椅加热开着吗"就答错了。</p>
<p><b>【铁律②：结果回写的唯一路径】</b>结果<b>绝不直接捅给Advisor</b>。统一写④ → ⑫ → ㉑Context。所有模块都从Context这一个"记事本"读。</p>
<div class="case">
<b>为啥不能直接通知Advisor？</b><br/>
假设㉔关了座椅加热，直接通知⑰动态Advisor"我关了"。但⑯静态Advisor（舒适）不知道→它还以为开着→出了"建议调低加热温度"的advice→Director执行→发现加热已经关了→报错。<br/><b>统一进记事本（Context）：</b>㉔写④{seat_heat: off}→同步⑫→上报㉑Context→<b>所有人</b>从Context读→永远一致。
</div>
<div class="up">24-7归一后的VLM结果，或24-9执行动作的结果，或24-4定时器触发的结果</div>
<div class="down">写入④端侧信号库 → L9同步⑫云端信号库 → ⑮监听⑫做阈值穿越检测（threshold crossing detection）；同时按策略上报㉑Context（全局记事本）</div>
</div>

<!-- 24-9 -->
<div class="atom">
<div class="atom-title">24-9 动作执行·白名单（Action Execution + Whitelist，执行动作 + 允许清单）</div>
<p><b>【是什么】</b>白名单（whitelist，允许清单）= 一张"允许端侧直接干、不需要绕云端Director确认"的清单。清单内的低风险动作㉔直接执行，清单外的高风险动作必须送回Director（决策者）确认。</p>
<p><b>【为什么分白名单内外——风险等级不同】</b></p>
<table>
<tr><th>白名单内（低风险，端侧直接执行）</th><th>白名单外（高风险，必须送回Director确认）</th><th>为什么这么分</th></tr>
<tr><td>㉖TTS播报（Text-to-Speech，说话）</td><td>车门解锁（door unlock）</td><td>说错话最多尴尬，解错锁可能被盗</td></tr>
<tr><td>㉙推卡片（push card，车机屏弹信息卡）</td><td>车窗升降（行驶中的window control）</td><td>卡片用户看看就行，行驶中开车窗可能进异物</td></tr>
<tr><td>座椅加热/通风开关（seat heating/ventilation）</td><td>发动机启停（engine start/stop）</td><td>座椅温度不舒服就手动调，发动机误启动有安全风险</td></tr>
<tr><td>空调温度调节（AC temperature）</td><td>任何涉及车辆移动的动作（vehicle movement）</td><td>温度调错了不危险，车动了可能撞东西</td></tr>
<tr><td>氛围灯开关（ambient light）</td><td>涉及支付/订单的动作（payment related）</td><td>灯开错了关掉就行，支付误操作涉及财产</td></tr>
</table>

<div class="case">
<b>实际case——弱网（weak network）场景下白名单的价值：</b><br/>
<b>14:05</b> 用户说"到目的地500米提醒我"→ 云端注册规则{condition: dest_distance<500m, action: TTS"快到目的地了"} → 下发给㉔<br/>
<b>14:20</b> 车进隧道，<b>断网</b><br/>
<b>14:25</b> GPS显示距目的地480m（信号通过CDC本地传感器获得，不需要网络）→ ㉔检测到 dest_distance=480m < 500m → 命中！<br/>
<b>14:25:01</b> 24-9检查白名单：TTS播报∈白名单✓ → 直接调㉖TTS播报"快到目的地啦~"<br/>
<b>14:25:02</b> 24-8回写④{dest_alert: "triggered"}。因为断网，暂时存本地。<br/>
<b>14:28</b> 出隧道恢复网络 → 补传（backfill）执行记录到⑫ → 上报㉑Context
</div>

<div class="up">24-3~24-5判定通过的规则 + 规则里定义的action（执行动作）</div>
<div class="down">白名单内 → ㉘车控执行（CAN指令） / ㉖TTS播报 / ㉙HMI（车机屏幕）显示。白名单外 → 送回⑲Director确认后执行。所有执行结果 → 24-8回写④信号库</div>
</div>


<!-- ============ §6 ============ -->
<h2 id="s6">六、⑮ 云端触发器（Cloud Trigger Engine，云端触发引擎）：14 原子（atom，最小步骤）逐个展开</h2>

<p>⑮是整套系统的<b>大脑（brain）</b>。所有规则都得到它这儿登记（register，注册）、校验（validate，检查）、打包（package），再决定下发（dispatch，分发）云端还是车端。</p>

<div class="up">⑮的全部input（输入），3个方向的场景注册：<br/>
① <b>⑬规则配置平台</b>（Configuration Platform，火山引擎后台的规则配置界面）—— 产品预设（preset，提前设好的）/运营配置（operation config，运营人员可随时调的）的静态场景（static scenario），如"舱内温度超45度→开空调"<br/>
② <b>⑰动态Advisor</b>（Dynamic Advisor，临时监工）—— 用户query（原话）产生的动态视觉场景，如"帮我看毯子"拆解出的观测任务<br/>
③ <b>⑳Director</b>（决策者）—— 对话中临时产生的条件任务，如"对了帮我10分钟后关空调"</div>
<div class="down">⑮的全部output（输出，4个方向）：<br/>
① L17 → <b>⑯静态Advisor</b>（Static Advisor，4个长期运行的顾问）：push event（主动推送事件）<br/>
② L18 → <b>⑰动态Advisor</b>（Dynamic Advisor，临时监工）：push event<br/>
③ L21 → <b>⑲Director</b>（决策者）：条件触发通知（condition triggered notification）<br/>
④ L14 → <b>㉔端侧触发器</b>（Edge Trigger Engine）：场景下发（scene dispatch）+动态SP（动态注意力提示词）</div>

<h3>第①段：登记校验（Register & Validate，注册和校验）（15-1 ~ 15-5）</h3>

<div class="atom">
<div class="atom-title">15-1 能力校验（Capability Check，检查这车能不能干这事——别名 iCanDo）</div>
<p><b>【是什么】</b>收到一个场景注册请求（registration request）后，先查"这车能不能干这事"。查三件：</p>
<ul>
<li><b>① DSL格式校验</b>（DSL = Domain Specific Language，领域专用语言，写规则的格式）：condition（条件）+action（动作）是否完整？字段名拼对了没？运算符支持不支持？</li>
<li><b>② 信号源可用性</b>（signal availability）：这场景需要的信号，这辆车有没有？在线不在线？比如场景需要VLM信号（需要摄像头），但这辆车是低配没摄像头的→校验失败</li>
<li><b>③ 查重</b>（deduplication check）：是不是已经注册过同样的场景？防止同一个"到公司提醒拿外卖"注册两遍→到公司说两遍</li>
</ul>
<div class="case">
<b>实际case①——校验通过：</b><br/>
⑰注册"持续观测后排覆盖物位置" → 15-1检查：<br/>
- DSL格式：condition={signal:"rear_cover_position", op:"changed_to", value:"地板上"} ✓ 格式合法<br/>
- 信号可用：rear_cover_position来自VLM → 查车辆配置：赛力斯D6X有舱内摄像头+AIBOX在线 ✓<br/>
- 查重：没有重复场景 ✓<br/>
→ <b>校验通过 → 进15-2入库</b><br/><br/>
<b>实际case②——校验失败：</b><br/>
同样的场景注册到上汽D6X → 15-1检查：<br/>
- 信号可用：rear_cover_position来自VLM → 查车辆配置：上汽D6X<b>没有端侧VLM（只有云端VQA）</b>→ 端侧VLM信号不可用 ✗<br/>
→ 15-1返回失败原因<code>{error: "signal_unavailable", detail: "VLM not available on this vehicle, fallback to cloud VQA"}</code> → ⑰收到后改走VQA链路
</div>
<div class="up">⑬（配置平台）/⑰（动态Advisor）/⑳（Director）发来的场景注册请求（含condition定义+action定义+依赖的信号列表signal_dependencies）</div>
<div class="down">校验通过 → 15-2入库。校验失败 → 返回失败原因给注册方（如"端侧摄像头不可用"），注册方可以告知用户"当前设备不支持这个功能"</div>
</div>

<div class="atom">
<div class="atom-title">15-2 入库（Store，存进云端规则库）</div>
<p><b>【是什么】</b>规则存进云端规则库（cloud rule store），带状态标记：<code>active</code>（生效中，正在被扫描监听的）/ <code>inactive</code>（停用，用户关了但没删，随时能恢复）/ <code>expired</code>（过期，自动标记的）。</p>
<p><b>【怎么干】</b>入库时自动打时间戳（created_at）、分配rule_id、标active。同时建立索引（index）：按VIN（Vehicle Identification Number，车辆唯一编号）+关注的signal key建索引，15-3扫描时可以快速找到"这个信号变了，哪些规则关心它"。</p>
<div class="up">15-1校验通过的场景定义</div>
<div class="down">入库后供15-3扫描器周期性读取。<b>不入库 = 15-3扫不到 = 规则等于不存在</b>——就像快递不入仓库系统，配送员永远查不到</div>
</div>

<div class="atom">
<div class="atom-title">15-3 扫描（Scan，周期性检查信号变化）</div>
<p><b>【是什么】</b>两种触发方式：</p>
<ul>
<li><b>① 信号驱动（signal-driven）</b>：⑫云端信号库收到新数据时<b>立即触发</b>扫描——最实时（real-time），延迟<100ms</li>
<li><b>② 定时扫描（timer-driven）</b>：每5秒定时扫一遍（兜底fallback，防信号更新事件丢了）。只扫当前这辆车VIN下、这个场景关注的信号字段——不是全扫所有信号，按索引查</li>
</ul>
<div class="case">
<b>边界case——端侧断网时怎么办：</b><br/>
端侧断网 → ⑫长时间没收到新数据 → 信号驱动不触发 → 但5秒定时扫兜底会跑 → 扫到的是旧数据（last known value，最后一次收到的值）→ 旧数据跟上次快照（snapshot）一样 → 不会触发穿越（crossing）→ <b>不会误触发</b>。<br/>
网络恢复后 → ⑫批量收到断网期间积压的信号 → 逐条触发扫描 → 有穿越的正常处理（已含15-6抖动过滤）
</div>
<div class="up">⑫云端信号库的信号快照（④的云端镜像mirror）+ 15-2入库的active（生效的）规则列表</div>
<div class="down">命中（matched）的规则列表 → 15-4条件判定</div>
</div>

<div class="atom">
<div class="atom-title">15-4 条件判定（Condition Evaluation，运算符引擎——判断条件成不成立）</div>
<p><b>【是什么】</b>判断规则条件成不成立。分两类：</p>
<p><b>① 死规则（structured rule，结构化规则——纯数值比较）</b>：温度>45？距离<100m？播放次数==10？档位==P？这种直接套运算符（operator）比较。</p>

<table>
<tr><th>运算符（operator）</th><th>含义</th><th>实际case</th></tr>
<tr><td><code>=</code></td><td>等于（equals）</td><td><code>gear_current = "P"</code> → 当前档位是P档吗？</td></tr>
<tr><td><code>> <</code></td><td>大于/小于</td><td><code>temp_cabin > 45</code> → 舱内温度超过45度了吗？</td></tr>
<tr><td><code>>=  <=</code></td><td>大于等于/小于等于</td><td><code>battery_level <= 15</code> → 电量低于等于15%了吗？</td></tr>
<tr><td><code>range(a,b)</code></td><td>在a到b之间（inclusive，包含边界）</td><td><code>speed_vehicle range(60,120)</code> → 车速在60-120之间吗？</td></tr>
<tr><td><code>changed_to</code></td><td>值刚变成（状态变化检测）</td><td><code>gear_current changed_to "P"</code> → 档位刚刚从其他值变成P了吗？（和一直是P不同——已经是P不触发）</td></tr>
<tr><td><code>AND</code></td><td>且（所有条件都要满足）</td><td><code>belt_fr = "unbuckled" AND speed_vehicle > 10</code> → 副驾没系安全带<b>且</b>车速大于10</td></tr>
<tr><td><code>OR</code></td><td>或（任一条件满足即可）</td><td><code>temp_cabin > 40 OR humidity > 80</code> → 温度超40<b>或</b>湿度超80</td></tr>
</table>
<p>覆盖约80%的场景。二期补复杂的（如NOT非、嵌套括号、时间窗口聚合）。</p>

<p><b>② 非结构化数据（unstructured data，自由文本）</b>：VLM输出的是"后排座椅上有蓝色毛毯"这种自然语言（natural language），没法直接做数值比较 → 转交15-5模型推理（model inference）做语义判断（semantic evaluation，理解含义的判断）。</p>

<div class="case">
<b>实际case——一条复合规则（compound rule，多个条件AND连接）的判定过程：</b><br/>
规则："副驾没系安全带 AND 车速>10 AND 行驶中 → TTS提醒系安全带"<br/>
即：<code>belt_fr = "unbuckled" AND speed_vehicle > 10 AND valid_phases = "driving"</code><br/><br/>
读⑫当前信号快照：belt_fr="unbuckled"(✓), speed_vehicle="45km/h"→剥单位→45>10(✓), gear_current="D"≠"P"→行驶中(✓)<br/>
三个条件全满足 → <b>判定成立</b> → 送15-6
</div>
<div class="up">15-3扫描命中的规则 + ⑫对应信号的当前值（current value）</div>
<div class="down">死规则（structured）判定结果直接 → 15-6抖动过滤。非结构化数据 → 15-5模型推理后再 → 15-6</div>
</div>

<div class="atom">
<div class="atom-title">15-5 模型推理/语义编译（Model Inference / Semantic Compilation——"人话→指令"的翻译层）</div>
<p><b>【是什么】</b>干两件事：</p>
<p><b>事①：语义判断（Semantic Evaluation）</b>—— 处理"运算符判不了"的语义条件。VLM输出"后排有个人闭着眼歪着头"→ 15-4的运算符判不了"这是不是在睡觉" → 15-5调大模型（LLM，Large Language Model，大语言模型）推理 → 得出"是，概率92%"→ 成立。</p>
<p><b>事②：SP编译（SP Compilation）</b>—— 把Advisor（顾问）说的人话（如"盯副驾睡没睡"）编译（compile，翻译成机器能执行的格式）成端侧VLM能理解的结构化指令——也就是7字段运行指令（见§5 24-6的表）。</p>

<div class="case">
<b>实际case——SP编译的输入和输出：</b><br/>
<b>输入（Advisor的人话）：</b>"持续观测后排区域，如果覆盖物（毯子被子等）从座椅上掉到地板了就通知我"<br/><br/>
<b>15-5编译后输出（7字段运行指令）：</b><br/>
<code>{
  task_id: "VT-20260622-001",
  task_goal: "持续观测后排区域，上报覆盖物（毯子/被子/毛巾等）的位置（座椅上/地板上/人身上）。只描述客观状态，不做判断。",
  priority: "P2",
  timeout: 30,
  mutex_group: "VLM_CABIN",
  trigger_source: "dynamic_advisor_17",
  expire_at: "2026-06-22T18:00:00"
}</code><br/><br/>
<b>为什么需要翻译？</b>Advisor说的是人话，㉕感知器（Perception Scheduler）要的是结构化JSON。没有15-5这个翻译层（translation layer），人话发到端侧，端侧解析不了。
</div>

<p><b>【15-5在视觉链路里的关键位置——连接两个世界】</b></p>
<ul>
<li><b>人话世界→机器世界（下行downlink）</b>：Advisor说"盯宝宝"→ 15-5编译成7字段 → L14下发㉔ → ㉕调VLM执行</li>
<li><b>机器世界→事件世界（上行uplink）</b>：VLM吐的是状态（"一直在睡"），Advisor要的是事件（"刚睡着"）→ 15-10做穿越检测 + 15-11组装event → push给Advisor。<span class="pm">这是6.22最大的链路纠正——原来Advisor要轮询Context等事件，6.22改成触发器组装event后push</span></li>
</ul>
<div class="up">上行：⑫云端信号库中VLM输出的状态文本。下行：Advisor的视觉需求（人话，如"持续观测后排毯子位置"）</div>
<div class="down">上行：语义判断结果 → 15-6/15-10。下行：编译好的VLM task 7字段运行指令 → 经L14下发㉔ → 转发㉕感知器执行</div>
</div>

<h3>第②段：过滤仲裁（Filter & Arbitrate，过滤抖动+解决冲突）（15-6 ~ 15-9）</h3>

<div class="atom">
<div class="atom-title">15-6 抖动过滤（Debounce Filter，防止信号抖动导致重复触发）</div>
<p><b>【是什么】</b>信号在阈值（threshold，临界值）附近跳来跳去时不反复触发。规则：<b>30秒内同一个穿越方向不重复触发</b>（30-second same-direction deduplication）。</p>
<div class="case">
<b>实际case①——数值信号抖动：</b><br/>
规则："车速>130 → 提醒降速"<br/>
<div class="timeline">
<p>14:30:00 speed=128 → 不满足</p>
<p>14:30:01 speed=132 → 满足！穿越（crossing）发生：128→132跨过130 → <b>触发</b> ✓ → 开始30秒冷却</p>
<p>14:30:05 speed=129 → 掉回130以下</p>
<p>14:30:08 speed=131 → 又穿过130！但距上次触发仅7秒 < 30秒 → <b>抑制（suppress），不触发</b> ✗</p>
<p>14:30:35 speed=133 → 穿过130，距上次触发35秒 > 30秒 → <b>触发</b> ✓</p>
</div>
<br/>
<b>实际case②——VLM文本信号抖动：</b><br/>
毯子在座椅边缘，VLM一会说"在座椅上"一会说"在地板上"（模型不确定）：<br/>
<div class="timeline">
<p>14:32:00 VLM="座椅上"</p>
<p>14:32:03 VLM="地板上"→ 穿越！"座椅上→地板上" → <b>触发</b> ✓ → 开始30秒冷却</p>
<p>14:32:06 VLM="座椅上"→ 又变了！但冷却期内 → 不触发 ✗</p>
<p>14:32:09 VLM="地板上"→ 冷却期内 → 不触发 ✗</p>
<p>14:32:36 VLM="地板上"→ 30秒到了，但跟上次穿越方向相同（都是→地板上）→ <b>不触发</b>（没有新穿越发生）</p>
</div>
</div>
<div class="up">15-4/15-5判定为"条件满足"的规则列表</div>
<div class="down">过了抖动过滤的规则 → 15-7频控冷却</div>
</div>

<div class="atom">
<div class="atom-title">15-7 频控冷却（Frequency Control + Cooldown，频率控制+冷却期——防持续骚扰）</div>
<p><b>【是什么】</b>有些条件长时间成立（宝宝一直哭、高速一直超速）。15-6防短时抖动（jitter，几秒内的来回跳），15-7防"一直成立变持续骚扰（harassment）"。</p>
<p><b>【怎么干——两层防护】</b></p>
<ul>
<li><b>冷却期（cooldown period）</b>：触发后N秒内不再触发。如"宝宝哭了提醒"冷却=300秒（5分钟）</li>
<li><b>每日上限（daily cap）</b>：每天最多触发N次。如"宝宝哭了提醒"每日上限=5次</li>
</ul>
<div class="case">
<b>实际case——宝宝哭半小时的提醒频率：</b><br/>
规则："VLM检测到后排哭泣 → TTS提醒"，cooldown=300秒，daily_cap=5<br/>
<div class="timeline">
<p>14:00 检测到哭泣 → 第1次提醒 ✓ → 开始5分钟冷却</p>
<p>14:00~14:05 持续哭泣，但冷却期内 → 不触发</p>
<p>14:05 冷却结束，还在哭 → 第2次提醒 ✓</p>
<p>14:10 → 第3次 ✓</p>
<p>14:15 → 第4次 ✓</p>
<p>14:20 → 第5次 ✓ → 达到每日上限</p>
<p>14:25 还在哭 → <b>不再提醒（今天用完了）</b></p>
</div>
<b>没有频控的后果：</b>每500ms扫一次→每半秒提醒一次"宝宝在哭哦~"→半小时3600次→用户把车机砸了。
</div>
<div class="up">15-6过了抖动过滤的规则</div>
<div class="down">过了频控冷却的规则 → 15-8业务仲裁</div>
</div>

<div class="atom">
<div class="atom-title">15-8 业务仲裁（Business Arbitration，多个场景同时命中时判谁优先）</div>
<p><b>【是什么】</b>多个场景（scenario）同时命中时，判谁优先执行。有冲突（conflict）的只执行赢家（winner），不冲突的可以合并（merge）执行。</p>
<div class="case">
<b>实际case——降温 vs 节能冲突：</b><br/>
<b>当前状态：</b>temp_cabin=38度，battery_level=12%<br/>
<b>同时命中两条规则：</b><br/>
规则A（P2）："temp>35 → 开空调"—— 开空调要耗电<br/>
规则B（P1）："battery<15% → 关闭非必要设备省电"—— 要省电<br/><br/>
<b>仲裁过程：</b><br/>
① 查互斥组（mutex group）：A和B都涉及空调→同组→互斥<br/>
② 比priority（优先级）：B=P1 > A=P2 → <b>B赢，执行节能模式</b><br/>
③ 规则A暂缓（defer），标记reason="被P1节能模式抑制"<br/><br/>
<b>不冲突的case——可以合并：</b><br/>
规则C："temp>30 → 空调调到24度"<br/>
规则D："humidity>70 → 开座椅通风"<br/>
C用空调、D用座椅通风 → 不同设备 → 不冲突 → <b>同时执行</b>
</div>
<p><b>6.22后安全类场景（safety scenarios）归CDC了</b>——不再有"安全类和体验类冲突"的问题。剩下的都是体验类（comfort scenarios，舒适场景）之间的仲裁，复杂度降了很多。</p>
<div class="up">15-7过了频控的、可能多个同时命中的规则列表</div>
<div class="down">仲裁后的最终执行列表（final execution list）→ 15-9端云分类</div>
</div>

<div class="atom">
<div class="atom-title">15-9 端云分类（Edge-Cloud Classification，判定该云端跑还是车端跑）</div>
<p><b>【是什么】</b>判定这条规则该在哪执行。每条规则只走一条路——<b>不能两边同时跑</b>（铁律①）。</p>
<table>
<tr><th>走哪</th><th>判断依据（criteria）</th><th>实际case</th><th>为什么走这条路</th></tr>
<tr><td><b>发端侧（dispatch to edge）</b></td><td>需要VLM持续观测的 / 需要定时器的 / 弱网也要能跑的</td><td>"看毯子"（需VLM持续看）、"10分钟后关加热"（需本地倒计时）、"到目的地提醒"（断网也要能提醒）</td><td>VLM在端侧AIBOX上、定时器断网要能跑——这些本质上离不开端侧硬件</td></tr>
<tr><td><b>留云端（keep on cloud）</b></td><td>需要大模型推理（LLM inference）的 / 需要云端API（如天气/POI搜索/web search）的</td><td>"帮我查附近加油站"（要联网搜索POI）、"天气变化时提醒"（要调天气API）</td><td>大模型在云端跑（端侧算力不够跑Seed 1.8这种大模型）、API在云端→必须联网</td></tr>
<tr><td><b>看车型（vehicle-dependent）</b></td><td>视觉类场景：赛力斯有端侧VLM → 发端侧。上汽没有 → 走云端VQA</td><td>同一个"看后排宝宝"需求：赛力斯→走VLM端侧链路。上汽→走VQA云端链路（用户问一次看一张图，不能持续看）</td><td>硬件配置不同→能力不同→走不同的路</td></tr>
</table>
<div class="up">15-8仲裁后的最终执行列表</div>
<div class="down">发端侧的 → 15-12打包（package）→ L14下发㉔。留云端的 → L21通知⑲Director执行</div>
</div>

<h3>第③段：6.22新增的事件感知（Event Sensing）（15-10、15-11）——<span class="pm">这是6.22最大的链路纠正</span></h3>

<div class="atom">
<div class="atom-title">15-10 阈值穿越·变化检测（Threshold Crossing Detection——检测"状态从A变到B"的那个瞬间。6.22从端侧上收到云端）</div>
<p><b>【是什么】</b>持续监听（listen to）⑫云端信号库的信号变化，检测"状态从A变到B"的那个瞬间。</p>
<p><b>【前因——为什么叫"穿越"不叫"满足"】</b>状态一直true（真）不能一直触发——宝宝一直睡，不能每5秒提醒"宝宝睡了"。只有<b>"刚睡着"那一刻</b>（状态从"醒着"穿越到"睡着"）才算event（事件）。持续保持"睡着"不再触发。</p>
<p><b>【穿越检测的精确逻辑】</b>只认 <code>last（上次）= A → this（这次）= B</code> 且 <code>A ≠ B</code> 的穿越。</p>
<p><b>【不判穿越会怎样】</b>→ 持续成立（state=true）= 持续触发 = 持续骚扰。和15-7频控冷却（cooldown）是两个病：15-7管"触发太频繁"（同一个穿越触发后别太快再触发），15-10管"状态变化的那一刻"（只有从A变到B那一瞬间才算事件）。</p>

<div class="case">
<b>实际case——用完整数据演示一次穿越检测过程：</b><br/>
<b>场景：</b>"副驾睡着了把音乐调小"<br/>
<b>监听的信号：</b>VLM每3秒输出一次副驾状态<br/><br/>
<b>15-10内部维护一张"上次快照表"（last snapshot table）：</b><br/>
<code>last_values = { passenger_fr_state: "看手机" }</code>（上一轮的值）<br/><br/>
<div class="timeline">
<p><b>14:30:00</b> ⑫收到VLM新数据：passenger_fr_state = "看手机"</p>
<p>→ 15-10对比：last="看手机" vs this="看手机" → 相同 → <b>无穿越</b> → 更新last表</p>
<p><b>14:30:03</b> ⑫收到：passenger_fr_state = "看手机"</p>
<p>→ 相同 → 无穿越</p>
<p><b>14:30:06</b> ⑫收到：passenger_fr_state = "闭眼低头"</p>
<p>→ 15-10对比：last="看手机" vs this="闭眼低头" → <b>不同！检测到穿越！</b></p>
<p>→ 但先别急——可选加强：<b>连续确认窗口（confirm window）</b>= 连续2次（6秒）输出相同才算穿越成立</p>
<p><b>14:30:09</b> ⑫收到：passenger_fr_state = "闭眼低头"</p>
<p>→ 连续第2次 = "闭眼低头" → <b>确认窗口满足！穿越成立！</b></p>
<p>→ 生成原始穿越数据：<code>{field: "passenger_fr_state", from: "看手机", to: "闭眼低头", timestamp: "14:30:09", confirmed_at: "14:30:09"}</code></p>
<p>→ 送15-6抖动过滤（30秒同方向不重复）→ 通过 → 送15-11事件组装</p>
<p><b>14:30:12</b> ⑫收到：passenger_fr_state = "闭眼低头"</p>
<p>→ last="闭眼低头" vs this="闭眼低头" → 相同 → <b>无穿越</b>（持续保持不再触发）</p>
</div>
</div>

<p><b>【6.22从端侧上收到云端的原因】</b>如果端侧也做穿越检测、云端也做穿越检测→标准可能不一样（端侧说"穿越了"、云端说"没穿越"）→结论打架。所以只放一个地方——云端⑮统一判。端侧㉔只做防抖冷却（24-5），不做穿越判定。</p>
<div class="up">⑫云端信号库持续同步的端侧信号快照（signal snapshot from ④ mirror）</div>
<div class="down">检测到的穿越（crossing detected）→ 送15-6抖动过滤（debounce filter）→ 通过后送15-11事件组装（event assembly）</div>
</div>

<div class="atom">
<div class="atom-title">15-11 事件组装·push给Advisor（Event Assembly & Push——6.22新增，最大的链路纠正）</div>
<p><b>【是什么】</b>把15-10检测到的穿越（crossing），组装（assemble，打包）成Advisor能理解的<b>语义化event（semanticized event，有含义的事件描述）</b>，主动push（推送，不是等别人来问）给订阅了（subscribed to）的Advisor。</p>
<p><b>【前因——为什么这一步是6.22最大的纠正】</b>原来设计是Advisor轮询（polling）Context读状态变化。佳杰25:48当场纠正：Advisor不可能24小时反复去翻记事本等"用户睡醒了没"——浪费算力（compute waste）、不实时（not real-time）。事件必须是发生那一刻被推过来的。</p>
<p><b>【轮询（polling）会怎样——用数字算算】</b>假设轮询间隔5秒。系统里有4个静态Advisor + 平均3个动态Advisor = 7个Advisor。每个Advisor关心3个事件类型。每次轮询要读Context、找变化、判断。→ 7×3÷5 = <b>每秒4.2次无效查询</b>（99%时间都没变化）。而push模式：只有真发生变化时才推一次 → <b>每天可能只有几十次push</b>。算力差100倍。</p>

<div class="case">
<b>实际case——从原始穿越到语义化event的完整组装过程：</b><br/><br/>
<b>输入（15-10检测到的原始穿越）：</b><br/>
<code>{field: "rear_cover_position", from: "座椅上", to: "地板上", timestamp: "14:32:09"}</code><br/><br/>
<b>15-11组装过程：</b><br/>
① 查事件模板库（event template library）：rear_cover_position有变化→匹配模板"覆盖物位置变化"<br/>
② 填充模板（fill template）：<br/>
<code>{
  event_name: "后排覆盖物位置变化",
  event_detail: "覆盖物从座椅掉到地板",
  signal_key: "rear_cover_position",
  from_value: "座椅上",
  to_value: "地板上",
  timestamp: "14:32:09",
  source: "VLM",
  confidence: 0.87
}</code><br/><br/>
③ 查订阅表（subscription table）：哪些Advisor订阅了"rear_cover_position变化"这个event？<br/>
→ 找到：dynamic_advisor_17（⑰动态Advisor，"看毯子"任务的监工）订阅了 ✓<br/>
→ static_advisor_comfort（⑯舒适Advisor）没订阅 ✗<br/><br/>
④ push给所有订阅者：L18 push给 dynamic_advisor_17<br/><br/>
<b>⑰收到后做什么：</b><br/>
⑰读㉑Context的condition（背景条件）：后排有3岁儿童 → 结合event："毯子掉地上了+后排有小孩" → 生成advice（建议）："后排宝宝毯子掉了，建议提醒用户" → 发给⑲Director
</div>

<p><b>【边界case（edge cases，特殊情况）】</b></p>
<table>
<tr><th>特殊情况</th><th>怎么处理</th><th>为什么这么处理</th></tr>
<tr><td><b>Advisor正好在重启（restart）</b></td><td>⑮保留最近N条（如最近50条）未确认（unacknowledged）event在buffer（缓冲区）里。Advisor恢复后发ack确认请求 → ⑮补发（backfill）所有未确认的event</td><td>不保留的话Advisor重启期间的事件就丢了。宝宝毯子掉了但Advisor刚好在重启→没收到→不提醒→safety issue</td></tr>
<tr><td><b>Advisor已销毁（destroyed，任务结束了）</b></td><td>⑮检测到push失败（subscriber not found）→ 自动取消订阅（unsubscribe）→ 从订阅表里删这条 → 清理相关资源（清掉㉔上对应的VLM任务）</td><td>用户说"不用看了"→Director删GoalList目标→⑰动态Advisor销毁→但⑮不知道→继续push→返回404→触发自动清理</td></tr>
<tr><td><b>多个Advisor订阅同一event</b></td><td>⑮同时push给所有订阅者，各自独立消费（independent consumption）。不是抢占式的——一个收到了不影响另一个</td><td>"档位从R变P"可能同时被出行Advisor和安全Advisor关心→两个都要收到</td></tr>
<tr><td><b>网络恢复后信号批量同步（batch sync）</b></td><td>去重（dedup）+合并（merge）：同一个信号的多次穿越只算最终状态。如断网期间毯子从"座椅→地板→座椅→地板"→只推最终的"座椅→地板"一次穿越</td><td>不去重的话一次性推4个穿越event→Advisor收到4次提醒→混乱</td></tr>
</table>
<div class="up">15-10检测到的阈值穿越（原始穿越数据）+ 订阅表（subscription table，记录哪些Advisor订阅了哪些event类型）</div>
<div class="down">L17 push event → ⑯静态Advisor（4个长期顾问）；L18 push event → ⑰动态Advisor（临时监工）。Advisor收到event后结合㉑Context的condition做综合判断 → 给⑲Director发advice（建议）</div>
</div>

<h3>第④段：下发（Dispatch，把规则和指令分发出去）（15-12 ~ 15-14）</h3>

<div class="atom">
<div class="atom-title">15-12 标准事件打包（Standard Event Packaging，把触发结果打成标准格式）</div>
<p><b>【是什么】</b>命中结果打包成标准JSON格式给下游消费。</p>
<div class="case">
<b>实际打包结果长这样：</b><br/>
<code>{
  task_id: "TRIG-20260622-003",
  rule_id: "RULE-GEO-012",
  signal_snapshot: { charger_distance: "45m", battery_level: "32%", gear_current: "D" },
  triggered_at: "14:25:03",
  action_list: [{ type: "tts", content: "附近有充电桩，要去充电吗？当前电量32%" }]
}</code>
</div>
<div class="up">15-8仲裁后的触发结果（trigger result）</div>
<div class="down">标准格式 → L21通知Director（如果留云端执行） / L14下发端侧（如果发端侧执行）</div>
</div>

<div class="atom">
<div class="atom-title">15-13 状态→事件push（State-to-Event Push，状态类信号达到阈值也能转成事件推给Advisor）</div>
<p><b>【是什么】</b>和15-11的区别：15-11是<b>阈值穿越（crossing，从A变到B的瞬间）</b>，15-13是<b>阈值到达（threshold reached，值到了某个阈值）</b>。</p>
<div class="case">
<b>区别举例：</b><br/>
15-11穿越：档位从R变P → 只在变化那一瞬间触发<br/>
15-13到达：电量降到15%以下 → 第一次降到15%时触发一次（后续保持<15%不再触发——和穿越一样只触发一次，但检测的对象不同：穿越是"值变了"，到达是"值达到阈值了"）
</div>
<div class="up">15-4条件判定结果中"需要通知Advisor"的</div>
<div class="down">组装成event → push给⑯⑰Advisor</div>
</div>

<div class="atom">
<div class="atom-title">15-14 动态视觉SP下发（Dynamic Visual SP Dispatch，把新的观测指令下发给端侧VLM）</div>
<p><b>【是什么】</b>把动态视觉任务的注意力SP（attention system prompt，告诉VLM"额外要看什么"的提示词）下发给端侧VLM。这是VLM知道"新增了什么观测目标"的<b>唯一通道（only channel）</b>。</p>
<div class="case">
<b>边界case——弱网SP下发失败的重试逻辑（retry logic）：</b><br/>
<b>14:25:00</b> ⑮尝试下发VLM task给㉔ → 网络延迟高 → 超时（timeout）<br/>
<b>14:25:01</b> 第1次重试（retry #1）→ 失败<br/>
<b>14:25:04</b> 第2次重试（retry #2，间隔递增backoff=3秒）→ 失败<br/>
<b>14:25:09</b> 第3次重试（retry #3，间隔=5秒）→ 失败<br/>
→ 3次都失败 → 标记"待下发（pending dispatch）"存到重试队列（retry queue）<br/>
→ 这段时间端侧用上次缓存（cached）的场景继续运行<br/>
<b>14:28:00</b> 网络恢复 → ⑮检测到pending任务 → 自动补发（auto-backfill）→ ㉔收到 → 转发㉕ → VLM开始新的观测
</div>
<div class="up">⑰动态Advisor拆解出的注意力SP + 15-5编译后的VLM task 7字段运行指令</div>
<div class="down">L14 → ㉔端侧触发器 → 转发给㉕感知器（Perception Scheduler）→ <b>增量追加（incremental append）</b>到VLM prompt列表（不是覆盖overwrite！原有AlwaysOn的SP不动）</div>
</div>


<!-- ============ §7 ============ -->
<h2 id="s7">七、VLM（Vision Language Model，端侧视觉语言模型）链路：18步完整walkthrough（走查，按步骤走一遍）</h2>

<p>这是6.22讨论最久的部分（19:19-42:34，持续23分钟）。用<b>"照看后排宝宝，毯子掉了提醒我"</b>这个case串完整链路。每一步标明数据格式。</p>

<div class="up">用户语音query（原话）"帮我照看后排宝宝，毯子掉了提醒我" → ASR（自动语音识别）转文字 → ⑨判断为复杂需求 → ⑲Director（决策者）</div>
<div class="down">最终输出 → ㉖TTS播报给用户"检测到宝宝的毯子掉到地上了哦" + 执行结果写入㉑Context（全局记事本）</div>

<h3>步骤1-5：用户→Director→Advisor拆解（云端部分）</h3>
<p><b>步骤1</b>：用户说"帮我照看后排宝宝，毯子掉了就提醒我"→ ①麦克风收音 → ②ASR（Automatic Speech Recognition，自动语音识别）转文字 → L2上传（upload）云端。</p>
<p><b>步骤2</b>：⑨意图分流（Intent Router）：不是车控指令（没有"打开/关闭"这种动词）、不是条件任务（不是"N分钟后/到哪里"的简单条件）、是<b>复杂需求</b>（需要拆解成多个步骤+持续监控）→ L5送⑲Director。</p>
<p><b>步骤3</b>：⑲汇入㉑Context（注入当前背景信息——"后排有一名3岁儿童，坐在左后座，系着安全座椅"）→ ⑳Director推理（inference，Seed 1.8大模型计算）→ 判断为<b>持续监控任务</b>（continuous monitoring task）→ ⑱GoalList（目标看板）建目标"监控后排宝宝覆盖物"→ TTS回复用户"好的，我帮你看着宝宝哦~"</p>
<p><b>步骤4</b>：⑱建监工 → ⑰创建Dynamic Advisor实例（dynamic advisor instance，一个专门盯这件事的临时AI助手）——每个目标（goal）对应一个独立的⑰实例。这个实例专盯"后排宝宝毯子"，直到任务结束或过期。</p>
<p><b>步骤5</b>：⑰拆解需求（decompose requirement）：需要持续视觉观测"覆盖物位置" → 改写注意力SP（attention system prompt）："持续观测后排区域，上报覆盖物（毯子/被子/毛巾等）的位置（座椅上/地板上/人身上）。只描述客观状态，不做判断。" → 向⑮注册（register）视觉类event"覆盖物位置变化" + 订阅（subscribe）这个event。</p>

<div class="quote"><b>仁杰 33:53纠正：</b>Director<b>不直接到触发器</b>。链路是 Director → 先建⑰动态Advisor（临时监工）→ Advisor拆解query、改写SP → 调⑮注册。<b>为什么不能跳过Advisor直连？</b>因为Advisor负责持续监控+消费event，Director只负责一次性决策。Director没有"持续盯着"的能力。</div>

<h3>步骤6-9：触发器注册→SP下发端侧→VLM开始观测</h3>
<p><b>步骤6</b>：⑮收到⑰的注册请求 → 15-1能力校验（iCanDo）：端侧有摄像头？✓。AIBOX在线？✓。VLM可用？✓。没有重复场景？✓ → <b>校验通过</b> → 15-2入库。如果校验失败（如上汽没有端侧VLM）→ 返回失败原因 → ⑰告知Director → Director告诉用户"抱歉，当前车辆不支持持续视觉监控，我可以帮你定时拍照看看"。</p>
<p><b>步骤7</b>：⑮ 15-9端云分类：需要VLM<b>持续</b>观测 → 必须在端侧跑（云端VQA只能一次性看一张图）→ 发端侧。15-5编译（compile）成7字段运行指令 → L14下发㉔。</p>
<p><b>步骤8</b>：㉔收到场景 → 24-2持久化存储（persist，断电不丢）→ 24-6发运行指令(7字段task descriptor)给㉕感知器。</p>
<p><b>步骤9</b>：㉕收到指令 → 检查摄像头mutex_group=VLM_CABIN → 舱内摄像头当前跑着P3的AlwaysOn → 新任务P2>P3 → <b>AlwaysOn暂停</b> → <b>增量追加（incremental append）</b>新SP到VLM prompt列表（<b>不是覆盖！</b>原有AlwaysOn的SP不受影响——AlwaysOn恢复后继续用自己的SP，新SP额外追加在后面）。</p>

<div class="quote"><b>丁彬 01:03:12原话：</b>"如果你不是增量的话，你就会导致原来的prompt就没了，原来感知的内容也没了。"——比如AlwaysOn本来能看到"主驾在打哈欠"，你一覆盖，只能看毯子了，打哈欠的感知就断了。</div>

<h3>步骤10-14：VLM观测→信号写入→云端检测穿越→push给Advisor</h3>
<p><b>步骤10</b>：端侧VLM每3秒输出一次<b>纯客观状态描述（objective state description，只说看到什么，不做判断）</b>：</p>
<div class="timeline">
<p>14:32:00 "后排座椅上有蓝色毛毯，覆盖儿童腿部。儿童闭眼，呼吸平稳。"</p>
<p>14:32:03 "后排座椅上有蓝色毛毯，覆盖儿童腿部。儿童闭眼。"（和上次基本相同→24-7去重dedup→不写④）</p>
<p>14:32:06 "后排座椅上有蓝色毛毯，覆盖儿童腿部。"（相同→跳过）</p>
<p>14:32:09 <b>"后排地板上有蓝色毛毯。儿童腿部无覆盖物。"</b> ← <b>值变了！</b></p>
</div>
<p><b>为什么VLM只报状态不做判断？</b>VLM说"毯子在地上"是<b>客观事实（objective fact）</b>——当前这一帧图片里毯子确实在地上。"毯子掉了"是<b>判断（judgment）</b>——需要知道上一帧毯子在哪（在座椅上）→ 现在在地上 → 所以是"掉了"。VLM只看当前帧不记历史（no memory across frames），做不了"和上次比较"这个动作。<b>比较交给⑮的15-10穿越检测做</b>——它维护着上次快照。（西钺46:12+丁彬46:24确认）</p>

<p><b>步骤11</b>：24-7归一+去重 → 24-8写入④信号库 <code>{key: "rear_cover_position", value: "地板上", source: "VLM", timestamp: "14:32:09", ttl: 6}</code> → L9同步⑫云端信号库。</p>
<p><b>步骤12</b>：⑮ 15-10持续监听⑫，对比上次快照（last="座椅上"）和这次（this="地板上"）→ <b>检测到阈值穿越！</b>→ 可选加强：等下一次VLM输出（14:32:12）也是"地板上"→ 连续确认窗口满足 → <b>穿越确认！</b>→ 过15-6抖动过滤（30秒去重）→ 通过。</p>
<p><b>步骤13</b>：⑮ 15-11组装语义化event：<code>{event_name: "后排覆盖物位置变化", from: "座椅上", to: "地板上", timestamp: "14:32:09", source: "VLM", confidence: 0.87}</code> → 查订阅表 → dynamic_advisor_17订阅了 → <b>push给⑰</b>。</p>
<p><b>步骤14</b>：⑰收到push的event → 开始消费（consume，处理这个事件）。</p>

<h3>步骤15-18：Advisor消费event→VQA确认→Director决策→播报</h3>
<p><b>步骤15</b>：⑰判断是否需要VQA（Visual Question Answering，云端视觉问答）二次确认。当前逻辑：VLM端模（edge model，端侧小模型）能力有限（准确率~85%），调一次VQA确认提高可信度 → 生成VQA请求："拍一张后排的照片，确认蓝色毛毯是否在地板上，儿童腿部是否无覆盖物"。</p>
<p><b>步骤16</b>：VQA链路：端侧抓帧（capture frame，拍一张照片）上传 → 云端VQA模型看图+问题 → 推理答案："确认，蓝色毛毯在地板上。儿童腿部无覆盖物。" VQA优先级P0，会抢占（preempt）VLM（但只需几百毫秒抓一帧，影响很小）。</p>
<p><b>步骤17</b>：⑰给⑲Director发advice（建议）：<code>{suggestion: "后排宝宝的蓝色毯子掉到地板上了（VLM检测+VQA确认），儿童腿部无覆盖物。建议提醒用户。", priority: "P2", confidence: 0.95}</code></p>
<p><b>步骤18</b>：⑲汇入 → ⑳Director推理 → talk_content（播报内容）="检测到宝宝的毯子掉到地上了哦，要不要在下一个服务区停一下帮宝宝盖好？" → ㉖TTS播报。同时写㉑Context记录这次事件。</p>


<!-- ============ §8 ============ -->
<h2 id="s8">八、VQA（Visual Question Answering，云端视觉问答——"现在帮我看一眼"）链路：5步walkthrough + 和VLM的7项区别</h2>

<p>VQA和VLM最容易搞混。<b>一句话区分：VLM是"一直帮我看着"（端侧持续跑），VQA是"现在帮我看一眼"（云端一次性问答）。</b></p>

<div class="up">两种触发来源：① 用户query（原话）如"窗外什么天气" ② ⑰Advisor的确认请求（confirmation request）如"毯子真的掉了吗——帮我用VQA确认一下"</div>
<div class="down">VQA结果（answer）→ 返回给⑳Director或⑰Advisor + 注入㉑Context</div>

<h3>5步walkthrough（以"窗外什么天气"为例）</h3>
<p><b>步骤1</b>：用户"窗外什么天气" → ②ASR转文字 → ⑨判断需要视觉信息（含"窗外""看看"这类视觉关键词）→ ⑲Director。</p>
<p><b>步骤2</b>：⑳Director推理，决定调<code>search_visual_info</code>工具（Director的23个工具之一——"拍照看看"功能）。这个工具调用触发器完全不参与——VQA和触发器<b>没关系</b>。</p>
<p><b>步骤3</b>：㉒工具库（Tool Library）执行 → 下发抓帧（capture frame）指令到端侧。VQA优先级P0（最高），如果VLM正在用摄像头，<b>VQA抢占</b>——抓帧很快（~100ms毫秒），VLM暂停时间极短。</p>
<p><b>步骤4</b>：图片上传（upload）云端 → 云端VQA模型看图（image understanding）+用户问题 → 推理答案："图像显示车窗外正在下中雨，路面有积水，能见度约200米。" <b>断网不能用</b>（no offline capability）——图片上传不了就用不了。</p>
<p><b>步骤5</b>：结果返回⑳Director → 组织话术（compose response） + 注入㉑Context（记录"当前天气：中雨"作为condition供后续使用）→ TTS："外面在下雨呢，能见度不太好，要帮你关车窗吗？"</p>

<h3>VQA vs VLM 7项区别——完整对照表</h3>
<table>
<tr><th>对比项</th><th>VQA（Visual Question Answering，云端视觉问答）</th><th>VLM（Vision Language Model，端侧视觉语言模型）</th><th>实际case对照</th></tr>
<tr><td><b>触发方式</b></td><td>用户主动问 / Advisor做二次确认 → <b>被动触发</b></td><td>系统后台持续跑，用户不需要说话 → <b>主动运行</b></td><td>VQA："窗外什么天气"（用户问了才看）。VLM："帮我看毯子"（设好后自动一直看）</td></tr>
<tr><td><b>运行位置</b></td><td>云端（cloud，必须联网）</td><td>端侧AIBOX（edge，本地跑）</td><td>VQA图片上传到火山引擎的GPU集群推理。VLM在车上AIBOX的NPU（Neural Processing Unit，神经网络处理器）上跑</td></tr>
<tr><td><b>频率</b></td><td>一次性问答（one-shot Q&A）</td><td>每3-5秒一帧，持续运行（continuous at 3-5s intervals）</td><td>VQA：看一张图答一个问。VLM：24小时不停地每3秒看一帧</td></tr>
<tr><td><b>跟触发器的关系</b></td><td><b>无关（independent）</b>。Director→search_visual_info→抓帧→云端推理→结果回Director。触发器⑮/㉔完全不参与</td><td><b>深度绑定（tightly coupled）</b>。触发器注册场景/下发SP/监听信号变化/做穿越检测/组装event/push给Advisor</td><td>VQA的数据流不经过⑮。VLM的数据流经过④→⑫→⑮→⑰→⑲整条链路</td></tr>
<tr><td><b>断网能用吗</b></td><td><b>不能</b>——图片传不上去就没法用</td><td>端侧VLM继续跑（模型在本地），但⑮收不到数据（④→⑫同步断了），event组装暂停。恢复后补发</td><td>隧道里：VQA不可用。VLM还在看，但结果暂时存本地等网络恢复</td></tr>
<tr><td><b>摄像头占用</b></td><td>抓一帧就释放（~100ms毫秒，几乎感觉不到）</td><td>持续占用（每3-5秒一帧，一直跑）</td><td>VQA像拍一张照——咔嚓一下完事。VLM像开着录像机一直录</td></tr>
<tr><td><b>优先级（priority）</b></td><td><b>P0最高</b>，会抢占VLM——因为是用户主动问的，要快</td><td>P2-P3，被VQA抢占时暂停（pause）</td><td>用户正在等"窗外天气"的答案（P0 VQA）→VLM的"看毯子"暂停100ms让VQA抓帧→VQA完事→VLM恢复</td></tr>
</table>

<div class="box gray">
<b>佳杰提到的资源竞争问题（39:07原话逻辑）：</b>上汽那边没有端侧VLM，用云端VQA。但端侧取图（capture frame）时会和其他业务（如AlwaysOn常驻观测）抢占摄像头——VQA要抓帧、AlwaysOn也要抓帧→撞车。所以佳杰主张：<b>"所有信号类的东西并到一个模块（信号库④）提供，事件类的也并到一个模块（触发器⑮）"</b>——避免各自重复造轮子（reinventing the wheel）和资源竞争。统一管控，效率更高。
</div>


<!-- ============ §9 ============ -->
<h2 id="s9">九、Advisor（顾问）/ Director（决策者）/ Context（记事本）/ GoalList（目标看板）：四角色关系</h2>

<table>
<tr><th>角色</th><th>是什么（类比）</th><th>干什么</th><th>上游（谁给我数据）</th><th>下游（我给谁数据）</th><th>实际case</th></tr>
<tr><td><b>⑳Director</b><br/>（老板/CEO）</td><td>对话推理引擎（Dialog Reasoning Engine），底层是Seed 1.8大模型。<b>唯一拍板的人</b>——说不说话、说什么、做什么，只有它能决定</td><td>收集所有信息→推理→输出3件事：talk_or_not（说不说话）/ talk_content（说什么）/ action_list（做什么）</td><td>⑲汇入：用户query（原话）+ Advisor的advice（建议）+ Context的condition（背景状态）</td><td>talk_content → ㉖TTS播报；action_list → ㉒工具库执行（调23个工具之一）；goal → ⑱GoalList建/删目标</td><td>收到⑰的advice"毯子掉了"→推理→决定说话→"宝宝毯子掉了哦~"→同时决定不主动帮盖（需要停车，太危险）→建议用户"要不要下个服务区停一下"</td></tr>
<tr><td><b>⑯静态Advisor×4</b><br/>（4个部门经理）</td><td>4个长期运行的参谋（long-running advisors），分别负责：<b>舒适</b>（温度/通风/座椅）/ <b>出行</b>（导航/路况/POI）/ <b>情感</b>（疲劳/情绪/音乐推荐）/ <b>内容</b>（播客/短视频/新闻）</td><td>读Context的condition + 接⑮push的event → 出advice（建议）给Director</td><td>㉑Context（背景条件condition）+ ⑮push的event（事件通知）</td><td>advice（建议）→ ⑲Director汇入 → Director决定采不采纳</td><td>舒适Advisor读到Context里"temp_cabin=35度"(condition) + 收到⑮push的event"温度刚超过35度" → 出advice"建议开空调调到24度"→ Director决定执行</td></tr>
<tr><td><b>⑰动态Advisor</b><br/>（临时监工）</td><td>Director按需创建的临时AI助手（on-demand instance），每个目标（goal）一个实例。任务结束就销毁</td><td>持续监控（monitor）+出advice。可向⑮注册event订阅（subscribe to events）</td><td>⑱GoalList分配的目标 + ⑮push的event + ㉑Context</td><td>advice → ⑲Director；注册请求（registration request）→ ⑮触发器</td><td>"看毯子"任务→Director建GoalList目标→⑰实例创建→订阅"覆盖物位置变化"event→收到→出advice"毯子掉了"→Director播报</td></tr>
<tr><td><b>㉑Context</b><br/>（记事本/白板）</td><td>全局统一事实源（Single Source of Truth），所有模块共享的"记事本"</td><td>存所有condition/状态——"现在车里什么情况"的完整记录</td><td>④信号库按策略上报 + VQA结果注入 + 拒识事件日志 + 执行结果回写</td><td>所有模块都从这里读condition（背景条件）</td><td>存着：temp=28度、后排有3岁儿童、副驾在看手机、电量78%、当前导航去公司... 所有人读这一份</td></tr>
<tr><td><b>⑱GoalList</b><br/>（共享看板/任务板）</td><td>Director和Advisor之间的共享任务板（shared task board）。类似Jira看板</td><td>Director建/删目标，分配给不同Advisor执行</td><td>⑳Director通过<code>goal_list_update</code>工具（23个工具之一）建/删目标</td><td>目标（goal）→ 触发⑰动态Advisor实例创建</td><td>Director决定要看毯子→goal_list_update建目标{id: "G003", name: "监控后排覆盖物", assigned_to: "dynamic_advisor"}→⑰实例创建</td></tr>
</table>


<!-- ============ §10 ============ -->
<h2 id="s10">十、通用视觉事件通道（Generic Visual Event Channel，处理预设清单外的长尾视觉需求）</h2>

<p>动态Advisor（⑰临时监工）可能提出预设清单（preset list，提前列好的事件列表）里没有的观测需求。比如用户突然说"帮我看看小孩有没有跑到前排"——这个在预设清单里没有。怎么办？6.22西钺+佳杰提出：在触发器支持配置<b>"通用视觉事件（generic visual event）"</b>。</p>

<div class="up">⑰动态Advisor的泛化视觉query（generalized visual query，预设事件集cover覆盖不了的需求）</div>
<div class="down">通过⑮编译 → L14下发 → ㉔转发 → 透传（passthrough，原样传递）给AIBOX的VLM system prompt → 端模尝试观测 → 结果写④信号库 → ⑮检测变化push event</div>

<h3>两条路：有限集（主力，效果好）+ 通用（兜底，效果不保证）</h3>
<table>
<tr><th>类型</th><th>是什么</th><th>效果</th><th>实际case列举</th><th>为什么效果不同</th></tr>
<tr><td><b>有限集事件（Finite Set Events）</b>——主力</td><td>提前列好的、可以针对性优化prompt效果的</td><td>效果好、稳定、准确率高（~90%+）</td><td>睡醒（woke up）/ 驶出地库（exiting garage）/ 开始打手机（start using phone）/ 结束打手机（stop using phone）/ 物品出现（object appeared）/ 物品移除（object removed）/ 系安全带（buckled）/ 解安全带（unbuckled）</td><td>这些事件是提前知道的，VLM的prompt可以针对性写（如"重点关注眼睛状态判断是否睡醒"），训练数据也可以针对性采集</td></tr>
<tr><td><b>通用视觉事件（Generic Visual Events）</b>——兜底</td><td>用户临时说的、预设清单没有的，走通用通道透传</td><td>效果不确定，可能准可能不准</td><td>"看看小孩是不是跑到前排了"/ "后排有没有人在吃东西"/ "主驾的手有没有离开方向盘"/ "后备箱的猫在干嘛"</td><td>没有针对性优化。VLM收到的是泛化的prompt（如"观测后排是否有人移动到前排区域"），没有专门训练过这类场景，准确率可能只有60-70%</td></tr>
</table>

<div class="quote"><b>佳杰 01:04:33原话：</b>"如果走通用通道，我们也有策略，可能让豆包先说一句：'这个任务我现在可能做的还不是那么好，你确定要让我试试吗？'"——给用户预期管理（expectation management，提前告诉用户效果可能不好），减少用户失望。</div>

<h3>通用视觉事件的三类划分（3 categories，按观测内容分类）</h3>
<table>
<tr><th>类别</th><th>看什么</th><th>用哪个摄像头</th><th>变化频率</th><th>实际case</th><th>追加到VLM的哪个prompt slot（提示词槽位）</th></tr>
<tr><td><b>舱内静态（Cabin Static）</b></td><td>性别、年龄、穿着等长时间不变的</td><td>舱内摄像头（cabin camera）</td><td>低频，几分钟检测一次</td><td>"后排是不是坐了个小孩"、"副驾穿的什么颜色衣服"</td><td>cabin_static_slot——不和动态观测混在一起</td></tr>
<tr><td><b>舱内动态（Cabin Dynamic）</b></td><td>动作、表情、姿态等快速变化的</td><td>舱内摄像头</td><td>高频，3-5秒一次</td><td>"副驾有没有在打电话"、"后排小孩在哭吗"、"毯子掉了没"</td><td>cabin_dynamic_slot——需要高频刷新</td></tr>
<tr><td><b>舱外（Exterior）</b></td><td>路况、天气、周围环境</td><td>前摄（front camera，目前只用前摄，后续可能加侧摄）</td><td>中频</td><td>"前方路边有没有停车位"、"路况拥堵程度"</td><td>exterior_slot</td></tr>
</table>

<div class="box red">
<b>关键铁律：prompt做增量追加（incremental append），不能覆盖（overwrite）。</b><br/>
丁彬01:03:12强调：新任务的SP加到prompt列表的对应slot里，原有AlwaysOn的SP不动。否则加了新的观测项"看毯子"，原来能感知的"打哈欠""打电话"就都没了。<br/>
西钺01:03:45补充：下发任务时还要标清（label）是舱内/舱外、静态/动态，因为增量追加到<b>不同的prompt slot</b>。标错了slot→VLM把舱外的prompt当舱内的用→用前摄看后排→看到的全是前方道路不是后排座椅→任务失败。
</div>


<!-- ============ §11 ============ -->
<h2 id="s11">十一、任务中心（Task Center）对接：佳杰需要触发器提供什么</h2>

<p>这是佳杰在51:07-55:08提出的，之前没聊过的新需求。佳杰做的是任务中心模块——用户在车机屏上能看到的"待办事项列表"（to-do list）。</p>

<div class="up">㉓云端任务中心（Cloud Task Center）的任务数据（静态Advisor+动态Advisor+触发器产生的任务）→ 需要同步（sync）到端侧</div>
<div class="down">端侧任务中心 → 给用户HMI（车机屏幕）展示任务列表 → 接受用户的enable（开启）/disable（关闭）/delete（删除）/restore（恢复）操作 → 操作结果反向写入（write back）㉔触发器</div>

<h3>佳杰的4个核心需求</h3>

<p><b>1. 端云任务同步机制（Edge-Cloud Task Sync，51:59）</b></p>
<p>云端配的任务要同步到端侧。端侧要有持久化数据库（persistent database，断电不丢的数据库）存这些任务。弱网时也能看到任务列表——用户进隧道不能连任务列表都没了。</p>
<div class="quote"><b>佳杰 51:59原话：</b>"我是只从你端上取数据的。你需要第一个建设的能力就是端云的任务同步机制。"</div>

<p><b>2. 任务级（task-level）vs 感知级（signal-level）字段区分（52:33）</b></p>
<p>触发器里的数据不全都该给用户看。</p>
<table>
<tr><th>类型</th><th>说明</th><th>是否在任务中心显示</th><th>实际case</th></tr>
<tr><td><b>任务级（task-level）</b></td><td>用户看得懂的功能描述。像App里的待办事项一样</td><td><b>是</b> — 展示在HMI（车机屏）上</td><td>"充电桩靠近自动开盖"（用户看得懂）<br/>"帮我照看后排宝宝"（用户设的）<br/>"10分钟后关座椅加热"（用户设的倒计时）</td></tr>
<tr><td><b>感知级（signal-level）</b></td><td>底层实现细节，用户看了没意义、看了也看不懂</td><td><b>否</b> — 只在后台</td><td>"订阅信号：rear_cover_position变化"（什么鬼？）<br/>"监听charger_distance<50m"（用户不知道charger_distance是啥）<br/>"VLM task VT-20260622-001 running"（纯技术状态）</td></tr>
</table>
<p>→ 触发器需要在每条规则上加一个字段：<code>display_level: "task" | "signal"</code>。任务中心只拉（pull）display_level="task"的数据。</p>

<div class="quote"><b>佳杰 52:33原话：</b>"我不可能把所有你触发器里面很原始的订阅的信号都拿过来做显示。只有那些对用户有价值的'任务'，我才会在任务中心做显示。"</div>

<p><b>3. 四种操作接口（4 Operation APIs，55:08-56:05）</b></p>
<table>
<tr><th>操作（API）</th><th>用户做了什么</th><th>触发器㉔要做什么</th><th>实际case</th></tr>
<tr><td><b>enable（启用）</b></td><td>用户把之前关的任务打开了</td><td>㉔重新激活（reactivate）这条规则→恢复信号监听和执行</td><td>用户在HMI上把"充电桩靠近提醒"开关打开 → ㉔恢复监听charger_distance信号</td></tr>
<tr><td><b>disable（停用）</b></td><td>用户暂时关掉某个任务（不删除，以后还可能开）</td><td>㉔把这条规则标inactive（停用）→停止监听、停止执行</td><td>用户觉得"到公司提醒拿外卖"今天不需要 → disable → ㉔停止监听GPS距离</td></tr>
<tr><td><b>delete（删除）</b></td><td>用户从待办（to-do list）移除某任务</td><td><b>软删除（soft delete）</b>——标记"不显示"、从HMI列表消失，但场景定义（rule definition）保留在后台。为什么不硬删？万一用户后悔了还能restore</td><td>用户滑掉"充电桩提醒" → HMI上消失 → 但⑮/㉔的规则库里标记deleted=true → 不扫描不触发但数据还在</td></tr>
<tr><td><b>restore（恢复）</b></td><td>用户从"推荐场景"里找回之前删掉的</td><td>恢复显示 + 重新激活（set active + visible）</td><td>用户在"推荐场景"列表看到"充电桩提醒"→点"启用"→restore→HMI重新显示+㉔重新激活规则</td></tr>
</table>

<p><b>4. 佳杰怎么判断哪些显示（53:54）</b></p>
<div class="quote"><b>佳杰 53:54原话：</b>"这些都是预设的，一定是可穷举（enumerable，可以列出全部）的。你可以先标一轮，我们大家看看。"——意思是泽民先把所有规则标一遍task/signal，佳杰审一遍就行。</div>


<!-- ============ §12 ============ -->
<h2 id="s12">十二、AlwaysOn（常驻观测，一直在后台跑的基础视觉任务）调度归属</h2>

<p>西钺问了（57:25）：AlwaysOn的调度（scheduling，决定什么时候执行、优先级多少）是触发器㉔做吗？</p>

<div class="quote"><b>佳杰 57:47原话：</b>"AIBOX上有一个模块是专门对所有请求做优先级排序的。AlwaysOn优先级大概多少、VQA优先级如何、其他视觉类需求优先级如何——优先级低的排队。"</div>

<div class="box blue">
<b>结论——两层不同的东西，别搞混：</b><br/><br/>
<b>触发器（Trigger Engine）㉔/⑮ 管 "什么时候触发"（WHEN）</b>：什么condition（条件）满足了就触发什么action（动作）。比如"温度>30→开空调"、"毯子掉了→提醒用户"。<br/><br/>
<b>AIBOX任务调度（Task Scheduler）管 "谁先执行"（WHO FIRST）</b>：VLM同时收到多个任务时，按priority（优先级）排序——P0的VQA先跑、P2的"看毯子"排队、P3的AlwaysOn最后。<br/><br/>
类比（analogy，打个比方）：触发器是"老板说今天要拍照"（决定做不做），AIBOX调度是"先拍VIP客户还是先拍普通客户"（决定先做哪个）。两件不同的事。
</div>

<div class="case">
<b>实际case——动态视觉任务的下发路径：</b><br/>
西钺追问（58:54-01:02:35）：动态视觉观测任务（如"看被子掉没掉"）怎么传给端侧？<br/><br/>
<b>确认的路径：</b>走触发器的云到端链路 —— ⑮ → L14 → ㉔ → ㉕感知器 → AIBOX任务调度排队 → VLM执行。<br/>
<b>不是</b>AlwaysOn自己去拿。AlwaysOn是自己一直跑自己的基础观测任务，不会主动去问"有没有新任务"。
</div>


<!-- ============ §13 ============ -->
<h2 id="s13">十三、6.22 会议全部讨论点还原（37个议题）</h2>

<p>按时间顺序（chronological order）列出会上所有讨论过的议题。每个标：<b>谁提的</b>（时间戳）/ <b>结论是什么</b> / <b>和你的产品工作有什么关系</b>。</p>

<div class="debate">
<div class="debate-title">① 端侧触发器scope（范围）盘点（泽民 02:35 开场）</div>
<p>泽民抛出7点清单：本地定时器 / 简单条件端侧执行 / 白名单车控直执 / VLM合规 / 弱网兜底 / 信号标准化 / 上报Context。问"有没有漏掉的"。</p>
<p><b>结论：</b>框架不错（加分项，说明有准备），但后续讨论把其中"简单条件端侧执行"和"白名单车控直执"划走了（归CDC底软）。</p>
</div>

<div class="debate">
<div class="debate-title">② 视觉强相关事件放云端还是端侧？（益红 03:29）</div>
<p>益红问：主驾睡着、副驾睡着、即将驶出地库——这些视觉类事件你打算放端侧还是云端？</p>
<p><b>泽民回答（04:14）：</b>放云端。上午跟仁杰聊过，faceid识别这些都放云端。</p>
<p><b>关系：</b>确认了视觉类事件的cloud-first（云端优先）原则——端侧做观测（VLM输出状态），云端做判断（穿越检测+事件组装）。</p>
</div>

<div class="debate">
<div class="debate-title">③ 触发器在主链路里什么位置？（丁彬 04:54）</div>
<p>丁彬问全局架构位置。泽民投屏讲了两条路：① 用户query → Director → 动态Advisor → 订阅触发器 → 端侧执行。② 白名单 → 拿信号 → 端侧自闭环。</p>
</div>

<div class="debate">
<div class="debate-title">④ 触发器和信号库是不是一回事？（丁彬 06:43 澄清）</div>
<p>丁彬没分清。泽民澄清：<b>信号库=只做存储（storage）</b>（现在端上有哪些信号、什么状态），<b>触发器=另一层逻辑（logic layer）</b>（当什么情况就做什么事）。触发器订阅（subscribe）信号库的变化。</p>
<p><b>类比：</b>信号库是温度计（只测温度、显示温度），触发器是空调控制器（温度超过30度就开空调）。两个不同的东西。</p>
</div>

<div class="debate">
<div class="debate-title">⑤ 充电大师冲突（丁彬 08:07）</div>
<p>丁彬举例：赛力斯的"充电大师"在P档时拍照看有没有充电桩、有就开充电盖。这跟触发器要干的几乎一样——都是"满足条件→自动执行"。泽民承认"这块会冲突"。→ 后续⑥⑨解决了：固定场景归CDC。</p>
</div>

<div class="debate">
<div class="debate-title">⑥ 统一收口原则（西钺 09:24 + 佳杰 10:29）</div>
<p><b>西钺：</b>"别开发两个触发器。不管从开发工作量还是部署资源上都浪费。要么泽民做、要么给赛力斯做。"</p>
<p><b>佳杰补充边界：</b>无感迎宾（人靠近车自动开灯）、无感开车门是<b>系统feature（系统功能）</b>——出现在设置菜单里有专门button开关 → 让赛力斯CDC做。场景引擎类的（"靠近公司推荐咖啡"这种有生命周期lifecycle的）→ 触发器做。</p>
</div>

<div class="debate">
<div class="debate-title">⑦ 丁彬的分工原则（11:02）</div>
<p>重车感知（heavy vehicle sensing，依赖CAN总线和底层硬件的）/车状态的 → 赛力斯做。语音query/复杂意图/非规则性逻辑 → 触发器做。</p>
</div>

<div class="debate">
<div class="debate-title">⑧ 系统feature vs 场景引擎区分（佳杰 11:32 + 丁彬 12:16）</div>
<p>佳杰："迎宾是系统功能——出现在设置列表里有专门的button，用户enable/disable。场景引擎的东西（靠近公司推荐咖啡）有生命周期（lifecycle，创建→运行→结束）。"丁彬确认："它会出现在设置列表里有专门的button对吧？所以是feature。"</p>
</div>

<div class="debate">
<div class="debate-title">⑨ 仁杰的判定标准——6.22最核心的scope结论（仁杰 13:14）</div>
<p><b>"三问判定法"：</b></p>
<ul>
<li><b>"这东西会不会变？"</b>——常驻/固定/标配/整车生命周期不变 → CDC底软做</li>
<li><b>"用户需不需要创建它？"</b>——不需要 → CDC。需要用户主动设 → 触发器</li>
<li><b>"需不需要AI（大模型推理）？"</b>——不需要 → CDC。需要 → 触发器</li>
</ul>
<p>全是"不" → CDC。任一个"是" → 触发器。</p>
<p><b>仁杰原话：</b>"像起步安全，你放在触发器里面专门去配一下，反而还要让触发器去接底层信号、考虑稳定性和延时，不如直接底软做掉。"</p>
</div>

<div class="debate">
<div class="debate-title">⑩ 丁彬确认"看毯子"为什么需要触发器（14:07）</div>
<p>"看毯子"是开放式规则（open-ended rule，没法提前写死）、条件是用户临时说的 → 需要触发器。和"起步安全"（固定规则、写死了、永远不变）本质不同。</p>
</div>

<div class="debate">
<div class="debate-title">⑪ 端侧scope收窄拍板（丁彬 16:17 + 西钺 16:26 + 仁杰 16:39）</div>
<p><b>丁彬：</b>"原来端侧闭环的还继续闭环，几乎不需要触发器做什么。"<br/><b>西钺：</b>"所有固定闭环放赛力斯，我们只用云端的，跟赛力斯做仲裁（arbitration），很干净。"<br/><b>仁杰：</b>确认。不然绕触发器链路反而变长——本来CDC直接做200ms搞定的事，绕一圈变成2秒。</p>
</div>

<div class="debate">
<div class="debate-title">⑫ 赛力斯内部仲裁问题（西钺 17:14）</div>
<p>西钺发现赛力斯内部多个业务（迎宾/充电/安全提醒）同一时机同时提醒、没有仲裁。面客体验差——上车同时听到3条提醒。需要推赛力斯梳理自己的业务仲裁。</p>
</div>

<div class="debate">
<div class="debate-title">⑬ 端云触发器原始设计意图（仁杰 18:02）</div>
<p>需要在线推理的→云端。不需要推理的→端侧（提高稳定性+降低延时latency）。比如"下雨关窗"——端侧检测到下雨直接关，不需要绕云端推理。大概20%的场景走端侧。</p>
</div>

<div class="debate">
<div class="debate-title">⑭ VLM信号源和触发源的"鸡生蛋"问题（丁彬 19:19）</div>
<p>丁彬的深层问题：如果"看到下雨"要VLM持续观测，那"开始观测下雨"这个<b>任务本身</b>谁来触发？触发器调VLM看→VLM的结果给触发器→<b>谁先谁后？</b>答：AlwaysOn一直在看基础内容（天气/路况/人），不需要触发器启动。触发器只管"额外的/动态的"观测任务。</p>
</div>

<div class="debate">
<div class="debate-title">⑮ 动态注意力SP下发机制（仁杰 19:58）</div>
<p>仁杰解释：动态Advisor创建监控任务时，需要生成对应的注意力SP，然后经⑮→L14→㉔→㉕把SP给到端模。上汽没端模所以没做；赛力斯有端模，这条链路要建出来。</p>
</div>

<div class="debate">
<div class="debate-title">⑯ 视觉类事件清单梳理（佳杰 21:06 + 22:29）</div>
<p>佳杰说这是"视觉类的状态变更事件（state change events）"——不是静态的，要检测<b>变化过程</b>（驶出地库=从"地库内"变"地库外"、打手机→结束打手机、物品出现/被移除）。佳杰和益红正在整理，周内给泽民。</p>
</div>

<div class="debate">
<div class="debate-title">⑰ AlwaysOn已有采集和动态任务的关系（西钺 22:32 + 23:00）</div>
<p>西钺问：AlwaysOn已经输出舱外路况/天气/舱内动作，<b>这些排除后（subtract what AlwaysOn already covers）</b>，才是需要动态预置的任务，对吧？佳杰确认：我对接触发器，要的是event（事件）。触发器怎么和AlwaysOn对接是另一层。</p>
</div>

<div class="debate">
<div class="debate-title">⑱ 链路图位置争论（佳杰 24:05）</div>
<p>丁彬让泽民放大架构图看。佳杰指出：视觉信号不管是AlwaysOn还是额外链路产出的，进入信号库后，触发器会监听信号变化→做阈值穿越/首次出现检测→组装成event给上游业务消费。</p>
</div>

<div class="debate">
<div class="debate-title">⑲ 佳杰当场纠错：Advisor不轮询Context（25:48——<span class="pm">6.22最关键时刻</span>）</div>
<p>佳杰指出泽民图里的路径错了。泽民画的是Context→Advisor（Advisor轮询Context消费event）。<b>佳杰纠正：应该是⑮触发器→Advisor（触发器push event给Advisor）。</b></p>
<div class="quote"><b>佳杰原话：</b>"我不会去轮询你的Context。我注册了这个事件之后，就等待你的云端触发器给我发这个事件。"</div>
</div>

<div class="debate">
<div class="debate-title">⑳ 事件放触发器而非Advisor的原因（西钺 28:44 + 佳杰 29:03）</div>
<p>西钺问：为什么事件处理放触发器不放Advisor？佳杰："event（事件）比state（状态）更有价值。如果触发器直接给的是状态，我就直接接信号库了。"西钺理解："从推理ROI（Return on Investment，投入产出比）角度，没必要24小时轮询不相关的事。"</p>
</div>

<div class="debate">
<div class="debate-title">㉑ 佳杰的经典三段拆解（29:34）</div>
<p>"用户倒完车之后，如果车里有小朋友，就把儿童锁打开"拆成：倒完车=event（触发器push）、有小朋友=condition（存Context）、开儿童锁=Advisor出advice给Director执行。见§4详解。</p>
</div>

<div class="debate">
<div class="debate-title">㉒ 规则驱动还是模型驱动？（丁彬 30:39 + 泽民 30:50）</div>
<p>丁彬问触发器是model-driven（模型驱动）还是rule-driven（规则驱动）。泽民答"目前规则驱动的，后边会加模型推理"——表述模糊。<b>准确答法：一期规则驱动为主（if/else+阈值穿越），二期加模型推理判断复杂语义（如判断"这算不算在睡觉"）。</b></p>
</div>

<div class="debate">
<div class="debate-title">㉓ 丁彬的"毯子"完整推演 + 致命漏洞（31:12）</div>
<p>丁彬推演全链路，最后点出：如果Director给的规则跟触发器<b>匹配不上（rule mismatch）</b>——比如Director说"看毯子掉没掉"，但触发器的事件模板库里没有"覆盖物位置变化"这个事件类型——这事就<b>漏掉了（silently dropped）</b>。→ 解决方案：通用视觉事件通道（见§10）做兜底。</p>
</div>

<div class="debate">
<div class="debate-title">㉔ 仁杰纠正：Director不直连触发器（33:53）</div>
<p>正确链路：Director → 先建⑰动态Advisor → Advisor拆解query改写注意力SP → 调⑮注册。<b>不是Director直连触发器。</b>原因：Director只做一次性决策（"要不要做"），Advisor做持续监控（"一直盯着直到完成"）。</p>
</div>

<div class="debate">
<div class="debate-title">㉕ SP下发路径的三选一（仁杰 34:59）</div>
<p>三个方案：① 经触发器⑮→㉔→㉕下发 ② 先注册触发器场景再给端模 ③ 动态版本直接给端模（跳过触发器）。各有利弊。<b>没定，7月中必须定。</b></p>
</div>

<div class="debate">
<div class="debate-title">㉖ 端模能不能做条件式判断（仁杰 35:38 + 丁彬 36:04）</div>
<p>仁杰问：端模本身能不能判断"毯子掉了就往上发"？如果能→链路大大简化（端模自己判→直接上报→不需要⑮做穿越检测）。如果不能→只能固定周期输出（every 3 seconds），需要云端模块从每次输出里比对判断。</p>
</div>

<div class="debate">
<div class="debate-title">㉗ 佳杰主张标准化链路（36:54 + 39:07）</div>
<p>佳杰主张："所有信号类的东西并到一个模块（signal module）提供，事件类的也并到一个模块（event module）。"避免各自重复造轮子和资源竞争。原因：上汽VQA在端侧取图时会和AlwaysOn抢占摄像头→乱套。</p>
</div>

<div class="debate">
<div class="debate-title">㉘ 仁杰追问端模条件式注意力（40:38）</div>
<p>"不需要端模做后半步（判断+上报），但<b>只有看到毯子才上报（conditional reporting）</b>，这件事端模能做吗？"——区分"一直看+一直报"和"一直看+只在看到目标时报"。</p>
</div>

<div class="debate">
<div class="debate-title">㉙ 丁彬三层评估端模能力（40:58）</div>
<p>① 有限集内（只看毯子/毛巾几样）通过prompt限制应该行；② 发散了（手绢/小方巾等VLM不一定认识的）效果是问号，<b>没试过</b>；③ 前提：prompt有渠道改到端上就OK。</p>
</div>

<div class="debate">
<div class="debate-title">㉚ 仁杰提出完整可行链路（42:01）</div>
<p>如果端模能做条件式注意力：动态Advisor → 触发器注册 → 触发器把动态SP给端模 → 端模判断特定目标 → 看到目标时返回 → 触发器组装event给Advisor。<b>7月中要定——取决于丁彬的验证结果（㉙）。</b></p>
</div>

<div class="debate">
<div class="debate-title">㉛ 西钺的两个担心（43:43）</div>
<p>① 复杂输出问题：如果要"某物出现在什么位置"，端模输出会比简单的"有/没有"复杂得多，不一定搞得定。② 端模没有push能力：端模不能主动推给某个Advisor，只能输出到信号库→还是依赖⑮的订阅+push逻辑。</p>
</div>

<div class="debate">
<div class="debate-title">㉜ 全量上报+事件模块过滤（丁彬 44:39）</div>
<p>丁彬提出：端侧还是全量上报（report everything），由事件模块（触发器⑮）做过滤和触发。不限制端模的输出范围——端模尽管输出所有它看到的，过滤的活交给⑮。</p>
</div>

<div class="debate">
<div class="debate-title">㉝ 端状态新增动态观测字段（西钺 46:41）</div>
<p>西钺提出：端侧信号库④要新增一个字段（field），专门作为动态观测内容的输出上报用，和AlwaysOn的固定输出分开存。因为动态观测内容是临时的（temporary），AlwaysOn的输出是固定的（permanent）。</p>
</div>

<div class="debate">
<div class="debate-title">㉞ 约VLM工程细节专题会（佳杰 47:57）</div>
<p>佳杰主动提：视觉感知类事件需要单独约会——把工程链路放大、和AIBOX怎么打通、数据格式怎么定、怎么拿数据，细聊。泽民同意。→ <b>PM-1行动项</b>。</p>
</div>

<div class="debate">
<div class="debate-title">㉟ SOP排期风险（丁彬 49:32）</div>
<p>丁彬提醒：方舟SOP（Standard Operating Procedure，标准运营流程——这里指排期计划）基本满了。佳杰梳理的信号如果和现有无重叠，可能<b>排不进去、当前提供不了</b>。<b>风险：你需要的信号可能没人排期做。</b></p>
</div>

<div class="debate">
<div class="debate-title">㊱ 任务中心端云打通需求（佳杰 50:53-55:08）</div>
<p>佳杰讲了完整需求：端侧要建任务同步持久化模块 / 区分任务级vs感知级 / enable-disable反向写入接口。详见§11。泽民说"我有这个模块"但又问"怎么判定哪些是任务哪些是感知"——<b>说明没设计透（design not fully thought through）</b>。</p>
</div>

<div class="debate">
<div class="debate-title">㊲ AlwaysOn调度归属（西钺 57:25 + 佳杰 57:47）</div>
<p>确认：AIBOX任务调度模块管AlwaysOn/VLM任务的优先级排序。不在触发器。详见§12。</p>
</div>

<div class="debate">
<div class="debate-title">㊳ 动态任务下发到AlwaysOn的路径（西钺 58:54 - 01:02:35）</div>
<p>西钺问：动态视觉观测任务怎么传给端侧AlwaysOn？走触发器的云到端链路（⑮ → ㉔ → ㉕），还是走佳杰的任务调度？讨论来回后确认：<b>走触发器链路下发</b>。</p>
</div>

<div class="debate">
<div class="debate-title">㊴ 通用视觉事件通道（佳杰 01:01:02 + 西钺 01:01:29 + 01:02:35）</div>
<p>佳杰提出：需要一个通用的视觉事件变量（generic visual event variable），不是预设的几类。西钺补充：预训练好的做主力（high confidence），通用做兜底长尾（long-tail fallback），两条路都要保留。佳杰建议再约会细聊。详见§10。</p>
</div>

<div class="debate">
<div class="debate-title">㊵ prompt下发的三类划分（丁彬 01:03:12 + 西钺 01:03:45）</div>
<p>丁彬强调：prompt必须增量追加（incremental append）不覆盖（not overwrite）。还要区分增到哪个slot（槽位）：舱内静态/舱内动态/舱外。西钺补充：还需要带摄像头视角标识（camera_view tag），否则VLM不知道用哪个摄像头的画面来执行这个prompt。</p>
</div>

<div class="debate">
<div class="debate-title">㊶ 信号库归属（丁彬 01:05:54 + 泽民 01:06:05）</div>
<p>丁彬问"端侧信号库④谁负责"。泽民答"可能是仁杰那边去搞的"——<b>对自己scope（范围）边界不确定</b>。实际上④端侧信号库∈触发器scope（617会议已确认），信号采集/上报由端模和各业务做，<b>泽民定schema（数据格式定义）</b>。</p>
</div>

<div class="debate">
<div class="debate-title">㊷ 赛力斯可穷举清单（泽民 54:56）</div>
<p>泽民上周让赛力斯梳理走触发器的场景清单，赛力斯拉了十几个人进群但还没给结果。任务中心的"任务vs场景"标注（labeling）依赖这个清单。→ 需要催。</p>
</div>


<!-- ============ §14 ============ -->
<h2 id="s14">十四、行动项（Action Items，要做的事）+ 待定项（Open Items，还没定的事）</h2>

<h3>产品（我）做的事</h3>
<table>
<tr><th>#</th><th>事项</th><th>来源（会议时间点）</th><th>时间</th></tr>
<tr><td>PM-1</td><td>约佳杰+益红+丁彬的VLM工程细节会：SP怎么下发、数据格式怎么定、和AIBox怎么打通</td><td>佳杰 47:57</td><td>本周</td></tr>
<tr><td>PM-2</td><td>等佳杰和益红的动态视觉感知event清单（有限集事件列表）</td><td>佳杰 49:25</td><td>周内等输入</td></tr>
<tr><td>PM-3</td><td>PRD更新：删"端侧简单条件判定"和"白名单车控直执"（已归CDC），scope按§5收窄</td><td>仁杰 13:14 + 丁彬 16:17</td><td>本周</td></tr>
<tr><td>PM-4</td><td>PRD补充：任务中心对接——端云同步机制/持久化数据库/enable-disable接口/任务级vs感知级字段</td><td>佳杰 51:07-55:08</td><td>本周</td></tr>
<tr><td>PM-5</td><td>拿到赛力斯场景清单后，给每条标"任务级/感知级"(display_level)，交佳杰过一轮</td><td>佳杰 53:54</td><td>等赛力斯输入</td></tr>
<tr><td>PM-6</td><td>跟赛力斯沟通各业务场景仲裁机制（多个提醒同时触发的优先级问题）</td><td>西钺 17:14</td><td>低优</td></tr>
<tr><td>PM-7</td><td>设计触发器给AIBOX任务中心的接口（read/enable/disable/delete/restore 5个API）</td><td>佳杰 55:08</td><td>本周</td></tr>
<tr><td>PM-8</td><td>改链路图：Context→Advisor改成⑮云端触发器→Advisor（event push推送模式）</td><td>佳杰 25:48</td><td>本周</td></tr>
<tr><td>PM-9</td><td>认领④信号库归属，明确"信号库schema归触发器PM定，采集/上报由端模+业务做"</td><td>丁彬 01:05:54</td><td>本周</td></tr>
</table>

<h3>开发做的事</h3>
<table>
<tr><th>#</th><th>事项</th><th>涉及模块</th><th>来源</th></tr>
<tr><td>DEV-1</td><td>⑮新增15-10/15-11：event组装+push能力——监听⑫变化→穿越检测→组装event→push给⑯⑰</td><td>⑮云端触发器</td><td>佳杰 24:05-27:02</td></tr>
<tr><td>DEV-2</td><td>⑮新增"视觉类event"大类（除了车控类、时间类之外的第三大类）</td><td>⑮云端触发器</td><td>佳杰 28:07</td></tr>
<tr><td>DEV-3</td><td>㉔支持接收+转发动态VLM SP（从⑮接收7字段运行指令→转发给㉕）</td><td>㉔端侧触发器</td><td>仁杰 42:01</td></tr>
<tr><td>DEV-4</td><td>㉕支持动态SP增量追加——slot机制（cabin_static/cabin_dynamic/exterior），不覆盖AlwaysOn的SP</td><td>㉕感知器</td><td>丁彬 01:03:12</td></tr>
<tr><td>DEV-5</td><td>端侧触发器scope收窄：不接CDC闭环场景信号（起步安全/迎宾/哨兵等6个场景全删）</td><td>㉔端侧触发器</td><td>仁杰 13:14</td></tr>
<tr><td>DEV-6</td><td>端侧持久化数据库+任务中心接口（read/enable/disable/delete/restore 5个API）</td><td>㉔端侧触发器</td><td>佳杰 51:59-56:05</td></tr>
<tr><td>DEV-7</td><td>端云任务同步机制建设（全量+增量同步，冲突以云端为准）</td><td>㉔↔㉓</td><td>佳杰 51:59</td></tr>
<tr><td>DEV-8</td><td>丁彬验证端模"条件式注意力"能力——有限集能做吗？发散了能做吗？</td><td>㉕感知器/VLM</td><td>仁杰 40:38 + 丁彬 40:58</td></tr>
</table>

<h3>待定项（Open Items，还没定需要跟进的）</h3>
<table>
<tr><th>#</th><th>待定项</th><th>为什么还没定</th><th>谁推</th><th>截止</th></tr>
<tr><td>1</td><td>动态视觉感知event清单（有限集事件列表——哪些事件需要预训练支持）</td><td>佳杰+益红在梳理（22:29, 49:25提到）</td><td>佳杰+益红→给泽民</td><td>本周内</td></tr>
<tr><td>2</td><td>VLM链路工程细节（SP下发走哪条路/AIBox接口怎么打通/数据格式）</td><td>需约专题会（佳杰 47:57提出）</td><td>泽民约会</td><td>7月中前</td></tr>
<tr><td>3</td><td>通用视觉event通道设计（怎么兜底长尾需求、怎么给用户做预期管理）</td><td>佳杰建议再约会（01:04:33）</td><td>佳杰+泽民</td><td>待定</td></tr>
<tr><td>4</td><td>端模条件式注意力验证（丁彬要试一下有限集内能不能做"只在看到目标时上报"）</td><td>丁彬"得试一下"（40:58原话）</td><td>丁彬</td><td>7月中前</td></tr>
<tr><td>5</td><td>赛力斯可穷举场景清单（哪些场景走触发器、哪些走CDC）</td><td>拉了十几人进群但还没给结果（54:56提到）</td><td>赛力斯→泽民催</td><td>待跟进</td></tr>
<tr><td>6</td><td>赛力斯多业务仲裁机制（多个提醒同时触发的优先级怎么定）</td><td>西钺发现的问题（17:14），赛力斯内部还没仲裁</td><td>推赛力斯做</td><td>低优</td></tr>
<tr><td>7</td><td>SOP排期风险——需要的新信号可能排不进方舟的开发计划</td><td>丁彬提醒方舟SOP已满（49:32）</td><td>丁彬+方舟评估</td><td>待评估</td></tr>
</table>

<hr style="margin-top:40px;border:none;border-top:2px dashed #cbd5e1"/>
<p class="en">v4 · 2026-06-23 · 所有英文带中文解释 · 每个知识点带上游+下游+实际case · 全部37个会议讨论点还原 · 基于6.22会议逐字稿（1h6m66分钟）</p>

</body>
</html>
