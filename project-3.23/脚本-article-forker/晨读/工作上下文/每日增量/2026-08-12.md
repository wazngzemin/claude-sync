# 2026-08-12 工作上下文增量

## 今日读取覆盖

- 飞书能力检查成功。当前只能读取 OAuth 用户本来有权访问的资料；普通私聊不能保证被完整枚举，机器人不在原会话时，图片和视频附件可能只看到名称，无法下载正文。
- 可枚举群聊共 214 个。本次围绕 Claude Tag 文章，重点读取了座舱产品、Planner、Task/Trigger、动态 Advisor、动态示例等 8 个相关会话的近 30 天消息。
- “座舱大模型助手产品群”读取为 partial 且结果截断；其中多处图片、视频和 PDF 返回 `234040`，不能据此声称完整读取。
- “场景智能产品共建”读取失败，错误为 `230001 invalid container_id`。这代表读取失败，不能解释为该群没有消息。
- 8 月 13 日日历读取成功，共 2 个安排：上汽&字节 Bug 日清会、生态伙伴 AI 三方工具 GTM 培训。当前没有本人参会证据，也没有会议纪要或附件正文。
- 关键词搜索中，“动态示例 Context 记忆 评测 版本”成功返回 0；长时任务、主动服务相关搜索读到大型文档，但返回 partial 且正文截断。

## 今天新增或再次确认的事实

1. 产品群正在处理 AI push 卡片展示时长、多个 push 是否并行、与其他卡片互斥等问题。这直接说明主动服务的核心约束已经从“能不能产出 advice”进入“何时出现、出现几个、与谁冲突”。
2. 产品群要求继续标记 0813 必解、0830 和长期 Bug，并讨论 AI Navi 拉高劝退率、媒体卡片无缘无故出现。这些信息说明当前仍处于量产体验和责任归因阶段。
3. 当前长时任务仍有“丢三落四、下电后忘记、触发时机错误”的反馈。飞书原始消息提到更稳定的定时和条件任务框架可能到 8 月底，但这是当时计划，不等于已经上线。
4. 动态 Advisor 资料明确把预设事件、动态事件、Context、记忆、子目标 review、交互策略和用户反馈串成主动服务链路。部分环节仍标为 2 期或 TBD，不能描述为当前已全部实现。
5. 实车和竞品走查继续暴露工具 Context 不互通：POI 可能进入美团、系统地图、联网搜索、AI Navi，多种能力之间上下文割裂；复杂音乐搜索也存在工具反复调用。
6. 近期 Planner 场景讨论出现“Planner + 分子级调用”与“Planner + 原子级调用”的边界变化，同时要求开始和结束触发信号。这说明共享 Context 之外，能力边界和可观察状态仍是前提。

## Claude Tag 文章的工作映射

- Tag 的频道常驻，对应座舱里的持续 Context 和事件时间线。两者都在解决“任务开始前，AI 是否知道前因后果”。
- Tag 的 Self-schedule，对应 Task 生命周期、Trigger 唤醒、等待条件、执行回调和结束清理。你当前的长时任务问题正好说明：记住一句目标不等于能跨时间可靠执行。
- Tag 的 EQ，对应 Advisor 的介入时机、AI push 互斥、主动服务开关、频控、用户反馈和打扰边界。
- Tag 的 Workspace/Channel/Thread memory，对应你的公共 Context、任务级 Context、人物记忆和车企/车型/版本隔离。座舱还多了驾驶安全、多人身份和端云状态可信度约束。
- Tag 的共享大脑不宜直接搬到车内。车内需要的是有边界的共享 Context：按人物、座位、车辆、任务、车型版本、隐私开关和安全等级隔离。

## 今日阅读的最小产物

- `座舱常驻 AI 适配判断器`：输入一个候选场景，按协作密度、Context 密度、及时性、脏活程度、长时性、权限风险、安全风险和可验收性评分，输出更适合单轮 Planner、动态 Advisor、Task+Trigger，还是暂缓。

## 证据边界

- Claude Tag 的产品数据、模型名称、代码占比、成本和市场预测来自用户提供文章，本次未独立联网复核。
- 飞书资料存在 partial、truncated、附件不可读和一个群读取失败。本文只能引用成功读到的正文，不能用缺口推断“没有讨论”。
