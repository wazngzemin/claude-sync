# -*- coding: utf-8 -*-
# 把纠正后的12段讲稿正文写回 HTML,保留每段 role/mini-flow/流程图/原子表
import re
F='触发器流程图-评审讲解稿.html'
c=open(F,encoding='utf-8').read()

P={}
P['一']=[
 '我先用一分钟讲这张图的目的。我们今天评审的，不是一个普通的条件判断，也不是单独做个 VLM 任务。我们要做的，是把“豆包上车以后怎么主动服务用户”这件事，做成一套能量产的端云协同机制。',
 '以前短期方案能跑，是因为很多逻辑是写死的：某个信号来了，就直接触发某个动作；某个 VLM 场景要跑，就临时拉一条链路。但这个方式一到量产就有问题：信号源散、规则不可配置、弱网不稳定、VLM 结果不能复用、Advisor 和 Director 拿不到统一事件。',
 '所以这张图要解决的是：<b>让系统从“用户问一句我答一句”，升级成“车辆能感知状态、能等待条件、能主动提醒、能弱网履约、能把结果回到上下文”。</b>',
 '今天我们重点看三个问题：第一，入口和流程怎么走；第二，触发器模块到底做什么；第三——这条最关键，也是我们这次反复核对的——<b>Advisor、Director、Context 三者到底谁先谁后、谁给谁，端侧的结果到底怎么回到大脑。</b>',
]
P['二']=[
 '这张图先分成几块看。<b>最左边是入口和信号源</b>：用户语音①②、车身/蓝牙/导航信号源③、端侧 VLM 感知器㉕。<b>左中是信号库</b>：端侧信号库④、云端信号库⑫——这一块解决的是“系统拿到了什么事实”。',
 '<b>中间是触发器</b>：规则配置平台⑬、云端触发器⑮、端侧触发器㉔——这是今天的主角，解决“什么条件下该触发”。<b>中间偏右有一个枢纽，就是㉑Context</b>——这个位置很重要，等会儿第十一段我要专门讲，它是端侧所有结果的唯一上报口，也是 Advisor 和 Director 统一的消费入口。',
 '<b>再往右是决策</b>：静态 Advisor⑯、动态 Advisor⑰、Goal List⑱、Director 汇入⑲、Director 推理⑳。<b>最右边是执行</b>：工具㉒、任务中心㉓、TTS㉖、机器人㉗、车控㉘、输出㉙。横着还有一条云/端分界线：线上面是云端，管注册、仲裁、复杂推理；线下面是端侧，管本地信号、定时器、VLM 调度、弱网执行。',
 '所以这张图的读法是：<b>左边拿事实，中间判时机，Context 做中枢，右边 Advisor 出主意、Director 拍板、执行端落地；云端管登记裁决，端侧管本地履约。</b>',
]
P['三']=[
 '入口有两类。第一类是用户开口，也就是 ①到②：用户说一句话，ASR 转成文字，然后送到 ⑨ 做拒识和意图分流。所有语音相关的链路，不管简单车控、复杂对话、还是条件任务，第一步都要先过这个分流。',
 '第二类是车辆自己产生信号，也就是 ③到④，再同步到⑫：车门状态、座椅占用、温度、档位、导航状态、POI 接近、还有 VLM 识别结果，这些都不是用户开口产生的，但它们是主动服务的基础。',
 '这里有一条铁律要先讲清楚：<b>触发器不直接监听原始信号源，只读信号库。</b>为什么？因为车型会变、信号源会变、CDC 接口会变，如果触发器直接监听原始信号，以后每接一个车型都要改触发器。现在我们把信号先标准化写到信号库，触发器只读标准信号，规则才能跨车型复用。',
 '<b>这次核对我专门纠正了一处：原来图上有一条 ③端信号源直接连到㉔端侧触发器的线，这是错的</b>——端侧触发器也得读信号库④，不能读裸信号源③。所以入口这段的结论是：用户开口走语音入口，车辆状态走信号入口，两类入口最后都要进标准化信号库，后面触发器才能稳定判断。',
]
P['四']=[
 '⑨ 是整张图的第一个关键分流口。ASR 转成文字以后，系统不能马上执行，必须先判断这句话是什么类型，至少分四类：简单车控、复杂对话、条件/定时任务、拒识。',
 '如果是“打开车窗”这种简单车控，就走端侧旁路，尽量端侧快速闭环，<b>绕过大脑</b>。如果是“我好热”“附近有招行吗”这种复杂需求，就走 Director 大脑。如果是“十分钟后关座椅加热”“温度超过 25 度开空调”，就走触发器注册。',
 '如果系统没听懂或能力不支持，就走拒识——<b>这里要补一个细节，这次从 Planner 讲义里核出来的：拒识不是简单丢掉，拒识成功的 query 会写进㉑Context 的事件日志，被 Advisor 感知，有可能转成主动服务。</b>',
 '这里评审时要强调：<b>⑨ 判错，后面全错。</b>简单车控如果误进大脑，会慢；条件任务如果误当即时车控，会直接错执行；复杂需求如果误当触发器规则，会丢上下文。所以 ⑨ 是前置分流，不是普通节点。',
]
P['五']=[
 '第一条蓝线是简单车控即时链路，用“打开车窗”举例。用户开口以后，ASR 转文字，到 ⑨ 做意图分流。⑨ 判断它是简单明确车控以后，不进 Director，也不进触发器注册，而是走端侧离线 fc+白名单。这里的目标是快速完成函数选择和槽位提取，例如打开车窗、关闭空调、打开远光灯，命中白名单就端侧原子执行，毫秒级，不进大脑。',
 '<b>这次核对要纠正一处：原来图上“白名单未命中”那条线指向了㉔端侧触发器，标签却写“回云处理”——指向和文字对不上。未命中的正确去向是回 ⑨ 做复杂分流，不是甩给端侧触发器引擎。</b>',
 '复杂车控，例如“我有点冷，把车里弄舒服一点”，不在蓝线里闭环，要转到绿线进 Director，由 Director 结合端状态、Context、偏好和工具能力做决策。这段的评审结论是：蓝线是简单即时车控链路，不是触发器主线，也不是复杂车控链路。',
]
P['六']=[
 '第二条绿线是复杂对话，用“我好热”举例。用户不是直接说“打开空调”，而是表达一个感受。系统要判断车内现在多少度、用户平时喜欢多少度、是否有人在副驾、当前是否适合直接调空调，甚至要决定要不要先问一句。所以 ⑨ 判断成复杂需求以后，会进 ⑲ Director 汇入。',
 '<b>这里我要把 Director 是什么先说死，因为后面第十一段全靠它：Director，就是 Planner，就是对话推理引擎——三个名字，一个东西。它是这套系统里唯一拍板的、唯一对外说话和执行的大脑，角色是 CEO，负责快决策。</b>',
 '⑲ 汇入会汇聚多路信息：用户 query、Advisor 建议、工具反馈、触发通知、Goal List；⑳ 推理再结合㉑Context 注入的偏好、历史、端状态、记忆，输出三个字段：talk_or_not（说不说）、talk_content（说什么，带情绪和 emoji）、action_list（做哪些动作）。',
 '这里要告诉研发：<b>Director 是复杂决策的唯一收口，触发器不能替代 Director。</b>触发器可以告诉系统“某个时机到了”，但不能决定“这个时候要不要打扰用户、说什么话、做哪些动作”，这些必须由 Director 结合上下文统一决策。绿线的评审结论是：复杂对话慢一点，但必须聪明和一致。说和做都从 Director 出来，工具失败再通过反馈回流，让 Director 二次推理。',
]
P['七']=[
 '第三条橙线才是触发器最核心的业务主线。例子是“车内温度超过 25 度就开空调”或者“十分钟后关座椅加热”。这类需求的本质不是现在执行，而是注册一个未来会被触发的任务。',
 '流程是：⑨ 识别这是条件话，进 ⑩ 意图模型，把自然语言拆成条件和动作。比如条件是 cabin_temperature &gt; 25，动作是打开空调；或者条件是 10 分钟后，动作是关闭座椅加热。然后进 ⑬ 规则配置平台，也就是火山后台“新建场景”那一页。⑬ 要做的事：查信号是否存在、查动作是否允许、把任务注册成标准 scene，生成 DSL——包括 conditions、dynamic_conditions、actions、priority、execute_policy、有效期、灰度范围。',
 '注册完成后进 ⑮ 云端触发器。⑮ 会判断这个任务应该在哪里跑：复杂语义留云端，弱网定时下发端侧，VLM 相关转成视觉订阅，安全不确定的回 Director。这里就是 15-9 端云分类。',
 '<b>这次核对补一条很关键的回环：Director 在对话里产生的条件任务，比如用户说“车里太热”它顺手设了个“两三分钟后关窗”，这个注册不是凭空消失，而是 ⑳ Director → ⑮ 云端触发器（这条叫 L33），经云端触发器中转再下发端侧。原来图上这条线终点是悬空的，我把它接到了⑮。</b>',
 '所以橙线的结论是：触发器不是直接执行动作，而是把未来时机变成标准事件。注册在云，判断按任务类型分云端或端侧，命中以后复杂动作回 Director，简单安全动作端侧闭环。',
]
P['八']=[
 '⑮ 是云端触发器，它不是一个简单规则表，而是云端 CTA 引擎，CTA 就是 Condition、Trigger、Action。它负责的是登记、判定、仲裁、分类、路由和下发。',
 '这里先回答一个研发和老板都会问的问题：⑮ 是不是都要研发写代码？答案是，是的，⑮ 最终一定是研发实现成云端服务，不是产品在后台手填几个条件就完事。但产品不是没事做。产品要定义这套引擎怎么工作：哪些场景能注册，哪些不能注册；条件 DSL 支持哪些运算符；优先级和频控怎么定；什么情况回 Director，什么情况能下发端侧；失败时用户看到什么、日志里记录什么。研发要做的是把这些产品规则工程化，做成接口、规则库、条件引擎、仲裁器、事件路由、下发服务、日志和监控。所以讲 ⑮ 的时候，不要讲成“研发写一个触发器”，要讲成“产品定义运行制度，研发实现运行底座”。',
 '它的原子能力分四段：<b>注册登记</b>（注册校验·iCanDo、场景库、信号扫描）——比如用户说“看到我妈上车提醒我”，如果没有特定人识别信号，就不能创建成功；<b>判定</b>（条件判定·运算符、模型推理·VLM 语义、抖动过滤、冷却频控）——这里产品要拍一期 DSL 到底支持什么：等于、大于、小于、range、持续时长、变更为、group 与或、任一车门、任一座位；<b>仲裁分类</b>（优先级仲裁 P0-P4·互斥、15-9 端云分类）——15-9 是 ⑮ 最关键的决策模块，决定任务留云端、下发端侧、走 VLM、还是只写 Context；<b>输出下发</b>（标准事件·snapshot、Callback 路由、场景下发·全/增/灰）。',
 '这里对产品的要求是：不能只说“要支持触发器”，要给出场景清单、信号清单、动作清单、DSL 能力清单、优先级规则、频控规则、端云分类规则、失败文案和验收用例，否则研发即使把 ⑮ 写出来，也不知道哪些行为算对、哪些算错。',
 '<b>还有一个这次特别要强调的职责：所有动态注册——不管是 Advisor 的视觉订阅，还是 Director 对话产生的条件任务——都必须经 ⑮ 中转，⑮ 是唯一的注册入口，不允许谁直接捅到端侧。</b>',
]
P['九']=[
 '㉔ 是端侧触发器，这里一定要讲重一点，因为如果 ㉔ 只是把信号转发到云端，那它没有存在价值。㉔ 的价值是本地定时、弱网履约、VLM 调度和白名单执行。',
 '这里也要先回答同一个问题：㉔ 是不是研发写代码？答案也是，是。㉔ 必须是车端真实存在的本地引擎，不是产品配置页，也不是云端服务的一个别名。它要在车机侧接收规则、存规则、扫本地信号、跑本地定时器、调度 VLM、执行白名单动作、断网缓存结果、恢复后补传。注意端侧不吃自然语言，也不自己生成规则，只接收 ⑮ 经 15-12 下发的、已校验的规则包。',
 '它的原子能力分五段：<b>接收持久化</b>（规则接收·灰度回滚、本地持久化·重启恢复）；<b>扫描判定</b>（信号扫描·500ms 对齐、两级判定·先判运行阶段再判触发条件）；<b>定时·防抖·仲裁</b>（本地定时器、防抖冷却、优先级·互斥 CABIN/FRONT）——24-5 本地定时器是端侧触发器最核心的量产价值，“十分钟后关座椅加热”不能靠云端倒计时，否则车进地库或隧道就失效；<b>VLM</b>（VLM 调度·7 字段、VLM 五态、阈值穿越 last=F&amp;this=T）；<b>回写执行</b>（结果回写、动作执行·车控/TTS/弱网）。',
 '这里要把 Always-on 单独说清楚，也就是常开感知：有些视觉任务不是问一次就完，而是要让摄像头一直盯着，比如持续盯二排宝宝有没有睡着、持续看副驾状态。它一直在出结果，所以 24-6 必须给它冷却：P0 安全类不冷却，其余 Always-on 控在 3s/5s 出一次，不然每一帧都触发就刷爆了。它又一直占着摄像头，所以 24-7 必须能抢占：舱内摄像头正在跑 Always-on，这时候来了一个高优先级 VQA，就要先抢占、执行完、再恢复 Always-on。',
 '所以产品在 ㉔ 上要交付的不是代码，而是四张清单：第一，哪些规则必须下发端侧；第二，哪些信号端侧本地可用；第三，哪些动作允许本地直执；第四，断网、重启、VLM timeout、执行失败时分别怎么反馈和回写。',
 '<b>这次核对还要补两条端侧的核心出口线：一是 ㉔ 命中后能在弱网下本地执行车控㉘和 TTS㉖，这是端侧触发器存在的全部理由；二是 ㉔ 的结果绝不直推大脑，统一回写信号库④，再走 Context——这条下一段细讲。</b>',
]
P['十']=[
 'VLM 这块要单独讲，因为它不是普通信号，也不是普通工具，它是视觉感知任务，需要摄像头资源、模型推理、任务调度、超时控制、资源互斥和结果状态。<b>而且这次核对，VLM 这条链路是改动最大的一段，我先把一个最容易搞错的事说清楚：动态 Advisor 的“视觉”，和端侧那个 VLM 感知器㉕，根本是两条路。</b>',
 '第一条，<b>动态 Advisor⑰ 自己的视觉，走的是云端 VQA，不接端模。</b>Advisor PRD 原话是“vlm 只接 vqa，不接端模，云对云对接，一个文本/prompt 过去，返回 result 和 description”。意思是：像“帮我盯着副驾有没有睡着”这种，动态 Advisor 自己有个定时器，定时取一张图、请求云端 VQA、VQA 返回 true/false 加描述、再回 Advisor 推理产 advice。这条全程在云端闭环，跟端侧㉕没关系。所以原来图上画的 ⑰ 直接连㉕“下发视觉订阅”、㉕ 又直接回⑰“识别结果回 Advisor”，这两条直连都是错的，删掉。',
 '第二条，<b>端侧 VLM 感知器㉕，是给“经触发器调度、结果走 Context”那条路用的，主要是赛力斯/端云结合形态。</b>它的正确链路是：动态 Advisor⑰ 或舒适 Advisor 把“要看什么”作为一个视觉订阅目标，先注册到 ⑮ 云端触发器（经云端中转，铁律①）；⑮ 的 15-5 把“副驾是否睡着”这种语义目标编译成 VLM task，带 camera_group=CABIN、seat=passenger、query、interval、timeout、priority、mutex_group；15-9 端云分类判断它该下发端侧，15-12 打成端侧规则包下发给 ㉔；㉔ 接收后，24-7 先判摄像头资源有没有被更高优先级占用，24-8 才把它转成运行指令下发给 ㉕。',
 '㉕ 只负责执行视觉任务，拉图、调 VLM/VQA、拿结果，<b>结果先回 ㉔——这里有两条独立回路要分清：一条是 24-9 把结果归一成五态（success/empty/failed/timeout/interrupted），另一条是任务状态（排队中/运行中/超时/被打断）反馈给 ㉔，供它做抢占、超时释放、恢复。</b>这里有条安全红线：timeout 不能写成“无异常”，因为模型没返回不代表外部环境安全。',
 '然后 24-10 做阈值穿越，“宝宝睡着了”不能状态一直 true 就每 5 秒提醒一次，只有 last=false、this=true 那一下才生成触发事件；24-11 把结果写回端侧信号库④，信号库再按策略上报㉑Context。<b>最后这一步是关键：上层的⑯⑰ Advisor、⑲⑳ Director 都是从㉑Context 消费这个事实，而不是端侧直接通知它们。</b>',
 '所以 VLM 触发的正确一句话是：动态 Advisor 的即时视觉走云端 VQA；端侧持续视觉走 ⑰/⑯→⑮ 注册→㉔ 调度→㉕ 执行→㉔ 五态/阈值→④ 信号库→㉑ Context→Advisor/Director 消费。这条链路讲清楚，研发才知道每个模块要做什么，产品也能定义每一步的失败态和验收口径。',
]
P['十一']=[
 '这几个模块最容易被混淆，也是我们这次核对反复抠的地方，我把它彻底讲清楚。',
 '<b>先定性。Director 是 CEO，Advisor 是部门经理。</b>Director（也就是 Planner、对话推理引擎）是唯一拍板的、唯一对外说话和执行的大脑，负责快决策。Advisor 是参谋，有四个静态的——舒适、出行、内容、情感，各管一条线、各有独立的 SP 和推理逻辑——再加一个动态 Advisor 当临时监工。<b>Advisor 只出主意，不执行、不控车、不直接对用户说话。</b>舒适 Advisor 看到车里 38 度，它不会自己去开空调，它只给 Director 输出一句“建议开空调 20 度、座椅通风 3 档”，执行不执行、什么时候执行，是 Director 的事；如果 Director 这会儿正在处理更高优先级的用户 query，这条建议可能被延迟、甚至被丢弃。',
 '<b>再讲顺序，这是上次我讲糊的地方。正确顺序是：㉑Context → ⑯⑰Advisor → ⑲Director 汇入 → ⑳Director 推理。Advisor 是夹在 Context 和 Director 中间的，不是跟 Director 并列、更不是 Director 先于 Advisor。</b>为什么？因为 <b>Advisor 的输入只来自 Context</b>——舒适 Advisor 的 SP 里注入的就是 {{attention}}事件、{{watcher}}VLM 描述、{{car_condition}}端信号、{{memory}}记忆，这些全是 Context 给的；而 <b>Advisor 的输出才是 advice，送给 Director</b>。所以一个完整的主动服务是这么走的：某个事件来了（车身信号、VLM 识别、或者定时器到点）→ 写进信号库 → 上报㉑Context → ⑯/⑰Advisor 读 Context、跑自己的 SP 推理、判断“这个时机合不合适、要不要给建议”→ 如果合适，产出 advice → 进 ⑲Director 汇入（advice 和用户 query、工具反馈是同一级的输入，数组同传）→ ⑳Director 推理 → 拍板说什么、做什么 → 执行端落地。',
 '<b>还有一条反向的线，叫派活闭环，这是 Advisor 和 Director 关系里另一半，之前图里完全没有。</b>Advisor 不只是给 Director 出主意，动态 Advisor 本身还是 Director 创建出来的：遇到“帮我照顾一下二排宝宝”这种临时持续任务，静态 Advisor 管不了，⑳Director 就建一个目标、再建一个动态 Advisor⑰ 当专属监工，Director 做完前两三步就撒手，之后这个动态 Advisor 自己循环盯。这里 ⑱Goal List 是共享看板，规矩是“谁建谁删、谁盯谁改”：<b>目标由 Director 创建和删除，进展由 Advisor 更新和 review，两者不交叉。</b>所以 ⑱ 上有两类线：⑳ 经 goal_list_update 工具新增/删除目标，⑯⑰ 写进展和 review 结果回 ⑱，⑱ 再随当轮输入汇入 ⑲ 影响下一次推理。',
 '<b>最后讲一条这次最该钉死的红线：端侧触发器㉔ 不直接通知 Advisor，也不直接通知 Director。</b>端侧的结果——不管是条件命中，还是 VLM 识别——统一写信号库、上报㉑Context，云端再让 Advisor/Director 从 Context 消费。这条原话是 5/7 西钺评审定的：“触发器到 Adviser 的直接通知链路，不存在；所有端侧结果统一走 Context。”这样做的原因是统一事实源，避免端侧多个模块各自往上推、最后状态不一致。配套还有一条铁律：动态注册必须经 ⑮ 云端触发器中转，Advisor 不能直接把订阅捅到端侧。所以原来图上 ⑮→⑯、⑮→⑰ 那种“触发器直接唤起/通知 Advisor”的画法，是 5/7 之前的旧画法，要改成 Advisor 从㉑Context 消费。',
 '<b>再补一个上汽的现状：上汽 D6X 当期其实没有部署静态 Advisor，触发器命中后那条到静态 Advisor 的线是断的，当前是临时直连 Planner/Director 顶上。所以图里静态 Advisor⑯ 在上汽当期要标虚线，这套完整形态是赛力斯/长期才成立。</b>所以主动服务的正确链路一句话：<b>事件→信号库→㉑Context→⑯⑰Advisor 出主意→⑲⑳Director 拍板→执行；Director 反过来建目标、建监工，Advisor 盯进度回写 Goal List。触发器只管“何时唤醒”，Context 管“统一事实”，Advisor 管“出建议”，Director 管“做决定”，执行端管“落地”——各司其职，谁也别越位。</b>',
]
P['十二']=[
 '最后收口到评审结论。第一，弱网怎么办？已经下发到端侧的定时和白名单任务，㉔ 要能本地履约，直接驱动端侧车控㉘、TTS㉖；没有下发或需要复杂语义的任务，不能假装执行成功。',
 '第二，VLM 异常怎么办？㉕ 执行失败返回 failed，超时返回 timeout；24-9 负责把这些状态归一，不能让业务自己猜；24-10 只有在 success 且阈值穿越时才生成触发事件；24-11 要把 empty、failed、timeout 都写回信号库再到 Context；timeout 不能写无异常，failed 不能触发主动服务，empty 可以作为有效“未发现”结果写回。',
 '第三，冲突怎么办？云端 15-8 做业务优先级和互斥仲裁，比如两个主动服务同时命中、两个动作互相冲突；端侧 24-7 只做本地资源抢占，比如 CABIN 摄像头、FRONT 摄像头、VLM 推理资源，不做复杂语义仲裁——<b>这条也是会上定的：仲裁要结合语义、过模型，所以放云端，端侧没那个脑子。</b>',
 '第四，端侧能执行哪些动作？需要白名单拍板，建议 TTS、卡片、AI Push、低风险车控可以端侧直执；涉及安全、确认、复杂语义的动作必须回 Director。第五，接口和 DSL 要拍板，⑮ 的 range/持续时长/group 与或、VLM task 字段、⑮到㉔ 的规则包字段、㉔到㉕ 的运行指令字段、㉔到 Context 的结果事件字段，都要明确一期支持还是二期支持。',
 '还有两个待拍板的大事：<b>一是端侧触发器㉔ 本身要不要独立存在，6/11 会上没拍死，赵益红已经标了虚线，要靠“离线弱网本地闭环”这个价值来证明；二是上汽当期 vs 赛力斯长期这两套形态，图上要分清，端侧 VLM㉕ 这套在上汽当期是不通的。</b>',
 '最后结论可以这样说：<b>这张图评审通过的关键，不是大家认可“要做触发器”，而是认可端云边界、触发器原子模块、Advisor/Director/Context 的顺序和闭环、异常降级和跨团队接口。</b>否则图画得再完整，也落不到研发排期。',
]

cn=['一','二','三','四','五','六','七','八','九','十','十一','十二']
changed=0
for ch in cn:
    role=f'class="role">第{ch}段'
    i=c.find(role); assert i>0, '缺第'+ch+'段'
    fig=c.find('</figure>', i)
    nextrole=c.find('class="role">第', i+10)
    assert i<fig and (nextrole<0 or fig<nextrole), '第'+ch+'段流程图越界'+str(fig)
    fend=fig+len('</figure>')
    # 该段 talk 块的关闭 </div>:下一个 talk/sect/section 之前最近的 </div>
    nxts=[c.find('<div class="talk"',fend), c.find('<div class="sect',fend), c.find('</section>',fend)]
    nxts=[x for x in nxts if x>0]; nxt=min(nxts)
    close=c.rfind('</div>',fend,nxt)
    assert close>fend
    paras='\n\n'+'\n'.join('        <p>%s</p>'%p for p in P[ch])+'\n      '
    c=c[:fend]+paras+c[close:]
    changed+=1

open(F,'w',encoding='utf-8').write(c)
print('已替换', changed, '段讲稿正文 | 新字节', len(c.encode()))
# 校验:段数/表/流程图都在
print('段:',len(re.findall(r'class="role">第',c)),'| 表:',c.count('<table'),'| 流程图:',c.count('class="segmap"'),'| mini-flow:',c.count('class="mini-flow"'))
print('关键新词在:', all(k in c for k in ['参谋↔','派活闭环','云对云','谁建谁删','参谋','只出主意']))
