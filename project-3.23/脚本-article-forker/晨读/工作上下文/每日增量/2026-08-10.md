# 2026-08-10 工作上下文增量

## 今日读取覆盖

- 主日历读取成功：2 个日程，分别是“触发器依赖端状态信号单位对齐”和“上汽&字节 Bug 日清会”。日历只能证明有安排；本次没有读到本人实际进入视频会议的证据，也没有读到会议纪要或妙记正文。
- “座舱大模型助手产品群”今天可见消息为 0 条。这个结果只代表该群今天窗口，没有覆盖全部群聊和单聊。
- 近 3 天消息检索补到 2 条与当前工作直接相关的原始消息：渠道端状态差异、长时任务条件触发失败。

## 今日新增事实

1. 上汽与赛力斯的端状态已经分开维护。两个渠道的同一信号可能使用不同枚举值，触发器也需要按渠道维护可用信号集。当前有人明确提出整理“赛力斯渠道触发器支持的信号集”，并索要赛力斯端状态定义文档。
2. 长时任务目前刚跑通，但与端状态结合的条件任务表现不稳定：
   - “到了目的地提醒我”被理解为距离目的地 10km 时触发。
   - “这首歌唱完就停止音乐”没有执行。
   - “距离终点 1km 时提醒”没有触发。
3. 当前反馈是：定时触发表现相对可用，依赖导航距离、媒体播放进度等端状态的触发较差。
4. 长时任务需要系统测试，计划在 8 月 14 日或 8 月 17 日向 Alex 展示，具体时间仍以约定结果为准。

## 对原有问题线程的更新

- 触发器的关键问题进一步收敛为“渠道真值治理 + 条件任务可验收性”，不再只是信号单位对齐。
- “统一端状态表”不能直接当作跨车企真值。每条信号至少要带渠道、车型、版本、字段名、单位、枚举、更新时间和 Owner。
- 长时任务需要拆开验收：定时型、导航距离型、媒体状态型、VLM 语义型不能共用一个笼统通过率。
- 这两条新信息正好验证了个人知识系统的核心风险：资料存得再多，只要缺少适用范围、版本和测试结果，检索出来的内容仍可能误导决策。

## 下一步建议产物

### 1. `触发器渠道信号真值表`

最小字段：车企渠道、车型、版本、业务含义、原始字段、单位、枚举值、是否可订阅、触发器映射、来源链接、更新时间、Owner、验证状态。

### 2. `长时任务四类专项评测表`

按定时型、导航距离型、媒体状态型、VLM 语义型分组。每条 case 记录自然语言任务、结构化 TaskSpec、订阅事件、条件表达式、真实上报、触发结果、Planner 后续动作、失败首层、Owner。

## 证据边界

- 上述两条业务事实来自近 3 天飞书原始消息，可信度高。
- 两场今日会议没有可读纪要，会议内是否已经产生新结论仍未确认。
- “8 月 14 日或 8 月 17 日展示”是消息中的计划，不代表已经锁定。
