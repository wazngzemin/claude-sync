# 本轮来源与模块复核

日期：2026-09-07。

交付：`产品/codex/HTML/02-已交付/豆包汽车-全链路来源与模块核对-v01.html`。

这是来源审计和后续重画的内容基线，不是已经发布的新 v13 架构图。没有覆盖原始飞书资料、用户现有画板或 v11/v12 文件。

## 已完成

- 将用户粘连的三个 Wiki 链接拆开，读取正文，并在浏览器中打开原生画板、放大和平移检查模块及连线。
- 长期架构查看两张感知/Planner/任务队列图；主链路查看规划、落地细化、更新架构、双闭环、TOb、车企协作；2.6 目标查看性能图关键分段。没有把概览、未逐条核实的评论、未看全的小字声称全部深读。
- 飞书额外检索：架构细化、触发器、Context、事件日志、动态加载工具和对话节奏。正文读取状态记录在 HTML 的 14 项来源表中。
- 完整阅读工具返回的 8 月 31 日动态工具评审逐字稿和 8 月 11 日事件日志评审逐字稿。9 月 1 日逐字稿返回截断，未用其作完整会议结论。
- 核对 v11 235 个图中节点、307 条边与 v12 116 个节点、87 条边；23 个旧分组和全部 235 个节点均有核对条目，30 个原始编号全部列出。
- 27 个工具名逐项与本地 SP 定义核验匹配；不把它称作生产环境全部工具，不把动态工具方案计为已上线第 28 项。
- 12 类需补回的关系、12 项连线/口径纠正、12 项建议验收指标及产品经理掌握深度。

## 证据缺口

- Director PRD 跳转的短期打断体验、长期四合一专项文档读取返回 131006 无权限；不使用已划掉的旧节奏表顶替。
- 9 月 1 日内审逐字稿不完整，不能据此认定 8 月 11 日的 Input Manager 对齐待办已解决。
- 本轮没有运行时注册表、实车 Trace、生产发布/新 Task 迁移验证。
- 现行连续状态聚合、卡片过期/卸载与重复调用契约等仍需核验。

## 检查与收尾

- `test-audit.mjs`：DOM、内部锚点、筛选、展开、235 节点覆盖、30 编号、27 工具名与 SP 一致、来源缺口显式、无外部资源请求，均通过。
- macOS Quick Look 生成本地 HTML 首页缩略图，已实际查看；首页字号、间距、换行与宽度无可见溢出。此检查不是整页或手机端浏览器视觉验收，不声称后者已完成。
- 本轮创建的浏览器资料标签 3/4/5/6 已关闭；原有画板标签 1 与 v12 HTML 标签 2 保留。没有关闭用户 Chrome 标签。
- 在 Codex 请求打开核对 HTML，返回 queued；交付以文件链接为准，不声称用户已经看到预览。

## 后续重画约束

从 `module-crosswalk.json` 而不是缩水后的 v12 节点集合开始；保持全部旧模块有解释的去向。旧链、新方案、规划和待核事项分版标注。必须将每根线的起点、终点、传递内容与状态写清，并进行真实缩放下的相邻框/回线视觉检查，再发布新画板。
