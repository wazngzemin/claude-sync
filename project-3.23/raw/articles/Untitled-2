
### 4.2 设计风格
- **单页应用风格**：左侧快捷按钮 + 右侧即时操作区
- **效率优先**：减少页面跳转，快速完成操作
- **色彩方案**：
  - 主色：绿色 (#4CAF50) - 按钮、活跃状态
  - 警告色：红色 (#f44336) - 逾期提醒
  - 提示色：橙色 (#ff9800) - 即将到期
  - 背景色：浅灰 (#f5f5f5)

---

## 五、数据模型

### 5.1 图书 (Book)

```json
{
  "id": "uuid",
  "name": "书名",
  "author": "作者",
  "floor": "楼层",
  "type": "书籍类型",
  "status": "available | borrowed",
  "createdAt": "创建时间"
}
```

### 5.2 借书人 (Member)

```json
{
  "id": "uuid",
  "name": "姓名",
  "idCard": "身份证号码",
  "phone": "手机号",
  "deposit": 100,
  "createdAt": "注册时间"
}
```

### 5.3 借阅记录 (BorrowRecord)

```json
{
  "id": "uuid",
  "bookId": "图书ID",
  "bookName": "书名",
  "memberId": "借书人ID",
  "memberName": "借书人姓名",
  "borrowDate": "借书日期",
  "dueDate": "应还日期",
  "returnDate": "实际还书日期（空表示未还）",
  "overdueDays": 0,
  "penalty": 0,
  "status": "borrowed | returned"
}
```

---

## 六、关键业务规则

### 6.1 借书规则

| 规则 | 说明 |
|------|------|
| 借书人需提前注册 | 必须是已注册的借书人才能借书 |
| 图书状态为"可借" | 只有 status = "available" 的图书才能借出 |
| 管理员手动设定还书日期 | 借书时由管理员指定还书日期 |

### 6.2 还书规则

| 规则 | 说明 |
|------|------|
| 自动计算逾期天数 | `overdueDays = max(0, 当前日期 - 应还日期)` |
| 逾期费用计算 | `penalty = overdueDays × 10元/天` |
| 从押金中扣除 | 还书时自动从借书人押金中扣除逾期费用 |
| 更新图书状态 | 还书后图书状态恢复为 "available" |

### 6.3 押金规则

| 规则 | 说明 |
|------|------|
| 首次注册需缴纳押金 | 默认 100 元 |
| 押金不足时禁止借书 | 当 deposit < 0 时，不能再借书 |
| 扣除记录保存 | 每次逾期扣费都记录在借阅历史中 |

### 6.4 数据存储

| 规则 | 说明 |
|------|------|
| 存储位置 | 浏览器 LocalStorage |
| 数据持久化 | 关闭浏览器后数据保留 |
| 数据迁移 | V2 版本支持导出/导入数据 |

---

## 七、核心流程图

### 7.1 借阅管理流程

```mermaid
sequenceDiagram
    participant 管理员
    participant 页面 as 前端页面
    participant 存储 as LocalStorage
    
    管理员->>页面: 1. 选择图书
    页面->>存储: 查询可用图书
    存储-->>页面: 返回图书列表
    页面-->>管理员: 显示可选图书
    
    管理员->>页面: 2. 选择借书人
    页面->>存储: 查询已注册借书人
    存储-->>页面: 返回借书人列表
    页面-->>管理员: 显示可选借书人
    
    管理员->>页面: 3. 设定还书日期
    管理员->>页面: 4. 点击"确认借书"
    页面->>存储: 创建借阅记录
    存储-->>页面: 保存成功
    页面->>存储: 更新图书状态为"已借出"
    页面-->>管理员: 显示"借书成功"
    
    管理员->>页面: 5. 还书操作
    页面->>存储: 计算逾期天数
    存储-->>页面: 返回逾期信息
    页面-->>管理员: 显示逾期费用
    管理员->>页面: 6. 确认还书
    页面->>存储: 更新记录、扣除押金
    存储-->>页面: 保存成功
    页面->>存储: 更新图书状态为"可借"
    页面-->>管理员: 显示"还书成功"
```

### 7.2 图书管理流程

```mermaid
flowchart TD
    A[开始] --> B{操作类型}
    B -->|添加图书| C[填写图书信息]
    B -->|查看列表| D[显示图书列表]
    B -->|编辑图书| E[选择图书]
    B -->|删除图书| E
    
    C --> F{信息完整?}
    F -->|是| G[保存到LocalStorage]
    F -->|否| H[提示填写完整]
    G --> I[更新列表]
    
    D --> J{搜索关键词?}
    J -->|有| K[过滤显示]
    J -->|无| I
    K --> I
    
    E --> L[修改/删除]
    L --> G
    
    I --> M[结束]
    H --> M
```

---

## 八、技术架构

### 8.1 技术选型

| 层级 | 技术选择 | 理由 |
|------|----------|------|
| 前端框架 | 原生 HTML5 + CSS3 + JavaScript | 轻量、无需构建、容易部署 |
| 数据存储 | LocalStorage | 无需服务器、数据持久化 |
| 样式方案 | 纯 CSS (Flexbox/Grid) | 现代化布局、无依赖 |
| 图标 | Unicode 字符 + 简单 SVG | 减少依赖、加载快 |
| 日期处理 | 原生 Date API | 足够满足需求 |
| 唯一ID | crypto.randomUUID() | 简单可靠 |

### 8.2 目录结构
