2026年7月24日 下午 1:02
47分钟 12秒
关键词
原子 事件 触发器 动作 规则 模块 座舱 信号 流程图 事件日志 条件判断 事件触发 业务流程 可视化平台 埋点数据 座舱场景 模型场景 动态条件 
文字记录
韩杰 00:06
胖哥，我们先开始吗？OK，好，大家能看到屏幕吗？哈喽，大家能看到屏幕吗？OK，然后我快速说一下，就是目前的话，其实是本身是基于端状态去做的这个需求，因为其实看端状态到底，比如说上传哪些内容，具体有哪些模块去用，然后在这个基础上的话，其实是目前去发现我们现在整个座舱的话，缺少了一个场景编排的能力，对，所以就单独去做了一个场景编排的一个需求，然后去前面的话就简单给大家去同步一下背景，就是为啥要做这样的一个场景编排，这个名字的话，我们后面大家可能都会有一些考虑，跟这个没有关系，我们先。

张航 00:11
要不我们先开始吧。

韩杰 01:02
对齐就是他具体做的事情，因为第一个事情目前会发现，比如说静态adviser，动态adviser，或者说端侧我们要去做的一些主动服务，以及现在 context 要做的事件日志，以后可能我们去做的一些任务大师，以及现在比如说明骏要的一些异步消息，其实他依赖的都是某一类型的原子能力，就是什么时候触发，在什么条件下触发，给用户执行什么样的动作。

韩杰 01:33
他们比如说不同的动作可能触发adviser，或者说生成事件日志，也就说实际上我们当前的整个需求的就是核心思路，就是把整个座舱的能力原子化、标准化。那也就是说我作为一个原子化的标准化的能力以后，那就可以给到我的这些相关的业务，自由组合，自由去使用。那也就是说实际上后续各个需求有了需求的话，先看我的原子能力库有没有，如果有的话就可以直接拿去用，没有的话统一我们排期，就是也一批一批去新增原子能力，那提升相应的迭代速度。然后现在的问题就基本上不多说了，就是可能现在的话就是各个模块做各个模块的，互相之间的关联可能不会特别清楚。然后下面的话就是说我们为什么要去做的一些具体的内容，然后这的话我具体去拆解了一下，就是可能我们现在比如说静态的advisor，动态的advisor。就是主动服务，或者说是，就是现在嘉锋在赛力斯上提的，就是客户现场要的一些离线的场景，或者说是我们生成一些事件日志，或者说是要给到 director 生成一些异步消息，这些基本上都是有触发、有条件，具体执行某一类具体的动作。

韩杰 02:54
这的话，痛点不多说了，我价值的话其实也不多说了，刚才也说过了，有原子能力，各个业务方如果要去用的话，或者说各个功能方，就是我们自己的功能方要用的话，它可以相当于是。一次开发，所有模块都可以去用。场景编排层的话，这个的话就相当于是我们去定义了一个，各个业务去用的一个统一的规则。也就说基于这个规则的话，云端和客户端它其实都是这样的规则去去确定流程该怎么走，怎么样去做监听判断什么样的条件。远期的话，其实我们也会想着去把现在的平台，触发器的那个平台去做一个优化升级，这样的话其实可通过可视化的编排，其实各个业务方，比如说谁想，比如说想去增加一个事件日志，那直接在平台上去做一个配置，那我们车端就可以去做生效。以及这一套原子能力，因为现在现在我看了一下，现在我们没有专门的埋点的需求，但实际上整个这个平台的，不是整个能力的运作怎么样？语音运作的怎么样？其实这个我们将来会加一些相应的数据，那这部分呢，原子能力的是，能力的使用的话，其实我们直接会和那个数据闭环去打通，云端可以去做一些相应的查询啊。

韩杰 04:18
然后也就是说一句话同步一下，也就是说其实现在去做的 AI 场景的编排，其实并不是做一个新的业务，而是说在我们现在已经做了和规划要做的一些能力上去把这些能力去打散。做一些标准化的原子化能力，那这样的话其实是相当于是一次投入，长期复用。然后简单去再说一下，我们这一套能力的话有三个，这一套引擎的话有三个能力，触发事件、条件和动作。然后触发事件的话，比如说下面会具体讲，这举个例子，它其实就是讲的就是发生了哪一些事情。条件的话偏向于状态，就是我判断当前是否是怎么样，它其实就是基本上就是是和否。

韩杰 05:08
然后动作的话类型其实比较多，比如说可能常规的场景引擎的。我去调车控，打开天窗，但是我的触发那个对应的主动服务也可以，我去生成事件也可以，我去生成异步消息也可以，这些都属于我的动作的类型。整体架构上来说，也就是说实际上我们会把车企的的信号， AI Box 的信号，或者说一些用户的行。为通过DMS、 OMS 或者VLM，或者说车机底层信号，给到原子能力层，那原封装成，比如说我基于一些状态的变化，那我可以封装成触发事件，基于一些状态的值，我可以封装成条件，基于我可以去调通的动作，具体执行的一些动作，我可以去定义成动作，那基于它的排列组合，那我们就可以去生成一些各种场景，这些场景就可以满足各个业务去使用。

韩杰 06:06
然后场景的原子能力的话，大概简单给大家说一下，就原子能力的话，就是我们就不直接看那个统一的概述了，看具体的定义。也就是说可以被启动的事件源，也就是说它是为了去唤醒某一个场景的流程。然后目前的话就，也就是说车况，这个是和车机硬件相关的。然后用户操作事件可能是通过埋点，或者说其他方式我们获取的，也就是说现在常规的，我想知道用户点击了哪一个按钮，进入到哪个页面，他有没有发起导航，有没有切割，以及环境感知，比如说我 VLM 感觉到的一些信息， CV 小模型，DMS、 OMS 感觉到的一些小模型，或者说我加一些定时器Timer。就是我定时去做哪些事情，因为车上可能会做一些任务的轮询，以及软件状态。

韩杰 07:01
这里的话就列举了，车机上所有可作为触发事件的，这是一个穷举值。但我们现在实际实现的时候，可能会有一些情况，我没有相应的底层能力，这部分原子能触发事件，其实是现在实现不了的，但我们后续可以慢慢去加，但基本上这个就是一个全集，理论上不会有这个集合以外的。

韩杰 07:25
下面的话就是去针对于这个内容去做了一些具体的举例，比如说车速的变化，我可能出行adviser，我可能关注车速。或者说我要去做一些，比如说用户控车的时候，我可能关注一下当前的具体的车速状态。然后这些是偏向于触发，也就是说满足这些条件的变化的时候，我就要进入到某一个场景。具体的所有的 case 的话，大家可以下来去详细看一下。

韩杰 07:57
触发器它最终不光是触发一个场景，它其实还会有一些相应的输出，也就是说它会有一些附带消息给到下游去使用，当然下游的话可用可不用。目前它生成的消息的话，我触发这个事件的名字到底是啥？我的当前的话就是这个触发器，触发那个事件叫档位切换，我可以去记录这个触发器它它的状态的变化值，它状态的之前和之后，那我就可以知道，OK，档位是从由 P 档切到了 R 档。我也会记录当应的相应的触发时间。当然的话就是我们后续的业务里面可能会增加一下这个到底是谁触发的，但这个的话其实可能要和那个音区去关联起来，但这个三个就是标准的，就是。我的触发器就是可以给到下游的一些相应的信息，这是针对于触发事件。

韩杰 08:52
接下来是条件，条件的话其实就是判断我当前的这个场景是不是要继续执行，支撑所有流程分支的。目前条件判断的话，基本上就是三类，也就是说实际上是数值类的，也就说有可能我们空调的温度，我判断空调的温度是不是在 25 度，或者说是不是大于 25 度，或者说布尔值，也就说是和否，或者也就说当前是不是在播放，当前是不是在听歌，导航，这些就是 0 和 1 的一个状态变化，但实际上就是。数值类判断和状态类判断，其实可以归结成一类，因为它基本上就是判断我是不是某一个值。

韩杰 09:33
第三个的话就是除了这两个以外，我们然后也会有时间段的判定，也就是说实际上，比如说我用户上车，我开车是不是已经大于 30 分钟？或者说我当前的时间是不是已经晚于 9 点？因为直接比晚晚上 9 点的，我可能我们有一些推荐或者是执行，其实是有一些差异的，所以这会有一些相应的条件的判断。然后下面的是也是一个当前基于车端的所有的状态，我们可能去获取到的一些值，但这部分信息不是，它和那个触发器一样，它不一定完全来源于端侧端侧的信号。有可能是 AI Box，有可能是云端的API，比如说海拔，因为我如果说能获取到用户当前的海拔，我可以去做一些相应的事情。就是它不是完全来源于车端的信号的。然后比如说可能就是我通过一些光线传感器，或者说我去拿一些当前的日出日落的时间，这些其实都可以去做一些我的条件的判定。然后目前座舱的话就是这些是相当是座舱给的，座舱加上云端的 API 给的，但实际上座舱也有LM、VLM，然后模型其实它的结果它可能不会像上面的一个固定的结果，所以其实我们也接入了一个，就是 AI 条件判断。它是可以基于动态的知识去进行。

韩杰 10:58
稳定输出一个稳定的结果，也就说是举个例子，就是最近的一个 case 就是宝宝如果哭了的话，你跟我说一下。那也就是说实际上我们会把 VLM 作为一个标准的原子能力去，就是判断它输出的结果和目标的任务，通过一个小模型判断它的结果是 true 还是false，也就是说任何这种非规则性的，我们都可以调用一个基本的小模型判断能力去判断它的布尔的结果。

韩杰 11:30
对，这个的话是针对于大模型场景，我们去会增加的一类原子能力。然后最终的结果的输出的话就是是和否，也就是说这个场景是不是往下去做。这是条件这部分，然后第三类的话就是第三个原则是动作，也就是说这个系统到底能执行哪些事情。目前的话我们会分为这几类，比如说生成日事件日志，调动相当于是调advisor，或者说调某一些硬件软件，其实我们也会去陆续把，比如说我弹一些toast，我去播报一些语音，或者说我模拟语音输入给到director，这些我们都会封装成原子能力。然后下面的话比如说给 context 用的，给 director 用的，给 advisor 用的，比如说给具体，比如说我们端侧自闭环，只执行某一些动作的，这些我们都是可以去做打通的。

韩杰 12:28
整体上来说，大模型座舱的话，其实执行的动作，其实以前的场景引擎，它可能执行的就是某一个车控的动作。但我们现在的话，会把规则升级成大模型的推理，也就是说实际上动作里面会新增很多自然语言的一些文本，它其实是为了给到下游的大模型的，所以它其实是会增加很多的变量，也就是说实际上这些动作，我的事件日要拼一些时间的变量，我可能要拼一些，就是状态值的变化。

韩杰 12:59
那这部分的话，我们会接入第第四个原子能力，它叫变量。那目前的话，就是座舱其实已经有一个端状态，然后我们。基本上等同于端状态，就是现在要的一些变量。这两个的话就是在一个表格里维护，但实际上所有的变量的话，我们会做一些新增。这个后面再给大家讲。动作的输出的话，就是动作，我可以去执行某一些动作，我可以把这个动作去给它记录下来。这的话是一个简单的规则，我们直接看举例。也就是说，这个可能是我通过事件去记录下来的，因为我发现我去发现当前可能是定时，我设置了一个定时任务，定时任务的是有，用户的驾驶时长超过 2 小时的时候。我去把这个生成一个事件日志，也就是说实际上我去定一个对应的模板，我把相应的变量去拉过来，我就可以生成一个自然语言的消息，这个消息可以直接去播，我也可以给到 director 去做下游的一些相应的动作。这是我针对于我们触发事件条件和动作的三类的说明。

韩杰 14:15
下面的话就是，这个是我们当前的一个原子能力的清单，座舱的原子能力清单的话，我们基本上就以这个为准。也就是说各个。项目无论是上汽还是赛力斯，我们都以这个清单为准，只不过这个清单去实现的时候，依赖于端侧的一些信号的接入，快速的给大家看一下。也就是说触发事件是哪些？比如说档位，我支持去判断到底是切入还是切出，它的值到底是多少？我可以定义一些相应的模板，也就是说这个事件触发的时候，我可能生成什么样的一个事件规则？比如说这个的话就是几点几分，档位由什么时候变成了什么？这部分其实是可以给到下游去使用的。目前的这个规则是给到吉利。 context 的事件日去用的。然后第二个是条件，条件的话就是代表状态的一些，我到底是开还是关？然后主驾到底是几度？温度是多少？然后动作的话，这个是我们当前去生成的一些，我可以去执行某一些动作，比如说我端侧自闭环的一些场景，我可能要调雾灯，我可能要调驾驶模式，我可能还要调生成对应显示主动服务的卡片，然后我要调事件日志，然后我要去调advisor，然后状态查询其实就是我们当前的端状端状态，这是我们当前的一个大表。

韩杰 15:42
然后这简单去讲了一下，就是说原子能力的标准化和项目的定制。也就说实际上我们整体上希望的就是，无论是哪一个项目，这些也就说触发事件、条件和动作状态，其实对我们火山座舱来说，它就是一张表。也就是说这部分就代表我们研发和产品管理的是一回事。但是因为车企底层的信号会有一些不太一样，或者说它有一些不同的枚举值，那我们去设定了一些相应的规则去解决。OK，它在我的范围内，我就只把信号接进来就好。所以我们有一个标准的字典，如果说是它的一些相应的枚举值和我们的定义不太一致，我们到时候去做一个车型的映射规则，去把它的对应的一些职能映射进来，它可以是我直接把信号拿过来，作为多对一、一对多，或者说是我直接把它的值透传作为一个我们的 value 值，做一个输出。

韩杰 16:43
这有个大概的规则，具体的规则不给大家去详细去说了，但整体上来说我们会希望，它整个原子能力的中枢的话，其实对我们来说就是一个标准的，对内的标准的文档，对客的话。就是自由的去做勾选，勾选完以后，客户端或者说云端，或者VLM，或者 AI Box 去做不同的能力接入。然后整体原子能力交付的话，目前我们去定义了两个阶段，我看一下。OK，这的话其实是和上面的，定制的这部分内容怎么样去做的一些相应的规则，这的话就不多说了，就是我们实际使用的，然后再去说。

韩杰 17:30
这的话就是再罗列了一下整个原子能力的大概的开发的流程是什么样子，比如说各个业务方，比如说是Advisor，比如说是条件任务，或者说是一些静态的Advisor，也就是说实际上。都可以去把自己的任务转化成一个基本的流程，也就是说场景编排，一会给大家去看一下场景编排大概是什么样子的。

韩杰 17:54
化成场景编排，它其实就是一个流程图，那就可以拆解出你需要哪些能力，你需要哪些触发事件，你需要哪些条件，你需要哪些动作。查原子能力库，如果有的话，我们将来直接平台配，或者说是没有平台的时候，我们把原子能力的这个流程画出来，去框选出来我需要有哪些原子能力，给到研发，研发就可以去做实现落地。如果没有的话，相当于是统一提需求。我们会定时定量地去把某一批可能不同业务方的原子能力都去开发完，开发完以后，那我能力接入以后，那就可以实现能力，对应功能的开发。

韩杰 18:36
OK，上面是讲的就是说，OK，原子能力是什么样子？那接下来的话就是，当我有了这些原子能力的时候，我怎么样去实现？然后执行模块的话，基本上你可以理解为就是一个把流程图上面的原子能力去执行的这样的一个过程。那么也就是说，它负责事件的监听，因为时线监听就是触发事件什么时候能够满足。

韩杰 19:02
以及我的条件，我是不是满足了一些我这个流程继续往下走？以及我动作到底执行哪些动作？然后这个动作在后端调的是哪些接口？然后这些，这都是在这个执行模块。然后那个目前的话，也就是说实际上执行模块和平台，其实它可以解耦，也就是说实际上我有了一个流程，那个流程图，那我其实研发就可以去实现。但我们那个后期的话，会有一个可视化的平台，那这个平台其实是。可以去做发布，比如说我云端平台上去下发我的流程图，那客户端和服务端都可以基于我的流程图知道，OK，我该调哪个原子能力，调的顺序是什么样子？串行还是并行？那我最后该去执行哪些动作？那这个模块的话，其实主要其实就是做执行，解析解析这个流程图和执行。

韩杰 20:00
然后整体上我们之前可能会有一个问题，就是现在的整个触发器其实是云端的，但实际上我们希望就是当前的这一套，无论是原子能力还是说整个执行模块，其实都是端云协同配合的。然后整个分工的话，其实我们会认为就是绝大多数触发和条件判断其实是放在端侧的，因为它距离信号最近。然后只有依赖于云端模型去做推理的，比如说VLM，就是我刚才所说的场景，判断后排宝宝是不是哭了，它其实是需要一个小的大语言模型去帮忙判断 true 和 false 的，那这部分我们会放在云端，也就是端侧主要承接可以自闭环的场景，或者说是涉及到安全隐私，那我们也会放在去端侧。

韩杰 20:46
然后目前来说绝大多数事件触发和条件判断，其实都可以在端侧执行，然后那个保证无网弱网的时候，因为实际上我们座舱场景的话，会出现大量的无网弱网的情况。然后云端的话其实主要是做的就是相关云端模型的推理，比如说我Advisor，它其实就是一类模型的推理，比如说我调那个。 director 的它是，或者说刚才所说的就是，我通过与需要云端模型去做一些条件判断的。然后统一编排的话，我们做预期的就是，无论这个场景到底是在端侧执行还是云端执行，它其实都是一个编排的能力。也就是说，实际上我可以通过一套编排快速地去调云端，还有端侧的相应的能力。然后下面是一个整体的链路，就是首先我们无论是那个对应的我们。我们先看一下图啊，也就说这是专家任务集里面的一个case，就是用户突发副，那个疾病，去三甲医院，怎么怎么样做。其实它是有一个对应的流程的，也就说我发起导航，我可能判做一些相应的目的地的判断，我判断一下 VLM 去识别的用户的一些面部的表情，我给到director，我主动服务，比如说用户上车，我去做一些相关的迎宾，或者说我的条件任务，比如说用户设置的一些条件任务，或者说定时任务，或者说长时任务，基本上我们都是可以转化成这样的一个流程图。

韩杰 22:19
这个流程图里面的所有的节点都是原子能力，也就说这个原子能力就是刚才给大家所说的原子能力库。也就是说原底链路的话，也就是说产品要去定义自己流程大概是什么样子，需要用到哪些原子能力。比如说，今天 AI 播客说的，OK，我要增加一个触发事件，这个触发事件就是判，你讲。

张嘉锋 22:45
我打断一下，韩杰，就像比如说你遇到了一些场景啊，比如说他看到大家有人生病啊，或者这种应该都是 OVZ 去识别，我理解条件任务它就是具体的条件，或者说有可能有点模糊条件，你可以有一加个 AI 判断，但是更模糊的就应该不需要这里来判断了。

韩杰 23:09
那个判断我也可以封装成一个叫 AI 判断的能力。这个 AI 判断的话，它会相当于是，比如说结合视觉的判断，也就是说判断车上发生的情况和用户的要求是不是一致的。那这个时候 director 或者说是目标，那个叫任务中心会给到我一个任务，那我会固定的去调视觉的模型，把它的结果拿回来，然后再去调一个，比如说，嗯嗯。

张嘉锋 23:37
我能理解你的逻辑，这是一个脚本的逻辑嘛。但是这种判断它需要依赖当前的情景，还有用户的偏好、记忆，以及对于 Advisor 给他设定的目标、 knowhow 等等来做综合判断。所以这种就不适合在这里做编排了。但是没有必要，因为我们希望是用 oa long 去做这个事情，不是人工去预设这些场景。如果是人工预设的话，没有人能预设得好这个事情。你做是能做得出来，比如说我强行定好一些逻辑啊，先做A，再做B，再做C。但是这个人为预设的A、B、C，我我觉得这种可能并不是合适的。

韩杰 23:55
也可以编排，这个也可以编排。

韩杰 24:09
这个，嗯。

韩杰 24:23
就是我不觉得这套架构满足不了你刚才所说的场景。

张嘉锋 24:29
就这个难点就在于，我们就不希望这种场景是人为先定义好的这样的一些workflow。

陈婧文 24:37
诶，他这个是不是区别是在用规则限定掉多少？比如嘉锋你刚刚说的那种，是不是他走这个我，韩杰现在这个workflow，他就是走一个，那个事件描述的上报啊，因为你上报了以后，你的逻辑是在 direct 里做的嘛。

韩杰 24:59
对，也就是说婧文你说的是对的，也就是说其实这一套逻辑只是负责一些简单的分发，但是这些由模型快速的基于不同的状态去推理，其实还是会走到 director 或者adviser，不是，而不是说是我这里就直接把所有的节点判断都完成。

张嘉锋 25:25
对，如果是这样的话，他就只是一个触发，而不是触发完他再决定第二步怎么触发，第三步怎么触发的流程，脚本流程了，对吧？

韩杰 25:34
对，也就是说，其实你看那个具体的场景编排，这个就是主动服务，我只是把用户上车这个事情去触发起来，同时我把一些简单的规则，也就是说我该去迎宾的时候，设定一些简单的规则，我会给到模型去做后面的处理的，而不是说我直接用户上车了，我满足规则的时候，我就直接给他播一个话术，执行某一些动作。其实这个就是我们现在的所触发，这个是条件的，这部分没模型一定是没办法去完全搞定的。

张嘉锋 26:11
对，那现在我理解，就现在说的一整套，跟当前的触发器有什么区别？不就是当前触发器要做的事情？

韩杰 26:20
对，所以第一个的话就是把它原子化了，现在的触发器就是基本上都是条件，现在的话是把它拆分成了触发事件和具体的条件，这一套架构的话就是所有模块都可以去用。

张嘉锋 26:36
就是我理解跟原来的变化是，现在第一把条件拆成了触发事件和条件，第二增加了一个 AI 识别的条件。就这两个变化，对吧？就一种。

韩杰 26:48
不是增加了一个，不是增加了一个，对，就是 AI 判断的是一个，或者说其实以这个图举例，就是判断后排宝宝是不是哭了，这是一个长时任务，那也就是说整个这个模块，当然我，就是这个模块，也就是说整个任务编排，不是说是预设的，不是，这个任务其实我不会预设，而是说我有一个任务编排的底层架构，那它可以灵活地去和任务中心去打通，也就是说你需要什么样的能力，我可以快速地调动，也就是说任务中心告诉我说，OK，你需要。

韩杰 27:27
基于视觉来判断我的目标有没有达成，也就是说当前的任务是宝宝告诉我宝宝有没有哭，这个时候我接到这个任务的时候，可能会有一个延时的机制，我去获取 always on 的结果，我拿到 always on 的结果，我再去调一个小模型，这个小模型是指云端的 LM 模型，基于这个任务和那个 always on 的结果去给出来一个 true 和false，我只是这部分是那个是它其实是有模型在里面，它不是一个纯规则。也就是说我会基于这个东西去告诉 director 当前用户的任务，目标，我有没有已经实现了？这是他和 director 配合的一个场景。

张嘉锋 28:18
O- OK，但这不就是现在触发器正在做的事情，是需要对现在触发器做升级，对吧？

韩杰 28:27
对，升级也就。

陈婧文 28:29
其实是应该算是一个延续吧，我那个韩杰写这个的时候，我跟他讨论也把触发器的几个 PRD 看了，仁杰的触发器到现在的版本里面，我觉得大的思路和韩杰这个基本是一致的。可能缺在两个地方，一个是一个是原子化的一个输入的规整，这个因为前段时候你们讨论了，也已经在做了嘛。

陈婧文 28:58
还有一个就是端云的协同，仁杰那份触发器的需求里面，更侧重的是云端，端侧那份没写，拆给了泽铭去写端侧的。端，但是泽铭的端侧触发器的需求是比较就可能因为刚好现在有 VLM 的需求，所以他那份写得非常的针对VLM，他并不是一个就是更能 general 一些囊括囊括各种端侧情况的一种架构，他更多的是去解决了 VLM 的单点需求。

陈婧文 29:34
所以就是像那个韩杰这份，我觉得叫什么名字都 OK 啊，就可能也考虑用我们后面对外。叫什么能显得就是一点？就可可能触发器是个特别实在的叫法，反正怎么怎么叫我们可以再想一想。但整体要做的事情还是就基于原来的那个人杰触发器的那个需求，把它做一个规整，完善，然后把它能够各种情况能放到一个框架里，当然它走的路可能是不一样的，每种情况在这个框架里走的路或是不一样。然后今天我看泽铭和谁都没上，和仁杰都没上。张航你觉得。你这今天这样评行吗？还是说单独组一个把这个事情以前做。

张嘉锋 30:14
了解。

张航 30:33
单独拉我，就小范围吧，拉韩杰跟泽民和仁杰同步一下，把这个事情，就是刚才说的几个点，让他们知道就好了，应该。对。他们那边应该这两天都在。灯亮调好了。在改动态实例，估计在那边弄没上。一直开着吗？还是是在。

陈婧文 30:42
啊，姜锋也加上，嗯。

陈婧文 31:02
那怎么弄呢？现在线上的同学还有没有什么有问题，或者要讨论的？

张航 31:09
你好，主驾。做了呢。要换一句吗？我们逻辑上应该没什么，我觉得。好的，给你放。

陈婧文 31:19
嗯，那下次再找个时间，小范围再对一下后面需求怎么拆吧。

韩杰 31:35
要不我把后面的先大概讲完。嗯，没关系，我先讲一下吧，因为我开了录屏。

陈婧文 31:39
也行，但这样你可能会又讲两遍，哈哈哈，要没关系的话，行行好，行行，到时候让泽铭和仁杰也提前看一下录屏，以后直接后面找范梅聊一下往后需求怎么拆。

张航 31:46
对，我们剩下小的，到时候简单说说就好了，应该很快。

张航 31:58
嗯，好的。

韩杰 32:01
OK，刚才找到，这个场景编排，整个执行模块的话，它其实负责触发事件的监听，这个其实就是现在的触发器里面的动态条件。条件判断的话，就是触发器里面的现在的那个条件。场景编排的话，实际上我们自己的业务里面，实际上会涉及到就是整个业务流程的串行、并行，或者说调度多个能力的。但是那个当前的云端的那个触发器的话，基本上就是一个链路的并行，同时满足若干条件的时候，就执行某一些动作。但实际上我们可能会有一些能力是需要去串联起来。

韩杰 32:45
说我可能就是，我一假如有一个任务的时候，就是车速大于多少的时候，其实底层信号是没办法告诉我说车辆信号是维持在60，已经 10 分钟了，那我们可能就要加一些延时，比如说我判断一下当前车速是多少，我过 5 分钟判断一下车速是不是还是维持这个值，我去往下面的流程去执行。

韩杰 33:09
也就是说这一套场景编排能力，我们是相当于是需求都是这样子，端云执行的时候，都可以按照我们定义的这个流程去把上面的各个原子能力去给它串起来。这是针对于执行模块所做的事情，也就是说这个的话会给到赵衍以及云端刘杨去做整个原子能力改造，因为我们希望就是端云是一套架构。至于怎么样去达成，研发内部再去聊。然后下面的话简单就是说一下，比如说一些其他东西，整个的话就是可能优先级的一些判定，然后就是我们将来可能去做哪些数据的上报，因为实际上整个流程业务的那个执行模块，它其实是一个漏斗，也就说我可能触发很多。但我条件过滤其实过滤了一些内容，最终执行的时候比例会比较小，这些我们会做一些将来线上的埋点数据的统计。

韩杰 34:08
然后模块上下游的话就是，上游的话是原子能力，这个原子能力的封装，这个我们到时候就以当前的这个需求作为第一版需求给到研发去实现。第二个的话是可视化，可视化配置平台，然后这个的话就是现在已经有一个平台，这个只是平台的升级能力，它的优先级没那么高。然后第三个的话是是任务管理，也就是说实际上当前我原子能力的所有的触发和条件其实都是任务管理的一个子集，它有点偏向于现在的那个。文生场景，也就说到底它需要生成哪些任务？其实我当前的原子的能力都是它的一个组件，也就说它可以基于我的这个原子能力和我的这场景编排规则，它动态的去生成一些任务的管理的逻辑。然后信号中枢的话就是底层的端侧，就是赵衍的AI， AI services，也就说能够把所有的信号集中起来。然后下游的话就是我们刚才所说的，可能日志上报，或者说director，或者说是具体的座舱的域控，因为实际上我们也承，这一套架构也承接了端侧的一些主动的服务。然后可视化平台就不给。

张航 35:21
诶，我韩杰，我问一下啊，我们后面比如一个新项目来，是说就要把他车的所有信号都对接上，是吧？对啊，就对到，那个类似于信号中枢里面嘛，但是后面的你得先全都对完，将来才能随便配置，才能用嘛。但是接的时候就是都要尽量全接，相当于。

韩杰 35:30
他只对接到我们的原子能力清单。对。

韩杰 35:45
对，是的。对，但全接是指，是按照我们的需求去接，而不是说是只要他有一个信号，我们就接到我们的原子能力库里面。对，也就说以前他可能只是接端状态，但我现在因为我的业务情况不一样，我的，你要满足我的触发事件，你要满足我的条件，这些我要的信息你也要给我，相当于是把原来端状态只是说要某一个信号 key 和value，现在把它据业务去详细，更详细了。

张航 35:58
这就是我们主线定义的那个全量定义的那些，那就只要有的，那我们就全都接肯定。消消光。

韩杰 36:31
胖哥，这还有问题吗？然后可视化平台具体就不给大家讲了，我们直接大概看一眼，大概后续的预期是什么样子？也就是说在页面的话，各个产品，或者说研发可以去创建自己的一些业务流程。业务流程的话，其实它是比如说有触发器，比如说我的事件日志可能想去监控一下档位，档位切换的时候你要告诉我，比如说我执行档位切换完以后， D 的就是用户切出 D 档的时候，OK，你要生成一个事件日志。事件日志的话，我会定义一些它的相应的规则，比如说。是哪个用户在什么时间，把档位切换到了什么档位，最终的车速变成了多少，通过这样的方式，其实就可以去定义好，OK，我事件日志需要哪些信息，将来的话，以事件日志这个动作为结束的时候，其实就可以把这些对应的日事件日，通过那个日志队列的时候，给到context。这个模块的话，到时候我们也会去支持发布到车端，它可以给到云端，它也可以给到车端，将来也可以去做一些线上的数据的运营。具体平台的功能，我就不挨个给大家去说了。

张航 36:34
没了没了。

韩杰 37:59
目，最后一下的话，简单同步一下，就是我们接下来可能会去做哪几个需求。第一个需求的话，就是规则脚本，也就是说无论是叫剧本也好，规则脚本也好，我们想先做一件事情，就是能够在服务端可以下发给客户端，这样它的一个优点的话，也就是说各个原子能力的输入输出，场景的编排，或者说流程图的编排，通过上传，它链路的打通只是其次啊，而是说云和端它的相应的协议。

韩杰 38:37
解析的方案，其实就可以通过同样一份文件去对齐，那这样的话，实际上就是端云配合起来就会比较方便。也就说他的目标是相当于是把端云这条链路能打通，然后把协议能打通，相当于是整个这条技术链路的协议去定义清楚，但它不是一个必须项。第二个事情的话，就是我们现在比较要那个赛力斯当前要求的是事件日志的上报，也就是 P0 状态的，也就是产品提供是指那个对应触发事件条件的判断，然后拼接的相应的规则。

韩杰 39:14
目前其实我已经去提出来了，也就是说触发事件当前列了P0，也就说哪些事件触发的时候，我要生成事件日志，然后它生成的规则大概是什么样子，目前已经罗列出来，然后拼接规则的话，将来平台上也可以自己去写，然后客户端基于我现在给出来的规则，也就说实际上触发事件相对来说比较简单，也就是说它只是 OK 有一个触发事件，然后它就直接调一个动作。所以这个规则比较简单，也就是说我们列一个表就可以去实现。那客户端去基于我现在定义的事件规则，就可以去把那个对应的编责规则去编好。然后当满足这些触发的时候，比如说档位切换，比如说发起导航，比如说是空调开关能调节，那他就可以去监听这些相应的示号信号，然后把那个事件日志生成，同时上报给Context。这个的话，也就是说实际上我们所要的是端侧的脚本。能够在端侧触发，端侧也能够执行生成事件日志，把这个链路去打通。

韩杰 40:28
第二个的话就是我们去要把这个信息给到云端，也就是说仁杰的 context 部分能够拿到这些信息去做相应的数据转化，无论是筛选还是说排序，还是说要做一些文本的修改。这个是我们当前比较重要的一个事情，就是赛力斯的事件日志。第二个的话就是我们现在还承担了一个一部分需求，就是赛力斯比较看重的就是端侧自闭环的这些主动服务。目前的话就是我已经基于之前是泽民去定义的一个。需求以及我自己考虑了端侧比较重要的就是及时响应的一些，去定义了一个脚本。它的脚本的需求，也就是说后续我们的需求基本上就是这样子。

韩杰 41:16
第一个，这个功能是啥？它的大概的逻辑是做什么事情？比如说切到 D 档的时候，如果有儿童的话，那我弹一个主动服务的卡片，让用户去点击正反馈、负反馈，我去执行对应的动作。这可能有一个依赖，就是视觉，这我们要出那个主动服务卡片，这个的话现在 UI 还没有出。然后我们也去把这个对应，OK，我要去把端侧去执行，那个智能开启儿童锁，它的原子能力、触发器哪些、条件哪些、动作哪些，我们都去定义清楚。

韩杰 41:51
这是一个case，这个是。赛力斯没提的，赛力斯提的其实是另一个叫天气路况保护，也就是说实际上我们可能车速，我发现我车速已经比较快了，我去动态的判断一下，这部分是通过 VLM 去判断的，我把这些封装成原子能力，判断一下当前的路面的情况，基于当前车辆的具体的状态，我判断一下是不是符合对应的，比如说我要开雾灯，我看看的雾灯有没有开，过滤掉一部分异常的，不需要去推荐的，我去执行相应的动作。

韩杰 42:26
目前的话，这部分端侧的场景的话，我们会以这样去实现，也就是说我会把这两个剧本给到研发，会把这两个剧本依赖的原子能力给到研发，研发去基于这个去把端侧的去实现。他主要所做的事情就是，我们去在端侧，满足赵衍所说的端侧触发器要做哪些事情？我们目前提了两个场景去把这个东西快速地去上车。但这的话其实就是我们快速讨论一下，就稍微有一点伪需求啊，因为就是我识别到路面湿滑这个东西不一定给，要给到端侧，给到云端可能会更合适。

韩杰 43:06
这个嘉锋是说那个赛力斯比较关注这样的一个端侧场景，我可能会后续会考虑到，就是它其实一个端云配合，如果有网的时候，我可能给到云端的advisor，或者说是某一个advisor，没网的时候可能走端侧的这一套逻辑。就是现在赛力斯提的这些端侧的自闭环的场景，稍微有一些鸡肋。我们还是搞吗？航哥加分。

张嘉锋 43:36
这块就是博总他们比较关注的安全什么，天气路况保护，希望用端侧能够实现。我觉得这个，比如说遇到大雾就开后雾灯，或者说冰雪开雪地模式，湿滑这个感觉我主要担心不一定能识别准，另外两个他端侧直接写死规则做了，这也没什么吧。

韩杰 44:06
它没有高时效的一个需求，端侧的它的优势其实就是我识别到我立马可以执行，这是一个要求啊，第二个就是无网弱网的时候我也需要，它必须得有，但这个东西我也感觉，至少就是从产品需求层面上来说，它不是一个端侧，一定要放在端侧的，当然我也理解客户有一些诉求才，无论合理不合理，我们都得去做。

张嘉锋 44:37
我觉得就是肯定是，如果他能够端云配合是更好的，只是现在他在我们整体的需求量级没有那么优先，我们想先低代价先做好这个，如果这块后面比如说再优化，可以有离在线配合的逻辑，可以再去优化它。

韩杰 44:59
OK，目前我们计划做这两个场景。

陈婧文 45:01
那我估计他们，对，我估计他们发布的时候肯定会说，就是端侧闭环的事情。总要有一些，切实确实实现的场景是在端侧的。如果说没有想到特别高价值的，就先做一些吧，也去验证。对，先有，也验证我们整个那个架构是通的，后面再看端侧要加哪些，以及说哪些要不要调整端云协同。

韩杰 45:19
就少量的先有。

韩杰 45:33
对，所以现在的链，就是当前我去列的这三个需求，基本上就是以最小范围，我们先把这一套链路去打通，架构跑通。后续的话，在这套架构的基础上，其实是前面也说得很清楚，只需要看我有没有这个原子能力，没有的话我去加，加完以后其实基于场景编排，其实可以快速地去上。目前需求大概是这样子。然后下面的 QA 我就不一一给大家过了，就是可能会有一些疑问的问题，大家可以去看一下这部分有没有已经写清楚，如果说还有一些问题的话，我们可以下来再去讨论。江然同学看看还有没有什么问题。

韩杰 46:29
OK，没有问题，那我们就先这样，接下来的话是两个事情，第一个的话就是泽民也上来，泽民、仁杰、嘉锋，我们小范围再碰一下。第二个的话就是碰完以后，我们就拉着研发要去开发了，因为相应的下面刚才所说的三个需求，其实是至少这两个是赛力斯上要交的啊。OK，那我们今天先这样，好，谢谢大家。

